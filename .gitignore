# dotnet。**只挡 dotnet 工程目录下的 bin/obj**：光写 `bin/` 会静默吞掉任何叫 bin 的源码目录
# ——票 91 就被吞掉过 `probe/akagi-wasm/crate/src/bin/parity.rs`（Rust 的 bin 目标就住在那儿），
# 掉了没有任何人报错，只会在别人 clone 之后发现「对拍跑不起来」。
src/*/bin/
src/*/obj/
tests/*/bin/
tests/*/obj/
*.user
.dotnet-tools/
TestResults/
artifacts/

# nix
result
result-*

# Fable（M1 起）。Fable 的输出与 Vite 的产物都是生成物，不入版本控制：
# `cd web && pnpm run build` 一条命令重建。
.fable/
fable_modules/
node_modules/
web/src/generated/
web/dist/

# 编辑器
.vscode/
.idea/
.ionide/

# Python（scripts/ 下的取数与统计脚本被 import 时会生成）
__pycache__/

# 牌谱样本（13 票对拍用；大文件不入版本控制，取法见 run/reports/13-prep-paifu-data.md，
# 全量 12,188 场的拉法见 run/reports/57-wider-paifu-differential.md）
data/paifu/
