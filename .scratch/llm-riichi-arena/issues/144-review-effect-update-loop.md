# 144 — 终局那一刻的 React 更新环：一条 CI 看不见的真红

**What to build:** 查清并修掉 `verify-export.mjs --to-end` 单跑时必现的 5 条
React `Maximum update depth exceeded`，并让**闸门从此看得见它**。

**Status:** ready-for-agent

## 这条是怎么被逮到的

票 133 的 agent 报的票外发现（`DECISIONS.md` 133-7）。它做过阴性对照：
**把票 133 的全部改动回退到 `46d18444`、把新文件挪走、重新 fable，照样必现**
⇒ **与票 133 无关，是仓库里本来就有的。**

## 为什么它值一张票（而不是记个档）

- **合并跑道上不冒**（那一趟只跑 0.8 s），∴ **`ci.sh` 是绿的**。
  这是判据 22 那一族最难看的形状：**闸门存在、能跑、退 0，而它守不到这件事**——
  只在冷启动 / 慢机 / 单跑时开口。
- `Maximum update depth exceeded` 是 **React 在说「有个 effect 把自己的依赖改了」**。
  它不是噪声：真触发时那一屏会掉帧甚至卡死，而它出现的时机正是**终局那一刻**
  ——也就是票 133 的记分卡、票 90 的复盘、票 93 的强 AI 那一行**同时上场**的那一屏。

## 已经查清、不必再查的

- 全仓库唯一的 `useEffect` 在 `src/Janpo.Web/ReviewPanel.fs` 的 `useReview`：
  终局那一刻 `setConsulted Consulted.Untouched`，依赖是
  `[| box seated; box (Review.signature model) |]`。**唯一嫌疑，但没人证实过。**
- `Review.signature` 是「便宜签名」：Live 侧是 `table.Turns`、回放侧是帧数。
  **它被特意做成一个整数**，就是为了不让 `useMemo` 每次渲染都失效（那一段注释写着理由）。
- 复现命令：`cd web && node scripts/verify-export.mjs --to-end --seed 447`（**单跑**，别合并跑）。

## 要什么行为

- [ ] **先复现、再归因、最后才改。** 报告里要写出「哪一次 set 触发了哪一次 effect」的那条环，
      不许只说「加了个判空就好了」——**这一票的价值一半在那条环被写下来**
- [ ] 环断掉：终局之后那一屏渲染次数收敛（给个数：改前 N 次、改后 M 次）
- [ ] **闸门从此看得见它**：加一条断言，让页面控制台里的 React 报错**成为红**
      （今天没有任何一趟在读控制台——这是这一票真正的交付物）。
      注意它**必须在合并跑道上也咬得住**：那一趟只跑 0.8 s，∴ 你得让它真走到终局，
      或者把这条断言挂到一趟本来就走到终局的跑道上
- [ ] 那条新断言**先红一次**（把修复回退掉，它必须报红）

## 边界

- [ ] **不动 `Review.settled` / `ReviewShown` 三态与「复盘只在终局之后」**（票 90 的地盘）
- [ ] **不动 `Review.signature` 的「只给一个整数」**（它是有理由的，见那段注释）——
      真要改就先在报告里证伪那个理由
- [ ] 不动引擎、不动牌谱格式、不动 `Store.fs` 的键
- [ ] **别顺手拆 `verify-home.mjs`**（另一票）；**别碰 `TablePanel.fs`**（票 143 在动）
- [ ] 若与票 133 的记分卡在同一屏上撞了，**以 133 已落地的形状为准**，把冲突写进报告交调度器
