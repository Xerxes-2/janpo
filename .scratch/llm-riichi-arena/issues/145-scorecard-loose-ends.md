# 145 — 记分卡收尾：第一列去掉风，`Houjuu` 收进术语表

**What to build:** 票 133 落地后挂着的两条，一次收掉。

**Status:** done（报告：run/reports/145-scorecard-loose-ends.md）

## 一、第一列「席位 · 风」去掉风

闸门印出来的真实一行（`node web/scripts/verify-scorecard.mjs`，东风战打完）：

```
| 座位 0 南 | … | 1 位 · 30700 |
| 座位 3 东 | … | 4 位 · 21300 |
```

**取的是末局自风。** 东风战里座位 0 是起家、开局是东家，而整场打完这张表写着「座位 0 南」。
**风每一局都在转，而记分卡是整场的结论**——写一个瞬时的风是误导，
且它正好是最左那一列、是人读这张表的第一眼。而这张表**是要被贴出去的**：贴出去之后没人能纠正。

- [x] 那一列只写席位（`座位 0`…`座位 3`），**风整个去掉**
- [x] 表头跟着改（`席位 · 风` → `席位`）
- [x] **复制出去那段文本同改**（两处同源，别只改一处）
- [x] `verify-scorecard.mjs` 那几条逐格对拍跟着改；**改完先红一次**（把实现回退，它必须报红）
- [x] 「起家是座位 0」这件事今天在页面上还看不看得出来？**看得出就别补**，看不出**也别在这一票补**——记进报告交调度器

**别顺手改成「起家 / 二家 / 三家 / 四家」**：那是又一套要人学的说法，而座位号页面上到处都是（名牌、视角按钮、危险度面板）。

## 二、`Houjuu`（放铳）收进 `CONTEXT.md`

票 133 落地了 `SeatTally.HoraTargeted`（= 放铳，**自摸不算**），而 `CONTEXT.md` 里没有这个词条。
**AGENTS.md 硬约束 5：改术语表要单票授权。主人 2026-08-30 已授权（「先把 133 和 143 收了」）。**

- [x] 收一条 `Houjuu` 词条，**写清它的执行者**（`Scorecard.tally` 里那个判据）与边界：**自摸不算放铳**
- [x] 照 `docs/agents/domain.md` 的收词规矩来：**逐条去核那个执行者还在不在**，不是抄写
- [x] **授权只到这一个词条**：顺手看见别的该收的词，写进 `DECISIONS.md` 等下一次授权，别自己收

## 边界

- [x] **不动记分卡其余任何一列**（票 133 刚落地、闸门逐格钉着）
- [x] 不动引擎的 `Scorecard.tally` 的算法本身——这一票改的是**怎么显示**与**术语表**
- [x] 不动 `web/scripts/verify-home.mjs`（票 137 要动它）、不碰 `ReviewPanel.fs`（票 144 要动它）
- [x] 不动引擎其余部分、不动牌谱格式、不动 `Store.fs` 的键
