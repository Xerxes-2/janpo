# 51 — 副露改用位置编码来源，删掉牌桌上那行「来自X」

**What to build:** 按真实麻将（雀魂/天凤）的画法，**横放那张牌在副露里的位置本身就表明来源**：
最左 = 上家、中间 = 对家、最右 = 下家。改完把牌桌上那行「来自上家/对家/下家」的**可见文字删掉**
（主人裁定：位置为主、文字不留）。

**Blocked by:** None（票 44 已落地，牌桌有了朝向）

**Status:** ready-for-human

## 为什么现在做：票 38 的理由过期了

票 38 当初**否决**位置编码，原话是「四家竖排面板**没有方位可锚**，且挪位会打乱吃的升序」。
那时对。但票 44 把牌桌改成三×三、每家有了自己的朝向，**方位现在锚得住**——
调度器集成 44 时没回头重问这个决定，主人指出后才发现。
（教训已记 `DECISIONS.md`：**一条决策的前提被后续票拆掉时，要回头重问那条决策。**）

数据不用新增：`Naki.Target` 记着被鸣那家，票 40 已把「相对副露方」的换算做成单一判据。

## 画法

- **参照系是副露方自己的左中右**，不是屏幕的左右。票 44 让上家/对家的副露行整体翻转过，
  所以「最左」在屏幕上的方向随座位变。**这条必须有断言**，否则就是票 40 那个「吃来自对家」的翻版
- **吃**：只能来自上家 → 横放那张恒在最左，另两张在它右边**按升序**。
  这意味着**副露整体不再是数字顺序**，这是对的（主人点名的正是这一条）
- **碰**：三张，横放那张按来源落在左/中/右
- **大明杠**：四张。**去找一个可引用的依据**（天凤或雀魂的实际画法）再定「中间」落在哪一格，
  **别自创**；找不到确证就在报告里写明你依据什么定的
- **加杠**：加上去的那张按惯例**叠在**横放那张上（现在用 `＋` 表示）。二维平面里怎么表达自己定，
  但要能看出「这张是后加的」且**来源仍读得出**（来源是当初碰的那家）
- **暗杠**：不变（无横放、两端牌背、无来源）

## 文字与机器可读标记

- [x] 牌桌上**可见的**「来自X」文字删掉（报告 §4 五张实拍，闸门里「又写回牌桌上」按红过：§5.2 E）
- [x] **`data-naki-from` 这类机器可读标记保留**——票 38/44 的闸门靠它们，删了闸门就瞎了
- [x] **prompt 里的文字一个字都不许动**（`web/src/agent/**` 在 `jj diff` 里零改动，`verify-invariants.mjs` 照旧绿）：票 41/49 的语义不变量（「吃恒来自上家」等）断言的是
      **prompt 文本**，而 prompt 只有文字、没有位置。**这是两套东西，别混**
- [x] 无障碍：来源那句话转入 `sr-only`（闸门核「必须在」+「必须看不见」），代价写在报告 §7；删掉可见文字后，读屏用户拿什么读来源？给个 `sr-only` 或 `aria-label` 都行，
      **不做也可以，但要在报告里明说这个代价**

## 闸门

- [x] 新增**位置断言**（`naki-marks.mjs` + `verify-board.mjs`：两颗种子摆齐九种槽位结果，五个视角逐个对拍）：横放那张所在的槽位 ↔ 决策包里的**绝对座位**对得上，
      四种位置 × 各座位视角 × 上帝视角都覆盖（票 44 的 `verify-board.mjs` 里已有方位断言的做法可抄）
- [x] **吃**那条：横放恒在最左（相对副露方）——闸门与单元测试各一条，两处都红过
- [x] **反向自证**：八次全实跑（报告 §5.2），其中 A＝参照系漂到观测者、G＝样式把那一行左右翻过来（属性全对、只有坐标红）
- [x] 票 38/44 的既有记号断言一条都不许弄坏（逐条存活证据在报告 §8；来源那条换了画法但两头都更严）（横放本身、加杠、暗杠、摸切、赤牌、摸牌间距、牌背）

## 边界

- [x] 不碰 `web/src/agent/`（prompt 与语义闸门）、不碰引擎、不碰牌谱格式
- [x] **自己打开图看**（五张：浅色围观 ×2、深色、上帝视角、820px）：重出 `docs/images/table.png`，并在报告里写明四种来源位置各自长什么样；
      浅深配色、围观/上帝视角、窄视口各看一张
- [x] `src/Janpo.Web/TablePage.fs` 里那句「重试两次」的帮助文字**没碰**（票 43 在收）
