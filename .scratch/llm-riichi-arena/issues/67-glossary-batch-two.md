# 67 — 第二批收词：四条攒齐的术语进 `CONTEXT.md`

**What to build:** M2 前半程攒下四条「只报不改」的术语表提案，逐票的裁决都是「归下一批收词」。
批齐了，一次收掉。**本票获准修改 `CONTEXT.md`，范围锁死以下四处，其余词条一个字不许动**
（第六次术语表授权）。

**Blocked by:** None

**Status:** ready-for-human

## 四条（各自的措辞依据在括号里的出处）

- [x] **重试判据（Retry）**（47-B）：票 48 已收过一条，**核对它与票 47 落地后的实际行为仍一致**
      （47 之后「不值得重试的第一次失败就兜底」，词条里的表述要对得上 `retry.ts` 的判据）；
      不一致就改词条，一致就在报告里写明核过（核过：一致，词条未动；它实为票 53 所收，见报告）
- [x] **`MinkanRinshanSekinin`**（59-3）：`Ruleset` 开关，大明杠→岭上开花的责任支付；
      默认关＝天凤口径；写清「现实里两种口径都存在」是它成为开关的理由（59 号报告）
- [x] **`Doujun`（同巡振听）措辞 + `RiichiAnkanMentsuUnchanged`**（63-A）：
      同巡振听的解除条件按 63 落地后的实际行为写（**自家鸣牌解除**，窗口到自家下一次摸打）；
      新开关写清天凤侧（禁送り杠+听不变）与竞技侧（另加面子构成不变）两种口径（63 号报告的查证）
- [x] **`Honba` 的点数措辞**（65-4）：按 65 号报告的提案改——包牌荣和时本场棒整根归包牌家，
      词条里「每根 300 点由放铳家支付」的说法要容纳包牌例外

## 纪律

- [x] 每条写完自问判据 2：**这条词条声称的行为，谁在执行**——四条都要能指出对应的代码或闸门
      （47 的 `retry.ts` 与用例、59/63 的开关与固件场、65 的 `honbaPayers` 与 pao 闸门）
- [x] 体例照既有词条（_Avoid_ 列表、不写票号——局限与理由写死在词条里）
- [x] 顺带一行活：`KanProperties` 那条注释（DECISIONS 66-5 悬着的）改对

## 边界

- [x] 只碰 `CONTEXT.md` 与那一行注释；不碰引擎、web、固件
- [x] `./scripts/ci.sh` 全绿（实跑 EXIT=0）
