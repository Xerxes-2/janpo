# 139 — 两档牌宽：屏幕前只有一个读者

**What to build:** 所看那一席的牌走大一档，其余三家小一档。评审 P1-F 的**前半**。

**Status:** done（报告：run/reports/139-two-tile-sizes.md）

## 评审那一段的数已经过期，别照抄

稿子写「118 之后 1280×800 上牌宽 26.4 px……固定舞台把纵向吃满」。
**固定舞台在「牌桌救援 票 1」里已经拆了**（删根字号缩放、改真三栏），
今天是 `--tile-w: calc(var(--board) * 0.0468)`，而 `--board` 由视口现算（`styles.css` :root）。
∴ 票面里一个具体像素数都别写死；**要落地的人自己先量一遍今天的两档实值**，再写进报告。

**稿子的后半（桌心五项改横排、本場供託画成棒）不必做了**：
「牌桌救援 票 5」已经把盘心改成只留会变的数，立直棒也早就是实物画法（`TableBoard` 的
「一根都没时整个字段不画」）。这一票**只做两档牌宽**。

## 要什么行为

- [x] 所看那一席（上帝视角下＝起家）的牌大一档，其余三家小一档；同一个 `--board` 体系，
      **不动旋转、不动四席绕盘心的几何**
- [x] 两档就是两个 CSS 变量，**由 `--board` 派生**（不许写死 rem：牌桌救援 票 1 的教训——
      写死 rem 之后小桌上四席的牌会互相压住，`verify-human` 逮到过）
- [x] 视角一换，大的那一档跟着换席（与 `TableState.viewpoint` 同源）
- [x] 闸门：「牌与牌桌同比缩放」那条**按两档分别量**（判据：量点要落在被关心的那件事上）
- [x] 闸门还要有一条：**两档之下没有任何一席的牌与邻席重叠**（票 132 修过一次的那个形状）

## 边界

- [x] 不动 `--board` 的算法本身、不动名牌与两条轨道的宽（票 131 的地盘）
- [x] 不改桌心（牌桌救援 票 5 的地盘）
- [x] 不改副露图例的位置（票 118）
- [x] 不动 F# 侧的 `Board` 投影——这一票**尽量只动 `styles.css`**；真要加 hook 也只加一个「哪一席是主语」的钩子
