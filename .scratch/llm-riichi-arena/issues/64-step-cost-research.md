# 64 — 研究：重放路径上 `GameState.step` 那 37µs 花在哪（引擎性能第 6 轮）

**What to build:** 一份研究文档 + 可证伪的实验清单。**`src/` 一行不许改**（研究票）。
登记册在 `docs/research/engine-performance-track.md`，这是第 6 轮。

**动机（这次是正当的，登记册规矩第 4 条点名的那类）**：主人把整年语料扫描变成**常态工具**
——修一轮 bug 就全量重扫验证（票 63 正在这么用）。全部 18 包 ≈ 2600 万局 ≈ **27 核时**；
`GameState.step` 占重放 93%（票 62 实测，37µs × 61 次/局），但**它内部是黑盒**。

**Blocked by:** None（63 在改引擎，但你只量不改，在 `/tmp` 树副本里做）

**Status:** ready-for-human

## 要回答的（每条给数字，或明确写「量不出来，因为…」）

- [x] **37µs 拆到 step 内部**：合法动作枚举、动作校验、`PlayerState`/河/副露的不可变更新、
      振听簿记（**打牌后要算 waits——那是一次向听类计算，量它**）、和了/流局判定、事件产出，
      各占多少？按事件类型分开（摸牌 / 打牌 / 鸣牌 / 杠 / 和了）——**打牌大概率是大头但别信我，量**
- [x] **「一趟做几次」先于「一趟多贵」**：每种内部操作每局发生多少次？
      （61 次 step/局是票 62 的数，先复核）
- [x] **重放路径与对局路径的成本是不是同一份**：重放时 `legalActions` 有没有被算？
      校验有没有走全量判定？若重放多付了对局不需要的钱，**最便宜的改法可能是「重放别付那份钱」**
      而不是「把那份钱变便宜」
- [x] **票 55 的暂存缓冲手法适不适用**：step 内部若有 34 长数组/临时集合的churn，
      55 的 `ShantenScratch` 先例可以照搬；量出 churn 的量级
- [x] **上限先算**：假设 step 优化 k 倍，18 包全扫的墙钟各是多少（4 核礼貌模式 / 32 核全开）？
      把「不改代码只加核」那条基线摆在第一行——**若 32 核全开已经够用，如实写，这一轮就到此为止**
      （第 5 轮的先例：研究票的价值可以是阻止一次工作）
- [x] 顺带：`pendingKanDora ≤ 1` 那条「不变量断言太弱」的 nitpick（59 号转来的）看一眼，
      值不值得加强，一句话结论即可

## 交付

- [x] `docs/research/step-cost-on-replay-path.md`：拆解表、按事件类型的分布、
      推荐做法（或「不做」）与被否决项及理由、**可证伪实验清单**
- [x] 登记册加第 6 轮一行、更新「未答」
- [x] 建议的实现票草案写进报告末节（**别自己建票文件**，票号调度器分配）
- [x] `./scripts/ci.sh` 全绿（没改代码，应自然绿），已 `jj commit`

## 边界

- [x] **不碰** 票 63 的地盘（`RiichiState`、振听、`KanTests`、固件）；你在 `/tmp` 树副本里量
- [x] 语料只读 `/home/xerxes2/janpo-corpus/`
- [x] 转引旧数字逐条标出处（37µs、93%、61 次/局都是票 62 的，标清）
- [x] .NET 侧为主（重放是 dotnet 离线作业，浏览器不跑它）——**这一点与前两轮不同，别照抄它们的双侧要求**
