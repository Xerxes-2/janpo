# 140 — 双回放并排：只差一个变量

**What to build:** 两份牌谱同屏、按手数对齐、只差脚手架档或模型档案。评审 P2-G。**第一版只做「导入两份 JSON 并排看」**，自动跑批以后再说。

**Blocked by:** 133（并排的结论要落在同一张记分卡上）

**Status:** needs-triage（主人 2026-08-30 裁：**可以等落地再动**——票 133 落地之后再切，不进 M6 波次）

## 为什么先挂着不派

它是竞技场真正的结论承载物，但它**动的是页面的顶层形状**（今天一页一桌、`TablePage` 是这一页
对外唯一的名字）。两桌同屏要么开第二个 `useElmish`、要么把 model 提一层——
**这是个架构决定**。主人 2026-08-30 已裁：**等票 133 落地之后再切**——
记分卡的形状定死之前，并排看两份牌谱没有共同的结论承载物。挂在这里是为了让「README 说成套对照实验还没人跑」
有个明确的去处，不是为了这一波做。

## 已经查清、不必再查的

- 牌谱字节能原样回放（`Paifu` 的 JSON 导入导出与 URL 分享共用同一对编解码器）。
- 全在客户端，零后端（ADR-0003）。
- 逐手对齐的锚是 `DecisionRecord.Turn`：**随机座位的手不产生记录，但照样占号**，
  ∴ 两份牌谱的 Turn 直接对得上，不必另造对齐键。

## 第一版的边界（若被裁下来做）

- [ ] 只做「导入两份 JSON 并排看」；不做自动跑批、不做批量调度
- [ ] 两侧共用同一份记分卡渲染（票 133），不许各画各的
- [ ] 不动牌谱格式
