# 48 — 术语表收下 M1 攒的四处新词

**What to build:** M1 里有四个概念已经成了代码与产品面的一等公民，但 `CONTEXT.md` 里没有名字。
主人 2026-08-17 裁定**按调度器的建议一次收掉**。**本票获准修改 `CONTEXT.md`**
（RUNBOOK 第 6 条的第三次例外），范围锁死在下面六处，其余一个字不许动。

**Blocked by:** None — can start immediately.

**Status:** ready-for-human

## 裁定的内容（措辞可润色，语义不许改）

- [x] **`PromptTemplate`（提示模板）** —— 新词条。三段 prompt 的可替换槽位；
      **与人格、脚手架档位是三个独立维度**，不是一个枚举
- [x] **`Persona`（人格）** —— 新词条。坐席级的自由文本，进固定 preamble；
      **一局内不变**（否则废掉可缓存前缀）
- [x] **`RenderVersion`（渲染版本）** —— 新词条。牌谱字段，用于归因命中率变化。
      **必须写明当前局限**：只覆盖模板内容，认不出渲染器代码改动（票 43 在处理）
- [x] **打码（Redaction）** —— 新词条。可分享物离开 Agent 层前，抹掉坐席配置里的字面量
      （API key 与端点地址）。建议措辞见 `reports/36-*.md` §9.5
- [x] **`Replayed`** —— **不单开词条**，往已有的 **Replay** 条目里加一句：它是那个 fold 的结果值
- [x] **「无依据」** —— 收进 **Danger** 条目。**必须标明它 ≠ 日麻的「無筋」**（后者只说数牌）

## 纪律

- [x] 每个新词条照现有词条的体例写（含 _Avoid_ 列表，别自创格式）
- [x] **不许顺手改别的词条**，也不许"顺便"补 M1 之外的词
- [x] 收词之后核一遍：这四个词在代码、prompt、页面上的用法与词条**一致**；
      不一致的**报上来别改代码**（术语表是权威，但改代码是另一张票的事）
- [x] `./scripts/ci.sh` 全绿（这一票理论上不动代码，若真需要动，停下来记 `DECISIONS.md`）
