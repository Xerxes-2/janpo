# 79 — 首页 Demo 换成真的四席对局（真 key 跑一场，挑一份好看的）

**What to build:** 票 71 的首页 Demo 是 bot 牌谱占位——牌在走，但气泡是空的，
而「LLM 边打边讲理由」正是这个平台的招牌。主人已把真 key 放在 `/tmp/deepseek_key`
（模型 `deepseek-v4-flash`，与 M1 同一把同一档），**这一票用它跑真的四席对局，挑一场好看的换上去**。

它同时是 M2 这一摞票的**端到端验收**：四席（73）、并发问话（74）、思考气泡（76）
三样第一次在真 provider 上一起走一遍。

**Blocked by:** 74（四席 + 并发问话）、76（思考气泡，已并）、**80（纸面牌谱风美化）**
——定妝照（首页截图、README 那张图）要拍在美化之后，不然换完资产又得重拍一遍

**Status:** ready-for-human

**park 了，等一条裁决**（报告 `run/reports/79-real-demo-paifu.md` §5、`DECISIONS.md` 79-3）：
资产换好了、气泡真出来了、闸门翻好面了，但 `./scripts/ci.sh` **EXIT=1** ——
票 75 的 `ReplayTimelineTests.拖到中间那几个游标…` 在这份真语料上红。
诊断到底了：**红的是那条断言的第三锚点，不是产品**（`Replay.game` 拿截断的事件流重建时，
若截断点落在某家该摸牌的相位上，它会从推断出来的牌山替那家摸一张假牌）。
修它要动票 75 的用例 = 单票授权，所以我停在这里。

## 换资产时要翻面的断言（票 78 集成时留的提醒）

- 首页 `table-no-bubbles` 那句指路话**会消失**（真资产带决策记录），气泡出现：
  `verify-home` 第Ⅶ条与 `verify-inbound` 里核那句话的断言都要跟着翻面（改成「气泡必须在」）
- `HomePageTests` 那条 **≤512 KB** 体积断言大概会撞（票 75 实测：带 thinking 约 2.5 MB/250 手）：
  放宽要给数（首屏耗时、堆增量），不许悄悄改数字

## key 的规矩（硬约束 4，不许有例外）

- key 从 `/tmp/deepseek_key` 读，**绝不进代码、测试、fixture、报告、提交**
- 现成的口子照用（别新发明）：`JANPO_KEY_FILE` + `--llm` 那一档
  （`web/scripts/verify-export.mjs` / `verify-llm-seat.mjs` 里就是这个形态）
- **CI 一个真实请求都不许发**：这一票产出的是**资产与报告**，不是新的 CI 关卡
- 导出的牌谱与 Demo 资产里不许出现 key（票 34 的闸门守着，跑一遍确认它绿）

## 要什么行为

- [x] 四席全是模型，**四个不同的人格**（防守 / 进攻 / 爱鸣牌 / 中庸），
      同一份 `ModelProfile`（`deepseek-v4-flash`）——人格写死在 `web/scripts/demo-game.mjs` 的 `PERSONAS`
- [x] 跑**东风战**：6 场里挑了种子 15 那一场（6 局、2 立直、34 副露、4 和了、以自摸和了终、顺位真的变过）
- [x] 换掉票 71 那份占位资产（只换 `web/public/demo-paifu.json` 一个文件，代码一行没动）
- [x] 首屏体积把住：**没用思考档**（它 6.9 KB/手、单手 11.8 s）。挑中那份 1,122,337 字、
      过线 gzip 148 KB、首屏 154 → 218 ms、堆 2.5 → 7.9 MB；上限 512 KB → 1.25 MiB（报告 §4）

## 报告里必须有的数（M2 的第一份真实语料账）

- [x] 墙钟：一场 11–18 分钟（一局约 2–3 分钟）；**并发问话在整场里省下约 0%**（报告 §3.3，DECISIONS 79-5）
- [x] 账单：挑中那场付全价输入 928,134 + 缓存命中 435,968（**32%**）+ 输出 47,952 tok（报告 §3.1）
- [x] 兜底：**2,213 次问话 0 手兜底**、重试 1 次、**429 / 断连 0 次**（期间最多 12 路并发）
- [x] 气泡的**等待感**：直觉档 1.7 s（p95 2.4 s）、思考档 11.8 s（p95 25.4 s）。
      **结论：提议判不做流式**（DECISIONS 79-4）
- [x] 模型打得怎么样：**裸奔档 8 局里立直 0、和了 0、听牌 1**（两个动作一次都没被摆到它面前）；
      换 assisted 档同模型同人格就立直、和了、听牌都有了（报告 §3.5，DECISIONS 79-1）

## 边界

- [x] **不改任何代码逻辑**去迁就这场对局。真跑里发现的那一条**只报不修**（`DECISIONS.md` 79-3）——
      它确实让这一票没能全绿，因此 park，而不是自己去动票 75 的那条用例
- [x] 预算：**6 场东风战**（另加 8 手冒烟与 30 手思考探针，报告 §7 认账）；先跑一场看账再开下一场
- [x] 不碰 CI 关卡、不碰 `web/tests/fixtures/agent/**`
- [x] 截图重出首页并**自己打开图看**：气泡里真有话、一张牌都没挡住；
      不满意的一处：座位 1 那个气泡最后一行被面板边缘切掉（建议另立一票，报告 §6.2）
