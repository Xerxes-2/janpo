# 25 — Danger 危险度并入 Assisted

**What to build:** 引擎多一个**分析附件**模块：给定局面与某座位，按现物、筋、壁与宝牌周边给出这一手
每张可打之牌的安全度排序与理由。它不参与任何规则判定，只被脚手架与（M3 的）复盘对照消费。接上
24 号铺好的通路后，Assisted 档至此完整 —— spec 对这一档的定义（向听 + 有效牌 + 进退向 + 危险度排序）
全部兑现。

**Blocked by:** 24

**Status:** ready-for-human

- [x] 规则化启发式，**不做统计模型**（第一版的明确边界）：现物、筋、壁、宝牌周边
- [x] 输出是排序 + 每张牌的**理由标签**（如「对家现物」「4s 筋」），理由要能直接进 prompt
- [x] 措辞与 `CONTEXT.md` 的 Genbutsu / Suji / Kabe / Danger 条目一致，不自创词
- [x] 是纯附件：引擎的规则判定路径**一个字节都不依赖它**（拿掉它，对局照跑）
- [x] 测试钉住判据：现物必最安全；四张全见形成壁；筋的成立条件；对未立直但副露的对家也给排序
- [x] 进 Assisted 档的决策包与 prompt；Bare 档不带
- [x] 牌桌上可选地把排序显示出来（围观者也想看），默认关

**注意**：安全度是**启发式**，不是概率。文案不要写成「放铳率 12%」这类会被误读为统计结论的说法。

---

**做完了**（change `txxznwps`）：报告 `run/reports/25-danger-module.md`，决策 `run/DECISIONS.md` 的「## 25」段（八条）。
引擎新模块 `src/Janpo.Engine/Danger.fs`；`Fallback` 的 Assisted 档从此是**不退向听的安全打**（还上 24-5 欠的那一半）。
留给人的待审项在报告第 11 节（威胁判据太粗、筋与壁的先后、`DangerTier.NoEvidence` 这个词要不要收进术语表）。
