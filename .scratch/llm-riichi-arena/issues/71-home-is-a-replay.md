# 71 — 首页第一眼是一桌牌在走：Live / Replay 接缝、`?table=1`、Demo Paifu 占位

**What to build:** 访客打开 `https://xerxes-2.github.io/janpo/` 什么都不用配、不用 key，
第一眼就是一桌牌在走（spec 的 story 1，ADR-0003 说它由 Demo Paifu 兑现）。
主持人要自己开一桌就去 `?table=1`。**页面从此有两种「牌桌从哪来」**，而播放控制、视角切换、
牌桌与结算的渲染**只有一份实现**。

**Blocked by:** 70（拆文件）

**Status:** ready-for-agent

## 主人已经裁掉的分叉（照做，别重新设计）

- **一页一 Model，模式是联合类型**：`Source = Live of … | Replay of …`，
  `Playback` / `Viewpoint` / `ShowDanger` 与牌桌视图在联合之外，两种来源共用。
- **地址**：`/` = Demo 回放（自动播）；`?table=1` = 配桌与 Live（**默认暂停**，与今天一致）；
  `#<载荷>` 留给分享（**这一票只保证它不炸**，解码是票 77/78）；`?dev=1` 与它们正交（票 35 不动）。
  hash **只装分享载荷**，不当路由用（35-1 当年就是为这件事把 dev 开关放在 query 上）。
- **Demo 资产先用 bot 牌谱占位**；换成真的四席对局是**票 79**（主人已把真 key 放在 `/tmp/deepseek_key`，
  但那要等四席与气泡落地才跑得出来）。这一票只负责「换文件就换了 Demo」的口子。

## 要什么行为

- [ ] `/`：载入随应用分发的 Demo Paifu，**自动播**；播到终局停在结算面板，页面上有「从头再放」
- [ ] `/` 上有一条「自己开一桌」的路（链到 `?table=1`）——访客要摸得到 Host 那一侧
- [ ] `/` 上**没有**配桌与模型面板（访客第一眼不该是一张表单；票 35 的「默认视图只该有牌桌」同一条标准）
- [ ] `?table=1`：今天的页面一字不少（配桌、模型面板、种子、单步/播放/倍速、导出、下一局、视角、危险度）
- [ ] 回放模式里**结算面板有役与符番**：`Replay` fold 的时候把 `HoraReading` 捞下来
      （`GameState.horaOf` 只在宣言那一刻答得出来，与 `Table.Readings` 同一个理由）
- [ ] 回放模式里视角切换照旧（座位视角要的掩蔽流也得在 fold 时建起来）

## Demo 资产

- [ ] **可复现**：由一条写在报告里的命令 + 一颗种子产出（CLI 加子命令、或复用现有输出拼装，你定），
      不是「某次手点导出下下来的一份神秘 JSON」
- [ ] 挑一场看得懂的：有立直、有副露、以和了终（有主见 bot；均匀随机基本碰不到立直）
- [ ] **东风战**，不是半庄——首页不该让人等半小时；也让资产小一截
- [ ] **用 `fetch` 拉，不打进 JS bundle**：主人事后换成真 LLM 对局的那一份会带 thinking
      （实测约 10 KB/手），打进 bundle 会把首屏拖死
- [ ] 报告里写清**换资产的手续**：换哪个文件、有没有断言会跟着红、体积上限建议
      —— 票 79 会照着它换成真的四席对局，写给它看

## 闸门（web/scripts）

- [ ] 现有四道改开 `?table=1`：`verify-board` / `verify-export`（两趟）/ `verify-llm-seat` / `table-drive`
      —— 它们今天开 `/` 就按 `table-*` testId 找那张**静止**的牌桌
- [ ] 新一道开 `/`：断言 ① 牌桌在动（隔一会儿采两次，手数不同）② 页面上没有配桌控件
      ③ 有一条去 `?table=1` 的路 ④ 页脚照旧（票 37）
- [ ] `verify-tracer` 那两道照旧绿（`/` 无开发向内容、`?dev=1` 才有曳光弹）
- [ ] 每道新断言**先红一次**，红的原始输出抄进报告（票 44 立的规矩）

## 边界

- [ ] **时间轴不在这一票**（拖动与逐事件步进是票 75）：这一票的回放只会顺着播
- [ ] **不解码 hash**（票 77/78）：带 hash 打开时退回首页 Demo，不许白屏
- [ ] 不碰 `web/src/agent/**`、不碰引擎的规则判定；`Replay.fs` 只准加「捞役种/建掩蔽流」这类**输出**，
      一条规则都不自己判
- [ ] 曳光弹留在 `?dev=1` 后面不动（22-A 那条泄露挂账归 M3 真人坐席）
- [ ] 截图会变（首页第一眼换了）：重出并**自己打开图看**，报告里写你看到了什么

## 为什么这一票要连着地址一起做

首页要自动播，而 `Playback.initial` 的注释明写「一进页面就自己跑起来会让无头验收读到一个动着的牌桌」——
四道闸门今天全靠 `/` 是静止的。**地址与闸门一次改到位**，否则票 75/76/78 每张都要再动一遍这四个脚本。
