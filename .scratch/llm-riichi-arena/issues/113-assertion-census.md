# 113 — 断言执行次数普查：把「看着在守、其实没执行」的那些一次找齐

**What to build:** 判据 3（「执行 0 次的断言与不存在等价」）在这个项目里已经攒了**六个案例**，
每一个都是**偶然**撞见的：

| # | 案例 | 怎么被发现的 |
|---|---|---|
| 1 | 票 95 的记法闸门 | 收尾时才想起来问「在真语料上跑了几次」 |
| 2 | 票 96 的立直属性十趟九空转 | 红了之后才去查 |
| 3 | 票 98 的抢杠零次 | 票 97 主动去数取值域 |
| 4 | 票 104 的「没有总分」没有执行者 | 收词时逐条弄坏执行者 |
| 5 | 票 92 那道剪枝一条闸门都没有 | 票 108 修同族问题时撞见 |
| 6 | 票 112 的播放速度**连断言都没有** | 票 111 顺手交回来 |

**六次全是偶然。这一票把它做成一次普查。**

**Blocked by:** 无

**Status:** ready-for-human

**做完了**：报告 `…/run/reports/113-assertion-census.md`；两件工具
`scripts/fsi/assertion-census.fsx`（coverlet 逐行命中数，Debug 三趟）与
`web/scripts/assertion-census.mjs`（`NODE_V8_COVERAGE` 块级计数）。
量到 **10,395 个断言点，10,353 个有数**；零次 354 条（真判断点 73 / 放行支 4 / 失败支 277）。
**一条既有断言的语义都没动**，发现的问题列在报告 §9 等调度器编号。

## 要什么产出

**一份可信的清单 + 一个能重跑的工具。不要求修任何一条。**

- [x] **工具**：两侧各一份（两边的计数器不同源，合不成一份）：
      `scripts/fsi/assertion-census.fsx` 与 `web/scripts/assertion-census.mjs`，
      都支持 `--from`（拿现成覆盖数据重算）与 `--json`（机器可读全表）
- [x] **清单**：报告 §3，按执行次数升序、零次的排最前，**每一行一句「为什么执行不到」**；
      零次分三档（真判断点 / 失败支 / 放行支）——不分档那张表 354 行里有 277 行是「绿的必然结果」
- [x] **恒真式也要抓**：三条便宜判据（运行时空转 / 入参没用到 / 两侧同一表达式），
      报告 §5：抓到 1 条真的 + 4 条判断题；**入参没用到那条在本仓库恒为 0**
      （`--warnon:1182` 让它编不过）；写法不同却恒等的那一族只有变异测试抓得住，写明了为什么没做
- [x] **排个序**：报告 §8 分四档（补执行者 3 条 / **该删的一条都没有** / 留着当防御分支但要单元执行者 6 条 /
      判据 4 那一档）

## 怎么量（自己判，但别自欺）

- 引擎侧属性：FsCheck 那族有现成的取值域探针（`scripts/fsi/arbitrary-coverage.fsx`，票 97 留的），
  **先读它、别重造**
- 页面侧不变量：票 95 / 104 / 107 那几道有「跑了几次」的先例（`verify-invariants.mjs` 会印次数）
- 浏览器闸门：`verify-browser.mjs` 的 `gates` 表是趟数的唯一真源（票 106），
  **但「一趟里那条断言被求值几次」是另一回事**
- **别把「代码行被执行」当成「断言被求值」**：覆盖率工具答的是前者，这一票要的是后者

## 闸门

- [x] **工具自身要有阴性对照**：两侧各两条（报告 §4 留原文，做完当场撤掉）。
      永远执行不到的那两条都落在零次那一行。**页面侧头一版对照写错了，写进了报告**：
      循环里的 `if (v > 100) push(…)` 普查报 3 次而不是 0——数的是**条件被求值几次**，不是它红了几次
- [x] 恒真式那两条也被点名（运行时空转那一条 + 两侧同一表达式那一条）。
      **`let … (x: T) = true` 在本仓库编都编不过**（FS1182），因此对照换成等价的两种形状
- [x] 零次那几行**每一条都有一句「为什么」**（报告 §3.1–§3.5），没有留空的
- [x] `./scripts/ci.sh` 全绿；**一条断言的语义都没动**（`src/**`、`tests/**`、`verify-*.mjs`、
      `scripts/ci*.sh` 一字未改）

## 边界

- **只读与新增工具**：不许改任何既有断言/属性/闸门在守什么
  （发现的问题**列出来交回调度器编号**，别顺手修——这一票的价值在清单的可信度）
- 不碰 `src/**` 的产品代码；不碰 `Paifu` / 账那一段 / `Review*.fs`
- 可以新增 `scripts/fsi/*.fsx` 与 `web/scripts/*.mjs`；**若要接进 `ci.sh`，先想清楚它值不值那点时间**
  （票 112 那一趟 16.5 s 已经是第二贵的，普查大概更贵——**默认不接进 CI**，做成手动跑的工具）
- 加闸门趟数不用手改总述（票 106）

## 顺手记着

这一票最容易做出来的废物是**一张看起来很全、其实统计口径可疑的表**。
宁可**只覆盖三分之一但每个数都站得住**，也不要一张「大概是这样」的全表——
**判据 4：抓不住的要写出来。** 覆盖不到的那三分之二，列个名单说清「为什么这一批没量」，
比给它们编个数有用得多。
