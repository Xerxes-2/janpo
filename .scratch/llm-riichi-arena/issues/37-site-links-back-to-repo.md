# 37 — 站点要能链回 GitHub 仓库

**What to build:** 现在只有单向链接：README 指向站点（`https://xerxes-2.github.io/janpo/`），
站点却没有一处指回仓库（`https://github.com/Xerxes-2/janpo`）。访客从搜索结果或别人转发的链接直接落到
站点上，就没有任何路径找到源码、许可与项目状态。补上。

**Blocked by:** 35（它正在改同一处页面壳的标题与说明，先落地免得白打冲突）

**Status:** ready-for-human

- [x] 页面上有一处指回仓库的链接，访客一眼找得到，但**不抢牌桌的注意力**
      （页脚或标题旁的一小行都行，自己判断）
      —— 页面最底一行页脚（`Main.Shell` 里的 `Footer.Bar()`）：细线隔开、小一号字、`opacity .7`
- [x] 措辞用户向，与票 33 的 README、票 35 改写后的页面同一个语感；不要出现 Fable / dotnet 这类词
      —— 「源码、现在做到哪一步、以及页面里提到的那几份文档，都在 GitHub 上的 Xerxes-2/janpo。
      按 MIT 许可放出。」无工具链词；CI 那道闸门同时盯着默认视图不得出现 Fable / dotnet
- [x] 顺带把**许可（MIT）**在页面上提一句 —— 现在只有仓库里有，站点上没有
      —— 「按 MIT 许可放出」，链到仓库里的 LICENSE 正文
- [x] 链接地址**不要写死在多处**。若页面别处已有外链（比如本地模型那份手册），照它现有的方式来
      —— `src/Janpo.Web/Footer.fs` 顶上一行 `repoUrl`，许可那条由它派生；本地模型手册现有的做法
      （正文里点名 `docs/host/custom-endpoint.md`）一字未改，页脚那句把它们接回仓库
- [x] **自己用眼睛看**：本地构建产物截图并真的打开图看，确认链接在默认视图里存在、位置不碍事
      —— 报告 §3：整页 1417px、页脚在 `top=1353px`，与座位 3 的卡片隔 32px，牌桌没被挤变形

## 不做

- [x] 不做徽章墙、不做 star 按钮、不引第三方脚本（`index.html` 现在零 CDN 零 analytics，
      这是票 34 核过的事实，别破坏它）—— `web/index.html` 一字未动，新增的只有两个 `<a>`
- [x] 不碰 `README.md`、`web/src/agent/`、引擎、`.github/workflows/`
- [x] 不做布局改造（M2 的活）—— `styles.css` 只追加 `.site-footer` 两块，既有规则一条未改

## 为什么值得单开一票而不是塞进 27

27 号是验收票，它的产出是「核对与报告」。往里塞实现会让验收的注意力被实现挤掉 ——
这与票 32 单开的理由相同（验收标准不同：这一票是「人看得见链接」，27 是「跑 CI 看数字」）。
