# 74 — 响应阶段同时问多席：等待状态按座位，四家一起想

**What to build:** 一家打出一张牌之后，能碰/吃/杠/荣和的可能有三家。今天页面**一次只问一家**
（`TableModel.Awaiting` 是一份，`Table.pending` 取 `legalActions` 的第一项），
四席都是模型时这就变成三趟串行跨网请求；而实测**一局要被问 88.6 次**（有主见选手 76.9，
`DECISIONS.md` 票 58 一节），思考预算开着时单手 17–180 秒。串行的话一局能跑掉一小时以上。
改成**同一个响应阶段里的待答席一起问**，等待状态与票号按座位各记一份。

**Blocked by:** 73（四席已经能各自绑定模型）

**Status:** ready-for-agent

## 前几波留给你的既成事实（70–77 除 74/78/79 外全已并入 `main`）

- **四席的形状**（票 73）：`SeatingPlan = { Profiles; Seats }`；按座位取配置用
  `TablePage.seatConfigOf (seat) (model) : LlmSeat option`（bot 席 = None）；`SeatingPlan.llmSeats` 列出模型席。
  `LiveTable.Pinned` 已是 **`Rendering option list`（每席一项）**——「一局内不变」按座位各自成立。
- **气泡的取值器**（票 76）：`TableState.bubbles : TableModel -> Table -> (Seat -> Bubble option)`，
  `Bubble = Thinking | Spoke | Troubled`。**你只改它里面 `asking` 那一段**（今天读的是单席的
  `AgentStatus.Asking`）；视图与 17 条用例全读这一个函数。`AgentLine` 的 `data-seats` 是四个名字逗号串。
- **`verify-bubbles` 要扩成四席各配一个假端点**（票 76 交代的：今天浏览器那侧只验得到
  「一席有、三席没有」）。灌坐法用 `web/scripts/seating.mjs` 的 `plantSeating`。
- **别改 `TablePage.initial` 的签名**（`rules` + `seating` 两参）——W3/W4 各撞过一次，教训在
  `DECISIONS.md`。票 78 此刻在另一个工作区里。
- **73-1 的触发条件**：若你要往 `SeatingPlan` 上加东西，先看 `DECISIONS.md` 73-1（统成
  `RosterDraft` + 独立档案库）；不加就不动。
- flash 档的真实基线（票 73 真跑）：单手延迟中位数 **1873 ms**、缓存命中 45%、105 次问话 0 兜底。

## 一条已经查实的既成事实（不必重新论证，但要有断言守着）

响应阶段里**某席的合法动作集不因另一席先答而改变**：`GameState` 的响应阶段收到一家的答复时，
只把那一席从 `Responses` 里滤掉（`src/Janpo.Engine/GameState.fs` 的
`let remaining = waiting.Responses |> List.filter (fun each -> each.Seat <> actor)`），
其余席的 `Actions` 一字不动，收齐之后才按优先级裁决。

因此**「同时建好几份决策包、答复到了再逐个落」是安全的**。
- [ ] 给这条加一道断言（属性或用例都行）：响应阶段里逐个落别人的答复，某席的动作 id 集恒不变。
      **它是这一票的地基**，塌了整票就错了；写不出反例就想想这条断言是不是空的（票 29b 的教训）

## 要什么行为

- [ ] 响应阶段一次把**所有**待答席问出去（bot 席当场答，模型席各发一趟请求）
- [ ] 等待与票号**按座位**：回执带的座位与票号对不上就丢（今天靠一个 `Ticket` 判，四席要各判各的）
- [ ] 定时器仍然「还有在飞的就不续」，但**不再因为一席在飞就不问第二席**
- [ ] Agent 层状态按座位各一份（Idle / Asking / Spoke / Troubled），状态线把多席都列出来
      （**好看是票 76 的事**，这一票只要「看得出三家在想」）
- [ ] **「在想」那一态要显示已等秒数与上限**（票 72 把超时默认值改成了 **240 秒**，
      重试两次 → 最坏一手 12 分钟才看得出模型不说话了；并发之后一席卡住会拖住整手）。
      人要看得出「还要等多久」，而不是干等一个不动的气泡——这条是 72-3 裁决里明写的代价
- [ ] 打牌阶段照旧只有一家（那里没有并发可言）
- [ ] 一席超时/报错不拖累别席：它走兜底，其余席的答复照收

## 闸门

- [ ] 四席全交给本机假端点、**给假端点设一个固定延迟**：断言一个响应阶段的墙钟时间接近
      **一份**延迟而不是三份（这是「真的并发了」的唯一硬证据），并把两个数抄进报告
- [ ] 同一局打完，终局点数与顺位仍与串行时**逐项相同**（并发只改问的时机，不改结果）
- [ ] 回执错位：故意让一份回执迟到（在飞时重开一桌 / 进下一局），断言它被丢掉而不是落进新的一手
- [ ] 每条断言先红一次，红的输出进报告

## 一趟真跑冠气（不进 CI）

假端点量得出「并发真的发生了」，量不出**真 provider 对四路并发什么反应**。

- [ ] 拿 `/tmp/deepseek_key`（`deepseek-v4-flash`）四席跑**一局**：有没有 429 / 连接被拒 /
      墙钟并没真的省下来？三个问题各给一行数据写进报告
- [ ] 若真的限流：**只报不自己发明限流器**（那是另一张票，且要先量）；
      票面要的并发不因此回退
- [ ] key 的规矩与票 73 同条：现成口子、绝不进提交、CI 零真实请求；预算 **≤ 1 局**

## 边界

- [ ] **不改引擎的裁决顺序**：头跳/双响仍由引擎按优先级裁（先被问到不等于优先）
- [ ] 不改 prompt、不改重试判据、不改兜底策略
- [ ] 不做「一家宣言荣和就取消其余在飞的请求」——引擎要收齐才裁决，取消会让那几席无答复可落；
      真想省钱另立票，先量它省多少
- [ ] 不碰 `Paifu` 格式：决策记录的 `Turn` 仍是「这一场里落定的第几手」，并发不改这条

## 顺手记着

`Rendering`（人格与模板一局内不变）在票 73 已经按座位各一份；这一票只要保证并发不把它弄乱：
**一局的头一次问话把那一席定住**，四席各自定各自的。
