# 66 — 引擎性能第 6 轮实现票：振听簿记与立直判定加向听前置闸（E2+E3）

**What to build:** 票 64 拆开了 `GameState.step` 的 37µs：**振听簿记 `refreshFuriten`→`waits`
占 55%，而 83.6% 的手不听牌——那些 waits 计算全是白跑**；`canRiichi` 逐张试打再占 17%。
研究文档 §11 给了两处修法并**已在 /tmp 预验证**（引擎重放 2.06×、129,179 局重扫逐字节相同、
721 测试绿、20 万手采样零反例）。这一票把预验证变成正式代码 + 完整验收。

**Blocked by:** 65（同在引擎清算/判定一带，等它落地避免 `GameState.fs` 撞车）

**Status:** ready-for-agent

## 两处修法（照 64 号研究文档 §11 的草案）

- [ ] **E2**：`AgariShape.waits` 前加向听闸——**向听 > 0 就不算 waits**（不听牌哪来的 waits）。
      注意研究里已踩过的坑：**判据是 `≤ 0` 不是 `= 0`**——20 万手采样否掉过 `= 0` 的第一版，
      反例是向听 **−1**（已和牌形）的手。把这个反例写成单测钉住
- [ ] **E3**：`canDeclare`（立直宣言判定）从逐张试打改为单次向听 `≤ 0` 前置
- [ ] 公开签名与语义**一字不动**（55 号的先例）；两处都是纯剪枝——**被剪掉的路径必须证明
      本来就返回空/false**

## 验收（性能票的语义证据，照 55 号的四类）

- [ ] 黄金用例 40+ 条逐字段零 diff；重录应无任何变化
- [ ] 真牌谱对拍：CI 固件 106 场零差异；**2025 整年重扫逐字节相同**（64 号预验证过，正式复跑一遍）
- [ ] soak 500 种子事件流逐条相同
- [ ] 双目标：Fable 侧黄金用例照跑（**剪枝逻辑两侧共编，别 `#if` 分叉**）
- [ ] 性能数字：引擎重放改前/改后（64 预验证 2.06×，正式复测）；`dotnet test` 墙钟与 CPU 之和；
      18 包全扫预估更新进登记册（64 给的是 E2+E3 后 4 核 2.7h / 32 核 20min）
- [ ] 登记册第 6 轮补「实现落地」一行

## 边界

- [ ] 只碰 `src/Janpo.Engine/` 判定路径与对应测试；不碰清算（65 刚改过）、不碰 `web/`、prompt、workflow
- [ ] 语料只读 `/home/xerxes2/janpo-corpus/`
- [ ] `./scripts/ci.sh` 全绿；预期墙钟应当**降**（振听省一半），报告给出前后数
