# 66 — 引擎性能第 6 轮实现票：振听簿记与立直判定加向听前置闸（E2+E3）

**What to build:** 票 64 拆开了 `GameState.step` 的 37µs：**振听簿记 `refreshFuriten`→`waits`
占 55%，而 83.6% 的手不听牌——那些 waits 计算全是白跑**；`canRiichi` 逐张试打再占 17%。
研究文档 §11 给了两处修法并**已在 /tmp 预验证**（引擎重放 2.06×、129,179 局重扫逐字节相同、
721 测试绿、20 万手采样零反例）。这一票把预验证变成正式代码 + 完整验收。

**Blocked by:** 65（同在引擎清算/判定一带，等它落地避免 `GameState.fs` 撞车）

**Status:** ready-for-human

## 两处修法（照 64 号研究文档 §11 的草案）

- [x] **E2**：`AgariShape.waits` 前加向听闸——**向听 > 0 就不算 waits**（不听牌哪来的 waits）。
      注意研究里已踩过的坑：**判据是 `≤ 0` 不是 `= 0`**——20 万手采样否掉过 `= 0` 的第一版，
      反例是向听 **−1**（已和牌形）的手。把这个反例写成单测钉住
      （钉在 `RiichiTests.已成和了形的手也立得了直`；闸的等价性另有 P1/P2′ 两条属性，
      各 live-fire 红过一次，报告 §3）
- [x] **E3**：`canDeclare`（立直宣言判定）从逐张试打改为单次向听 `≤ 0` 前置
- [x] 公开签名与语义**一字不动**（55 号的先例）；两处都是纯剪枝——**被剪掉的路径必须证明
      本来就返回空/false**（报告 §2：四条里三条是既有钉住属性的定理级推论，
      第四条靠采样 + 新属性 + 全语料重扫）

## 验收（性能票的语义证据，照 55 号的四类）

- [x] 黄金用例 40+ 条逐字段零 diff；重录应无任何变化（40 条 / 2069 字段 / 3378 行全一致；
      `golden write` 重录零 diff）
- [x] 真牌谱对拍：CI 固件 111 场（65 号后的全量）零差异；**2025 整年重扫逐字节相同**
      （178,888 场 / 1,893,891 局，差异同为 59 处、diffs 文件逐字节相同；顺带 2026 也扫了，
      同为 6 处逐字节相同）
- [x] soak 500 种子事件流逐条相同（四种选手 × 种子 1–500，每树 2000 场 / 1,288,194 行，
      `cmp` 静默）
- [x] 双目标：Fable 侧黄金用例照跑（**剪枝逻辑两侧共编，别 `#if` 分叉**；Fable 产物里
      核过两处剪枝都在）
- [x] 性能数字：引擎重放改前/改后（2.29→0.74 ms/局 ≈ 3.1×，整包端到端 1.90×）；
      `dotnet test` 墙钟 13.55→9.96 s、duration 之和 110.6→77.1 s；18 包全扫预估已按实测
      吞吐更新进登记册（4 进程 ≈ 3.9 h / 32 核外推 ≈ 29 min，口径差见报告 §5.5）
- [x] 登记册第 6 轮补「实现落地」一行

## 边界

- [x] 只碰 `src/Janpo.Engine/` 判定路径与对应测试；不碰清算（65 刚改过）、不碰 `web/`、prompt、workflow
- [x] 语料只读 `/home/xerxes2/janpo-corpus/`
- [x] `./scripts/ci.sh` 全绿；预期墙钟应当**降**（振听省一半），报告给出前后数
      （同机同盘交错：35.1 → 30.4 s 中位，−13%；`dotnet test` 那段全数体现）
