# 108 — 模型席那条路上的「过期问话」：一份在飞的问话会拿旧包落子

**What to build:** 票 92 修了强 AI 那条路的过期问话（`drain` 顶上按
「引擎此刻给这一席的合法动作集与包里那一列逐条相同」剪一遍 `Consulting`），
**并明说同一族的问题在模型席那条路上也存在**（`live.Awaiting` 没有这道剪枝），
只描述、不编号，留给调度器。就是这一票。

**为什么它比清理更急**：响应阶段可能在别席答完之后**当场散掉**（有人碰了），
那一席的待答整条消失，而它那份在飞的问话还留着——**包里的 id 是按旧的合法动作集编的号**。
`Awaiting` 那一侧今天没有剪枝，于是那份回执回来时，**同一个 id 指的是另一条动作**。
引擎会拒（`DecisionPackage.tryAction` 拿不到就没有事情发生），
但在那之前 `waiting` 为真、牌桌停着——**要么打错、要么卡住，两条都不能接受**。

**Blocked by:** 无

**Status:** ready-for-agent

## 先复现，再修（判据 1）

- [ ] **构造那个竞态**，不许靠时序碰运气：假端点按席给不同延迟（`fake-endpoint.mjs` 有现成开关），
      让 A 席的问话在飞时 B 席先答、且 B 的答复**让响应阶段散掉**（碰/杠）
- [ ] 断言**修之前**真的坏：那份过期回执回来时，要么落错一手、要么牌桌停住
      （把实际观察到的那一种写进报告，别照抄我这句猜测）
- [ ] 判据与票 92 **逐字同一条**：合法动作集逐条相同才算还在当下
      （`stillCurrent` 已经在 `TableState.fs` 里，别另写一个）

## 要害：过期的问话已经花掉钱了

这一票最容易做错的地方**不是剪枝，是剪掉之后的账**：

- [ ] **token 不许从账上消失**：那次问话真的调了 provider、真的计了费。剪掉它之后，
      账单行里那几个数**该是花掉的总额**，不是「落了子的那几手的总额」
      （票 79 / 83 立的账单口径，去读它们的报告再动手）
- [ ] **但它不许留下一条声称落了子的 `DecisionRecord`**：那一手没有发生。
      「花了钱、没落子」是真实情形，记录要说得出这件事
- [ ] 那一席**要被重新问**（散掉之后引擎给的是新的合法动作集），
      而重问不许把上一轮的历史丢掉（可缓存前缀那条属性别破：票 95 的 `prefix.test.ts`）
- [ ] 剪枝不许把**还在当下**的问话误剪（阴性对照：正常一局里剪枝次数应为 0，量出来写进报告）

## 闸门

- [ ] 那个竞态的定点用例：修前红、修后绿，红的原文进报告
- [ ] 一局四模型同桌的既有闸门全绿（`verify-llm-seat.mjs`、soak 那几趟）
- [ ] **账那一条**：构造一次过期问话，断言账单里那次调用的 token **还在**、
      且那一手**没有** `DecisionRecord`
- [ ] 正常一局的剪枝计数为 0（阴性对照，判据 20：量点停在那一局真的打完之后）
- [ ] `./scripts/ci.sh` 全绿

## 边界

- 你的地盘：`src/Janpo.Web/TableState.fs` / `Table.fs`、`tests/Janpo.Web.Tests/**`、
  `web/scripts/verify-llm-seat.mjs` 与 `fake-endpoint.mjs`
- **不碰 `src/Janpo.Web/Review*.fs`**（票 105 刚落地）、不碰 `src/Janpo.Engine/**`
- 不碰 `web/src/agent/**` 的 prompt 渲染（票 95 的记法闸门会红）；
  要动 `loop.ts` 的重试语义**先在报告里说清为什么**
- 加闸门趟数不用手改任何总述（票 106 立的：往 `verify-browser.mjs` 的 `gates` 表加一项）
- 新加的数会被票 107 的逐数溯源查（若你往页面上印剪枝次数之类的数，记得两处各写一格来源）

## 顺手记着

票 92 把这件事**写在报告里、不编号**——那是对的（它当时的地盘不含模型席那条路），
但它也因此在主线上躺了三天，期间跑过十几趟真人对局与 CI。
**「描述在这里，不编号」是一种把风险交出去的方式，只有在有人真的会接的时候才成立。**
这一票就是接的那一下；写报告时也照这个规矩：**你发现而没修的，要么编号、要么在简报里点名交回调度器。**
