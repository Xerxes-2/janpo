# 63 — 2025 整年语料抓到的两个新 bug 族：立直后暗杠判得太严、同巡振听没被自家鸣牌解除

**What to build:** 票 62 用 2025 整年包（178,888 场 / 1,893,891 局）扫出五类差异，
其中 **E、F 两族是我们的规则判定与天凤不符**。它按纪律只报不修，这一票来修。
两族都在立直/振听一带、都要重扫同一份语料验收，所以一张票一起做（59 号的先例）。

**Blocked by:** None（59 已落地）

**Status:** ready-for-human

## E 族（20 处）：`RiichiState.allowsAnkan` 第三条比天凤严

立直后暗杠的合法性判据，我们的第三条拒绝了天凤允许的暗杠。62 号报告里有 20 处实例
（`/home/xerxes2/janpo-corpus/` 的差异落盘里有逐场证据）。

- [x] 先把天凤的实际判据从那 20 处实例里**归纳出来**，写进报告（是「不改变听牌」「不改变牌姿」
      还是别的口径——**从证据归纳，别从记忆里背规则**）——fsi 逐条评估：20/20 ①②过、只敗在③，
      即天凤口径是「禁送り杠 + 听不变」，不附加面子构成（报告第一节逐场表）
- [x] 修 `allowsAnkan`；那 20 处在重扫时归零
- [x] 天凤判据与常见规则书若有出入，照天凤（默认口径是鳳凰卓，spec 定的）；
      是否需要 `Ruleset` 开关按 59 号责任支付那次的判法办——**现实里存在两种口径才开开关**
      （查证：M-League / 连盟 / 最高位战 / WRC 都要求面子构成不变 → 开关
      `RiichiAnkanMentsuUnchanged`，默认 false＝天凤）

## F 族（4 处）：同巡振听没被自家鸣牌解除

同巡振听的解除条件缺一条：**自家鸣牌**后天凤允许荣和，我们仍拒（±8300 点的实例在语料里）。

- [x] 同样先从 4 处实例归纳天凤口径，再修（两种子形各 2：同巡先过后鸣、鸣走听牌本尊；
      归纳：同巡的窗口到自家下一次**摸打**为止，修在 `PlayerState.addNaki`）
- [x] 注意 29a-3 当年把同巡振听改成「响应收齐才落」——**修这条时别把那个不变量弄坏**
      （掩蔽流里「我刚才过了」不是事件；`MinogashiStreamProperties` 在守它，弄坏会红）
      ——fold 与引擎共用同一个 `addNaki`，29a-3 一字未动，三方闸门全绿

## 验收（重扫是同一次，三件事一起验）

- [x] **E 20 处、F 4 处归零**（重扫 178,888 场 / 1,893,891 局：两族全部零差异，
      那 24 场整场逐事件与天凤一致）
- [x] **A 431 处、B 337 处也该归零**——A 全归零（含 13 场钱错）；B 真身 333 场归零，
      另 4 场重判为新类 G（它们一直藏在 B 的签名桶里，根因不同：包牌荣和的本场归属）
- [x] 重扫后剩下的差异**只许是 D 族**与**逐条查清的新类**——剩 64 处 / 59 场：
      D 55（51 + 4 残影，与 62 同一批 id）+ 新类 G 4 场（大三元包牌荣和时天凤把本场 300
      记在包牌家头上、引擎记在放铳家，4/4 逐场查实；只报不修，票号由调度器分配）
- [x] 那 24 处（E20+F4）的**代表性子集进 CI 固件**（`pick-fixtures.py`），撤修法当场红，红输出进报告
      （E 6 场 + F 4 场，96→106 场；分开撤两处修法各自当场红，输出在报告第四节）
- [x] 黄金用例照逐字段流程重录（若这两情形不在黄金用例里，diff 应为空，写明即可）
      ——重录 diff 为空（40 条 / 2069 字段 / 3378 行全一致），两情形确不在黄金用例里
- [x] soak 改前改后同种子事件流：只有涉及局该变（500 种子涉及局为 0，500/500 逐字节相同；
      非空覆盖：立直后暗杠 11 次、鸣后荣和形 31 次）
- [x] `./scripts/ci.sh` 全绿；CI 墙钟 ≤ 基线 34s 的 115%

## 边界

- [x] 语料只读 `/home/xerxes2/janpo-corpus/`（重扫输出落 `/tmp`，共享目录未写一字节）
- [x] 不碰 `web/`、不碰 prompt、不碰 workflow；术语表若要新词只报不改（提案 63-A 在 DECISIONS）
- [x] 剩余 17 个年度包**不在这一票**（62 的建议采纳：等这票落地再扫，否则每年 ~750 场 A/B
      重复噪声会淹没新东西）
