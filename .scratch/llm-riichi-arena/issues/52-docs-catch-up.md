# 52 — 文档追上 42 / 44 / 47 三票

**What to build:** `README.md` 与 `docs/development.md` 里有几处已经跟不上代码。项目已经公开，
**对外的每句话都要能兑现**——这是 M1 抓过两次的老毛病（README 说「有自动检查守着」但 CI 里跳过、
「还差 一整场验收」但已经跑过两次）。这一票只做**文档追平**，不改任何行为。

**Blocked by:** None

**Status:** ready-for-human

## 逐条（已核实为不实或不全，别再去论证它错，直接改）

- [x] **截图说明还在描述旧布局**：现在写的是「他家那几行看不到牌面」「四家的河与副露」——
      那是票 44 之前**四家竖排**的样子。44 已改成三×三真牌桌（对家上、上家左、自家下、下家右，
      场况在桌心）。**照当前的 `docs/images/table.png` 重写说明**，并核对 alt 文字里的种子与手数
      与图**真的对得上**（**注意**：票 51 正在重出这张图，若你集成时图已变，按新图写）
      → 图注改写成三×三真牌桌（自家在下 / 下家在右 / 对家在上 / 上家在左、场况在桌心、
      河朝桌心、副露在身侧、他家只看得见牌背、换座位方位跟着转），**不依赖任何计数细节**，
      51 重出图之后仍然成立；alt 里的「种子 1177 走了 52 手」亲手复核过
      （`node scripts/shoot-table.mjs --scan 1 --seed 1177 --turns 52` 打印「走了 52 手」，
      scan 档不写图，`docs/images/table.png` 一个字节没动）
- [x] **没提「自带 bot 有两种」**：票 42 加了**有主见的选手**（听牌就立直、能和就和、有役才鸣），
      牌桌上可选。README 三处说「三家随机选手」的地方要说清有两种可选（默认仍是均匀随机）
      → 三处（WIP 告示、「怎么玩」第 3 步、「现在能玩到什么」）全改；三条规则与默认值
      出自 `src/Janpo.Web/Roster.fs`（`Bot.toDisplay` 的两个标签、`TablePage.fs:392` 默认 Uniform）
      与 `reports/42-opinionated-random-players.md` §2 / §5
- [x] **重试语义变了**：票 47 之后，**不值得重试的错误不再重试**（key 不对、请求本身不合法、
      模型名不存在这类，再问一遍还是一样）→ 直接代打。README 现在只说「每手最多问 3 次」，
      要补上「重试没有意义的直接代打，不白烧请求」
      → 已补，并写清页面上两种收尾话（「没有重试」/「重试 2 次仍无结果」）。
      出处：`web/src/agent/retry.ts` 的 `NAMED_4XX`、`web/src/agent/loop.ts` 的 `if (!retry.worth) break`
- [x] **坏 key 演习的数字是 47 之前的**：README 写「60 次请求全部 4xx/5xx，20 手由引擎代打」。
      票 47 报告有改后的整场数字（255 → 85 次请求）。**要么复跑一局取新数**
      （便宜：坏 key 不发真请求成本，`/tmp/deepseek_key` 不需要），**要么引 47 的整场数字**，
      两者都要写清是「一局」还是「一整场」
      → **两个都写了，各自标了是一局还是一整场**：一局的数字**本次复跑**
      （`cd web && node scripts/verify-llm-seat.mjs --bad-key`，默认种子 2088 / 座位 1 / 裸奔档：
      20 次请求全 4xx/5xx、20 手全代打）；一整场那一组引 `reports/47-…` §3（85 次 / 85 手）
- [x] `docs/development.md`：CLI 新开关（`--opinionated` / `--hanchan` 等）与 CI 闸门道数是否还准，
      核一遍改对；**别把会漂的数字写进正文**（测试条数这类），要写就写「怎么自己查」
      → CLI 那节补上「开关以 `janpo --help` 为准」并点名 `--hanchan` / `--tonpuusen` /
      `--uniform` / `--covering` / `--opinionated`（`src/Janpo.Cli/Program.fs` 的 usage 与 parser）；
      「浏览器内三道」「JS 侧的那八道」两处闸门道数**不写死数字**，改成指向脚本头部注释；
      「构建五个工程」改成「构建解决方案里的全部工程」（`janpo.slnx` 现在是 6 个）；
      顺带补上 `pnpm run verify:invariants` / `verify:redaction` 两条真实存在的命令

## 纪律

- [x] **每个数字都要有出处**：要么你亲手跑出来（写清命令与种子），要么引某份报告的哪一节。
      核不实的**整条删掉**，不许改弱成含糊话（这是票 33 定的规矩）
- [x] 措辞照票 33 定的**用户向**语感：README 正文里不许出现票号、M 代号、
      以及 nix / dotnet / pnpm / Fable 这类词（`docs/development.md` 里可以有）
- [x] **不改任何行为**、不碰引擎与 `web/src/`、不碰 `.github/workflows/`
      （`jj diff` 只有 `README.md` 与 `docs/development.md` 两个文件）
- [x] 若发现别的不实之处（很可能有），**一并改并在报告里列出**
      （改了三处，另有三处在别人的禁改文件里，逐条列在报告 §4）——但只改「说法与事实不符」，
      不做文风重写
