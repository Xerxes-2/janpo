# 65 — G 族：大三元包牌荣和时，本场棒记错了人

**What to build:** 票 63 重扫 2025 整年时浮出的新类 G（4 场）：**大三元包牌**的**荣和**清算里，
**本场 300 点天凤记在包牌家头上、引擎记在放铳家头上**。此前它藏在 62 号 B 类（责任支付）的
签名桶里，59 修掉真 B 之后才现出原形——**修一族 bug 让下一族可见**，这正是逐族清账的理由。

**Blocked by:** None

**Status:** ready-for-human

## 要做的

- [x] 先读 63 号报告第三节与 `/tmp/t63/g-cases/` 的 4 场证据（**若 /tmp 已清，
      按 63 报告附录的复现命令从 `/home/xerxes2/janpo-corpus/` 重导**），
      归纳天凤的本场归属口径：包牌荣和时，本场棒（与供托？——**查证据，别猜**）各归谁付
      （本场整根归包牌家 4/4；供托照常归和了者，2 根那场实证；tenhou JSON 互证）
- [x] 修清算逻辑；那 4 场归零（`Score.hora` 的 `honbaPayers` 一处）
- [x] **顺带核对同族边界**：包牌**自摸**时本场归属对不对？四杠子/四喜和的包牌呢？
      语料里有没有实例？没有实例的写明「无语料佐证，按规则书口径，未验证」——
      **别把没验证的当成验证过的**（包自摸 7/7 语料实证引擎对；大四喜 0 实例、
      四杠子唯一实例无判别力，两条均写明未验证，见报告第三节）
- [x] 重扫 2025 整年：G 4 处归零，**剩下只许是 D 族**（55 处上游缺口）；出新类照四分类续编、只报不修
      （64→59 处 / 59→55 场，剩余与 63 的 D 名单逐场相同，新类为零）
- [x] 代表性场进固件（`pick-fixtures.py`），撤修法当场红，红输出进报告（106→111 场）
- [x] 黄金重录（不涉及则 diff 为空，写明）；soak 同种子只有涉及局变；
      `./scripts/ci.sh` 全绿、墙钟 ≤ 39.1s 上限（黄金空 diff；soak 500/500 相同且
      涉及局为 0 已量化；CI 热跑 33.6s / 33.9s）

## 调度器顺带裁掉的一条（63 的待裁三之三）

`riichi_ankan_mentsu_unchanged` 对旧导出件的取舍：**可缺省，缺省＝false（天凤口径）**，
不涨牌谱版本。依据是 26 号定的版本策略「加可缺省字段不涨版本」，与 59 号 `minkan_rinshan_sekinin`
同样处理。**本票顺带核一遍这两个开关字段的解码器行为一致**（都可缺省、缺省都是天凤侧）。

已核：两字段原都是 Required（与裁定不一致），已一并改 Optional + 默认 false，
`PaifuTests` 新用例钉住（含「其余字段仍必需」），版本仍是 3。见报告第六节。

## 边界

- [x] 语料只读；不碰 `web/`、prompt、workflow；术语表要新词只报不改（提案在 DECISIONS 65-4）
- [x] 票 64（step 成本研究）在 ws-b 的 `/tmp` 副本里量性能，与你无关，但**别去动共享语料目录的内容**（未写一字节）
