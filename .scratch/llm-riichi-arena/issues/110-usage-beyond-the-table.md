# 110 — 这笔账出不了牌桌：`Paifu` 带不走，`Table` 上还多了一个只能从历史算的字段

**What to build:** 票 108 给 `Table` 加了 `Voided: VoidedAsk list`，让账单说得出
「花了钱、没落子」。它同时交回来两件它没修的：

1. **牌谱带不走这笔账**：`Paifu` 只有 `Decisions`，作废的问话不在里面
   ——导出/分享/回放之后，「这一桌一共花了多少」就少了一块（而票 83 的账单口径是「花掉的总额」）
2. **判据 9 的形态债**：`Table` 上现在有**第二个**「只能从历史算出来、不能从局面算出来」的字段
   （第一个是票 87 那条的教训）。**这不是加个字段的事，是这块地的形状问题**

顺带第三件（产品口味 + 闸门）：**账单行要不要印「其中几笔没落子」**。

**Blocked by:** ~~109~~、~~111~~（都已集成）

**Status:** ready-for-human

## 这一票先想清楚，再动手

**不许直接往 `Paifu` 加一个 `Voided` 数组就交差。** 先回答三个问题，写在报告最前面：

- [x] **这笔账属于谁**？它是**这一桌的事实**（该进牌谱、该能分享）还是**这一次会话的事实**
      （只属于此刻这个浏览器，别人拿到牌谱也复现不出）？
      —— ADR-0002 说牌谱是唯一可分享的产物；票 83 说账单口径是「花掉的总额」。
      **这两句话在这件事上打架**，你要判并写出理由
- [x] 若进牌谱：**它会不会让牌谱不再可复算**？（牌谱今天是事件流 + 决策记录，
      fold 一遍就能重建牌桌；作废的问话不改局面，进去之后 fold 要怎么处理它）
- [x] 若不进牌谱：**导出之后账少了一块，页面要不要说出来**？
      （「这份牌谱里的账不含 N 笔作废的问话」——**没说出来的缺失就是骗人**）

## 形态债那一件

- [x] 读票 87 的报告与 `DECISIONS` 里那条（`LiveTable.Human` 被否掉的理由：
      「一个投影不许再存第二个只能从历史算的字段」），判断 `Voided` 是不是同一个形态
- [x] 给出**两三种做法与代价**：留在 `Table` 上 / 挪去一个专管账的地方 /
      改成从 `Decisions + 事件流` 现算（现算得看信息够不够）
- [x] **你可以判「留着不动」**——但要写清为什么它与票 87 那条不同，
      否则下一个人会拿票 87 那条判据来推翻你

## 闸门

- [x] 你选的那条路各自的先红后绿（判据 1）
- [x] 导出 → 导入 → 账对不对（选进牌谱那条路）；或页面说出缺了什么（选不进那条路）
- [x] 票 108 / 109 的用例与闸门全绿，账单那几个数改前改后 diff 逐字相同（除你有意改的）
- [x] `./scripts/ci.sh` 全绿

## 边界

- 可以碰 `Paifu`（这是唯一被授权动它的票），但**改了形状就要跑一遍牌谱那几道闸门**
  （导出两趟、从外面进来两条路、黄金用例）
- 不碰 `src/Janpo.Engine/**`、不碰 `Review*.fs`、不碰 `web/src/agent/**`
- **不改 `CONTEXT.md` / `docs/adr/*`**：若你判「账该进牌谱」，那是 ADR-0002 的修订，
  **写提案交调度器**
