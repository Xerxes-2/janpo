# 100 — 把「宣言中的暗杠」那条术语裁决落进断言

**What to build:** 主人 2026-08-19 裁定（第十次术语授权）：**宣言中的暗杠（与加杠加上去的那张）
是公开信息**，「掩蔽流里不出现他家暗牌」这条不变量的**定义域不含它们**。
术语表词条 `Ankan Declaration` 我已经写进 `CONTEXT.md`（`docs/adr/0006` 那一批之后）。
**这一票把那句话落进断言**，并顺手补上票 99 §5.4 点出的同型漏。

**Blocked by:** 无（99 已集成；术语已裁定）

**Status:** ready-for-human

## 要什么行为

- [x] `MaskedStreamProperties.任意局面任意座位，掩蔽流里不出现他家暗牌中的任何一张`
      的判据改成「他家暗牌**减去宣言中的那个暗杠/加杠那张**」
      ——**改的是定义域，不是强度**：改完之后它守的局面**比从前多一种**（今天那条轨迹被排在锚点外）
- [x] 把票 99 显式排除的那条国士抢暗杠轨迹**收进锚点**
      （`ChankanFixtures` 里现成，99 报告 §5.3 那张表点了名）
- [x] **补上同型漏（票 99 §5.4）**：加杠亮出去的那张若是赤宝牌、而底下那组碰里没有同记法的牌
      （碰的是 `5s 5s 5s`、加杠加 `5sr`），掩蔽流里会冒出一个引擎认为「仍在手里」的记法。
      **现有两条轨迹与真牌谱那两局碰巧都不踩**——摊一条踩得到的，让它先红一次再修判据
- [x] `MaskedEvent.fs` / `Observation.fs` **预期零改动**（99 已经把 fold 修对了，这一票只动判据与固件）；
      **真要改就先在报告里说清为什么**

## 闸门

- [x] 每一条改动**先红后绿**（判据 1），红的原文进报告
- [x] **判据 20**：阴性对照要停在「宣言中、还没结局」那一刻上量，不是走完一整局再抓整页
- [x] 改完之后**把定义域收窄这件事自证一次**：构造一个**不在**宣言窗口里的他家暗牌，
      断言那条不变量**照旧抓得住**（否则「收窄定义域」与「把断言调松」在证据上无法区分）
- [x] `dotnet fsi scripts/fsi/chankan-trace.fsx` 那 54 条**全绿**（今天剩 `Masked/不出现他家暗牌` 一条）
- [x] 引擎全量连跑 20 趟全绿；`./scripts/ci.sh` 全绿

## 边界

- 不碰 `src/Janpo.Web/**`、`web/**`（M3 那几票刚落地）
- **不许改 `CONTEXT.md`**（词条已经写好了，照它执行；措辞不合用就在报告里说）
- 不许改属性的**强度**：这一票只把「本来就不管的一段」写明，别顺手放宽别的
- 不碰 `src/Janpo.Engine/Shanten.fs`（主人有未提交改动）
- 你的地盘：`tests/Janpo.Engine.Tests/**`、`scripts/fsi/**`

## 顺手记着

判据 3 在 M3 里已有四个案例，判据 20 有三个。**这一票同时踩在两条上**：
它要动的那条断言今天有一段局面不管（判据 3 的形态），
而它要加的阴性对照必须停在宣言那一刻（判据 20 的形态）。**两条都在票面上，别只顾一条。**
