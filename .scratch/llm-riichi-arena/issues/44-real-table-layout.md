# 44 — 牌桌从四家竖排改成真牌桌，并补上「渲染给人」那侧的闸门

**What to build:** 现在四家是**竖排列表**，能看懂但不像牌桌 —— 座位之间的方位关系（谁是上家、
谁在对面）得靠读文字而不是看位置。改成方位正确的牌桌布局：自家在下、下家在右、对家在上、上家在左，
河向中心、副露向外。

同时补上 M1 留下的一处不对称：**「渲染给模型」有黄金用例逐字段钉着（2069 个字段），
「渲染给人」只有截图**。M1 里两次信息缺失（牌背隐形、副露没来源）都是主人肉眼发现的，CI 一次没抓到。

**Blocked by:** None — can start immediately.

**Status:** ready-for-human

落地：change `nkyxytqu`（工作区 `janpo-ws-a`），报告
`.scratch/llm-riichi-arena/run/reports/44-real-table-layout.md`。

## 布局

- [x] 方位正确：自家在下、下家在右、对家在上、上家在左；**方位由观测视角决定**（切座位/上帝视角时跟着转）
      —— `Board.position`（参照系 `Board.anchor`：坐着看是观测者，上帝视角是起家，页面中央把这句话写出来）；
      3 条单元用例 + 闸门里五个视角各一遍（属性与**坐标**两头都核）
- [x] 河朝中心、副露朝外；**保住票 38 定下的全部记号**：被鸣那张横放、来源文字、加杠的 `＋`、
      暗杠两端牌背、摸切虚线淡色、赤牌红字、刚摸那张的间距——**一个都不许弄坏**
      —— 七种逐条存活，且其中五种现在有闸门盯着（报告 §5）
- [x] 保住票 35 的读者面边界（默认视图无开发向内容）与票 37 的页脚 —— `verify-tracer` 那两道照旧绿
- [x] 窄视口不许挤烂（M1 验过 820px，这一票也要验）—— 820px 仍是三列真牌桌（图
      `44-table-narrow-god.png`）；40rem 以下才排成一列

## 「渲染给人」的闸门

- [x] 人能看见的信息每项都要有可断言的 `data-` 标记 + 一道断言，至少覆盖八项：
      **手牌（自家牌面/他家张数）、河（含手切摸切）、副露（含来源与被鸣那张）、点数、
      供托与本场、宝牌指示牌、巡目、立直状态** —— `web/scripts/verify-board.mjs`，已进 CI
- [x] 每道断言**先红一次**（临时去掉对应渲染），红的原始输出抄进报告 —— 13 次，报告 §4
- [x] 方位也要断言：给定观测座位，断言四家出现在正确的方位槽里
      （这条能抓住「切了视角但布局没转」这类错）—— 两条反向自证各红一次（参照系漂、格子摆错）

## 边界

- [x] **DOM 不用 Canvas，第一版无动画**（spec 明写动画预算后置）—— 一行 `canvas` / `transition` / `animation` 都没有
- [x] 合格线是**看得懂一局**，不是雀魂级演出。CSS 上不许过夜 —— 样式表净增 100 行（一个三×三格子 + 三排牌的朝向）
- [x] 不碰引擎、不碰 `web/src/agent/`、不碰牌谱格式 —— `src/Janpo.Engine/**`、`web/src/agent/**` 在 `jj diff` 里不出现
- [x] 截图会变：重出 `docs/images/table.png`，并**自己打开图看**（这条在 M1 立过规矩）
      —— 重出了，并另拍了浅/深两套配色、围观/上帝视角、820px 与终局那一屏，逐张写了看到什么（报告 §2）

## 为什么它是 M2 的前置

M2 要做四家 LLM 同桌 + 思考气泡。**四个气泡挂在竖排列表上没有合适的锚点**，
而布局一改气泡就得重挂。先把布局定下来，气泡才有地方长。
