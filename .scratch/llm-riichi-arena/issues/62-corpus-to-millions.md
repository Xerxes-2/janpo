# 62 — 把牌谱对拍推到百万场量级：先量这条路的开销，再流式扩语料

**What to build:** 主人要继续扩大真牌谱对拍以逼出更多 bug。调度器查明两件事：

1. **上游那个 release 有 18 个年度包（2009–2026，共约 11.4 GB）**，我们只用了 `2026.zip`
   （55MB，12,188 场——今年的残包）。**任意一个整年包约 190–290 万场**（按 2026 的解包比外推：
   ×10.4 解包、47KB/场）。也就是说**语料可以直接大两个数量级，且每个包只要 1 次请求**——
   不必再多打扰天凤。
2. **这条路的开销是 6ms/局**（调度器实测：固件 87 场 / 908 局，对拍墙钟 5.5s）。
   在 12 万局上无所谓（分钟级），**在 3000 万局上就是 50 小时单核**。所以主人问的
   「性能还能不能优化」——**在新量级上是真问题**，而在旧量级上不是。

**Blocked by:** None

**Status:** ready-for-agent

## 一、先量（不许先改）

- [ ] 把那 6ms/局**拆开**：mjson 文本解析、天凤 JSON 解析、牌山重建（`Wall.ofOrdered`）、
      引擎重放（逐事件 `step`）、比对本身，各占多少？**给出百分比**
- [ ] **调度器不猜这个分布**（判据 13 与它的补强：先问「一趟做几次」再问「一趟多贵」——
      解析是每行一次、重放是每事件一次、牌山重建是每局一次，次数级别差很多）
- [ ] 按测出来的分布决定改什么。**只做能把「一个整年包」变成可routine跑的那些改动**，别镀金

## 二、流式扩语料（**别把 12GB 落盘**）

- [ ] 语料放**共享目录** `/home/xerxes2/janpo-corpus/`（不在任何工作区里，别再每个工作区各存一份 723MB），
      `JANPO_PAIFU_DIR` 指过去
- [ ] **直接从 zip 流式读**、处理完不留解包文件（`fetch-corpus.py` 已经会用 Range 读中央目录，
      那套手法可以复用）。判据：跑完一个整年包，**磁盘净增长应当接近零**（除了压缩包本身）
- [ ] **可续**：记下已处理的 log id，中断了能接着跑；**抓到差异就落盘**（差异那几场要留证据）
- [ ] 分片并行（本机 32 核；**远端与 CI 一概不跑这个**，它是离线作业）
- [ ] **下行预算**：单个整年包 800MB–1.2GB，**超出 `workbook.md` 写的「单次下载 ≤ ~200MB」**。
      调度器**授权本票下载一个整年包**（对方是 GitHub release，不是天凤；1.4TB 空闲盘）。
      **一次一个包**，处理完再决定下一个。别一口气拉五个年份

## 三、oracle 的用法要反过来（**这一条是省下来的最大一笔**）

现在 1,058 场 oracle 是**盲取**的（1.5s/场、28 分钟）。而**点数与钱流的对拍根本不需要 oracle**
（票 13 的设计：本局打完 vs 牌谱下一局的开局点数；票 57 就这么比了 116,991 局）。
只有**役 / 符 / 番**需要天凤 JSON。

- [ ] 先用**免 oracle 的对拍**扫整年包，把**可疑的场**筛出来
- [ ] **只对可疑的那些场去取 oracle**（仍然 1.5s 间隔）。这样每一次打扰天凤都是有针对性的
- [ ] 顺带：若某种役在整年包里从没出现过，那也是覆盖信息（写进报告）

## 四、验收

- [ ] 改前 / 改后的**每局开销**与**整年包的预计总时长**（含核数）
- [ ] 跑完**至少一个整年包**的免 oracle 对拍：给出场数、局数、差异数与**逐类判定**
      （引擎错 / 上游噪声 / 口径不同 —— 票 57 的四分类可以直接用）
- [ ] 抓到真 bug **只报不修**（写清复现，立票交调度器排）
- [ ] `./scripts/ci.sh` 全绿；**CI 里跑的那份固件不动**（87 场 / 908 局，它是按覆盖挑的；
      要动固件另说）；CI 墙钟涨幅 ≤ 5%（这一票原则上不该动 CI）
- [ ] 磁盘：报告里给出**跑完之后的净增长**

## 边界

- [ ] 不改引擎的规则判定（这是对拍票，不是修 bug 票）；票 59 正在修两个真 bug，**别碰它的地盘**
      （`GameState` 的杠处理、`KanTests`、黄金用例、`tests/fixtures/paifu/`）
- [ ] 不碰 `web/`、不碰 workflow、不碰术语表
- [ ] **别把大语料提交进仓库**（`data/paifu/` 已 gitignore；共享目录在仓库外，更不会）
