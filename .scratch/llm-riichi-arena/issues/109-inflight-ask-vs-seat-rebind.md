# 109 — 在飞的问话遇上「换人」：开下一局清表、拨座位撤票

**What to build:** 票 108 把过期问话剪掉了、账也记住了「花了钱、没落子」，
但它交回来两件同族的、**它没修**的：

1. **`KyokuAdvanced` 把在飞的问话整表清空**，token 跟着从账上消失
   ——与票 108 修掉的那条是同一个病（账漏），只是触发点在「开下一局」而不是「剪枝」
2. **拨座位没有「撤回在飞那一票」的语义**：回执赶在他出手前回来时，**那一手仍由模型落**
   ——人已经把那一席拨给自己了，模型却替他打了一手

**Blocked by:** 无（108 已集成）

**Status:** ready-for-human

## 第一件：开下一局，那几笔账去哪了

- [x] 复现并量出来：一局结束时有几笔在飞（票 108 量过每局多席同问的分布，去读它的报告 §误剪那一节）
- [x] **口径要先判再写**（这是这一票真正要想的事，不是代码）：
      开下一局把问话作废，**算不算「花掉的钱」**？
      - 我的看法是**算**（钱真的付了），但**你要自己判并写出理由**；
        若你判「不算」，那就得说清账单那一行到此刻是什么口径，以及用户看到的数少了什么
- [x] 落地之后账要对得起那句话：`Table.usage` 是**花掉的总额**（票 108 立的），
      开局清表不许让它变小
- [x] `Table.creditVoid` 那条补账路径（回执晚回来）在跨局之后还成不成立？
      **成不成立都要有用例**——跨局的票号会不会撞、补到哪一局的账上

## 第二件：拨座位要撤票

- [x] 人把某一席从模型拨给自己（或拨给别的选手）时，**那一席在飞的问话当场作废**
      ——不是等它回来再剪（票 108 那道剪枝是兜底，不是语义）
- [x] 断言**回执赶在他出手前回来也不落子**：这是票 108 明确留下的洞
      （它的剪枝按「合法动作集是否还是当下」判，而拨座位那一刻动作集往往没变，所以剪不掉）
- [x] 反过来也要有阴性对照：**没换人**的正常一局，撤票次数为 **0**（判据 20，量点在那一局打完之后）
- [x] 拨给别的**模型**呢？（provider/key 换了，旧回执是上一份配置答的）
      ——同样作废，并在报告里写清「凭什么它不算这一席的答复」

## 闸门

- [x] 两件各自**先红后绿**，红的原文进报告（判据 1）
- [x] 跨局那件：断言 `Table.usage` 在开下一局前后**不变小**（用例里带上真实数字）
- [x] 撤票那件：构造「回执赶在出手前回来」，断言那一手**是人打的**、且账上有一笔作废
- [x] 票 108 的第 19 趟（`verify-stale-ask.mjs`）与既有闸门全绿
- [x] `./scripts/ci.sh` 全绿

## 边界

- 你的地盘：`src/Janpo.Web/TableState.fs` / `Table.fs`、`tests/Janpo.Web.Tests/**`、
  `web/scripts/verify-stale-ask.mjs`（往里加，别重排）
- **不碰 `Paifu` 的形状**：牌谱带不走这笔账那件事**是票 110**，别顺手做
  （110 还牵着判据 9 的形态债，要单独想）
- 不碰 `src/Janpo.Web/Review*.fs`、不碰 `src/Janpo.Engine/**`、不碰 `web/src/agent/**`
- 加闸门趟数不用手改总述（票 106）；往页面印新数要两处写来源（票 107）

## 顺手记着

票 108 的那句「**你发现而没修的，要么编号、要么在简报里点名交回调度器**」是这一票存在的原因
——它交回来五件，我编了两张（109 / 110），三件当场处置。**照这个规矩写你的简报。**

---

## 落地记录（agent 填，2026-08）

- **口径判「算」**（开下一局作废＝花掉的钱），理由与「判不算要付的代价」写在
  `run/reports/109-inflight-ask-vs-seat-rebind.md` §②。
- **第一条那个量**：一局结束那一刻在飞恒是 **0 笔**（80 场 / 336 局）——
  `KyokuAdvanced` 那条清表在结构上会丢账，但**今天从页面走不到**（§①，判据 4）。
- **撤票做成了语义**（`VoidCause.Rebound`），剪枝退成兜底；
  **顺带量到票 108 那道剪枝执行次数归 0**（修前剪枝 30 / 修后撤票 30）——交回调度器（§⑦ 第 1 条）。
- **`loop.ts` 一个字节没碰**：撤票不去 abort 那趟 fetch，三种做法与代价写在报告 §⑨。
