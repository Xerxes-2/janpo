# 27 — M1 验收：LLM 坐席跑完一整场东风战

**What to build:** M1 收官。LLM 坐一席、三家随机 Player，在浏览器里**打完一整场东风战**（不是一个
Kyoku —— spec 的下限是一局，但引擎 M0 已经能跑整场，局间推进在 UI 层也该跑一遍），导出 Paifu，
再把导出的事件流 fold 回来验证一致。跑完这一票，spec 的 M1 四项逐条成立。

**Blocked by:** 21, 24, 25, 26, 28, 29a, 29b, 30, 31, 32, 33, 34, 35, 36, 37

**Status:** ready-for-human

报告：`run/reports/27-m1-acceptance.md`。**这一票没有改任何产品代码**（`README.md` 删改一处不实声称、
报告 / 票据 / `DECISIONS.md`，以及报告目录下的五个跑批脚本与两张证据图）。

- [x] 一整场东风战打完：局间推进、连庄、本场、供托结转在 UI 层都正确显示，终局有精算与顺位
      —— 两档各打完一整场（Bare 4 局 / Assisted 7 局，东2局连庄 ×3、本场到 5 本场）；
      供托结转另用四家随机的种子 447 验（东2局2本场起三局都是「供托 1 根」，立直棒画着）。
      **⚠ 终局那一屏的点数有两个说法（座位卡 27000 / 精算 28000），只报不修 → 票 39**
- [x] 全程不卡死；LLM 的任何一手超时或报错都被兜底吸收，牌桌上能看出是哪几手
      —— 真跑两场兜底 0 手；另做一整场断电演习：255 次请求全 401、85 手引擎代打、4 局照样打完
- [x] 导出的 Paifu 重新 fold，事件流与终局点数逐项相同
      —— 四份牌谱 `same_events` 全 true；点数那一条的口径问题见票 39
- [x] Bare / Assisted 两档各跑一场，同一种子下 prompt 差异与决策差异都留在报告里
      —— 种子 2088；第一手可逐字节比：Bare 的尾部 517 字是 Assisted 那 2,371 字的字节前缀
- [x] 报告给出整场的**缓存命中率与实际账单**（票 29 让它可观测），以及命中失效时的最坏估算
      —— Bare 46%（一局内 20%→70%）/ Assisted 29%（同手数截断）；命中全废时付全价 token +86% / +42%
- [x] CI 双目标全绿：dotnet 测试、Fable 编译、JS 侧黄金用例、`pnpm build`、fantomas、风格闸门
      —— `./scripts/ci.sh` 全绿；dotnet 775 条、Agent 层 105 条、黄金用例 40 条 / 2,069 字段 / 3,378 行
- [x] M1 报告：Fable 踩坑清单、一整场的 token 与费用实测、延迟分布、兜底触发次数、
      以及 spec M1 四项的逐条验收陈述
      —— **Fable 一条坑都没踩**（全仓库连一处 `#if FABLE_COMPILER` 都没有）；只报 token 不报钱（裁决 29b-6）
- [x] 票据与文档同步：`CONTEXT.md` 若因这一里程碑多出新词（决策包、脚手架档位……）在此补齐
      —— **没有改**：29a/29b 立起来的六处已由票 31 收进去；还差的四个词是提案，
      按 RUNBOOK 不许 agent 自行改术语表 → 全部标【术】进了待裁索引

## 二、核对对外声称（M1 后半段反复踩的那类错）

- [x] `README.md` 与页面上的**每条声称逐条核实**，证据是「它在闸门里失败过」或「我亲手验过」
      —— 26 条逐条列在报告 §7.2；**删改一处**（「当时给模型的 prompt」→ 尾部 + 整场一份开头；
      thinking 补上「开了思考预算才有」）。另两条只有「别人报告里实测过」的（OAuth、本地端点）**保留但点名**
- [x] 真的打开线上站点核一遍：默认视图无开发面、页脚两条链接点得开、`?dev=1` 才出曳光弹
      —— 全过（报告 §7.1）：三个开发向 testId 各 0 个、正文不含「曳光弹 / Fable / dotnet」、
      两条链接**用真浏览器导航**都是 200、`?dev=1` 后五行数读得到、console 0 条错误、
      整页只请求自己那一个 origin
- [x] **CI 十道闸门逐道回答「它会不会红」**
      —— 报告 §8：**3 道常驻自证 + 4 道实证过 + 3 道没有**；dotnet 侧五道里这一票**补了三道自证**
      （风格闸门、Fable 白名单、fantomas），做法是把闸门原样拷到 `/tmp` 喂坏输入，**一行仓库代码没动**
- [x] 牌谱 v2 读得动、v1 也读得动（亲手核一遍导出→改版本→读回）
      —— 报告 §5.2：v2 ✓、手工降级的 v1 ✓、版本 3 读不动 ✓、preamble 掏空 `rebuildable` 报红 ✓

## 三、给主人的一张单子

- [x] 把 `DECISIONS.md` 里所有「待裁 / 提案 / 留给人」汇成**一张表**，补进已有的待裁索引
      —— 「### 第三批：M1 收官的全部挂账（27 号验收汇总）」，**29 条**；已落地的（20-A / 21-c / 23-A /
      29a-A / 29a-B / 29b-A / 29b-B / 34-A / 34-B）明确列为不再列出
- [x] 标出哪些是**术语表候选**
      —— 表里打【术】：31-A（`PromptTemplate` / `Persona` / `RenderVersion`）、36-A（打码）、
      26-A（`Replayed`）、25-A（`NoEvidence`「无依据」）
- [x] 报告里单开一节讲**形态返工的账**
      —— 报告 §10（为什么发生 / 代价的数字 / 收益 / 那条判据 / 还欠着的一笔）

## 四、纪律

- [x] **不许为了让某条验收通过去改实现**
      —— 终局显示那两处**只报不修**，立票 39；`verify-export.mjs` 的假红陷阱一并写进那张票
- [x] 真跑用真 key，思考预算默认不开，key 绝不进任何文件
      —— `--thinking off`；key 只从 `/tmp/deepseek_key` 读、只经 `addInitScript` 注入 localStorage
- [x] 两档各一场用**同一种子**
      —— 2088（页面默认种子，也是此前各票用的那个）
