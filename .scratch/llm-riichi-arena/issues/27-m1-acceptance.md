# 27 — M1 验收：LLM 坐席跑完一整场东风战

**What to build:** M1 收官。LLM 坐一席、三家随机 Player，在浏览器里**打完一整场东风战**（不是一个
Kyoku —— spec 的下限是一局，但引擎 M0 已经能跑整场，局间推进在 UI 层也该跑一遍），导出 Paifu，
再把导出的事件流 fold 回来验证一致。跑完这一票，spec 的 M1 四项逐条成立。

**Blocked by:** 21, 24, 25, 26, 28, 29a, 29b, 30, 31, 32, 33, 34, 35, 36, 37

**Status:** ready-for-agent

- [ ] 一整场东风战打完：局间推进、连庄、本场、供托结转在 UI 层都正确显示，终局有精算与顺位
- [ ] 全程不卡死；LLM 的任何一手超时或报错都被兜底吸收，牌桌上能看出是哪几手
- [ ] 导出的 Paifu 重新 fold，事件流与终局点数逐项相同
- [ ] Bare / Assisted 两档各跑一场，同一种子下 prompt 差异与决策差异都留在报告里
- [ ] 报告给出整场的**缓存命中率与实际账单**（票 29 让它可观测），以及命中失效时的最坏估算
- [ ] CI 双目标全绿：dotnet 测试、Fable 编译、JS 侧黄金用例、`pnpm build`、fantomas、风格闸门
- [ ] M1 报告：Fable 踩坑清单、一整场的 token 与费用实测、延迟分布、兜底触发次数、
      以及 spec M1 四项的逐条验收陈述
- [ ] 票据与文档同步：`CONTEXT.md` 若因这一里程碑多出新词（决策包、脚手架档位……）在此补齐

## 二、核对对外声称（M1 后半段反复踩的那类错）

项目已经公开：仓库 https://github.com/Xerxes-2/janpo ，站点 https://xerxes-2.github.io/janpo/ 。
**对外的每一条声称都要能兑现**，而这一轮抓到过两次「记录声称了一件事、执行体不存在」
（README 说「有自动检查守着」但那道断言在 CI 里被跳过；排班表标着「派工中」却没有 agent）。

- [ ] `README.md` 与页面上的**每条声称逐条核实**，证据是「它在闸门里失败过」或「我亲手验过」，
      **不是「代码里有这一行」**。核不实的当场删（不许改弱成含糊话），并在报告里列出删了哪些
- [ ] 真的打开线上站点核一遍：默认视图无开发面、页脚两条链接点得开、`?dev=1` 才出曳光弹
- [ ] **CI 十道闸门逐道回答「它会不会红」**：列出哪几道已有反向自证（票 34 的 `--poison`、
      票 35 的默认视图断言、票 36 的阳性对照、票 37 的页脚断言），哪几道没有。
      没有的不必现在补，但要写进报告——一道从不失败的闸门等于没有闸门
- [ ] 牌谱 v2 读得动、v1 也读得动（票 31 有测试，验收要亲手核一遍导出→改版本→读回）

## 三、给主人的一张单子

M1 到此结束，攒了一路的待裁项要一次性摊给主人，别散在十几份报告里。

- [ ] 把 `DECISIONS.md` 里所有「待裁 / 提案 / 留给人」汇成**一张表**（编号、一句话、不裁的后果、在哪），
      放在 `DECISIONS.md` 的 M1 待裁索引里（那张表已经存在，把后来的补齐）
- [ ] 标出哪些是**术语表候选**（`PromptTemplate`/`Persona`/`RenderVersion`/打码/`Replayed`/`NoEvidence`…）——
      这些堆着会让 `CONTEXT.md` 逐渐失真
- [ ] 报告里单开一节讲**形态返工的账**：29a/29b 为什么发生（快照 vs 事件历史）、代价是多少
      （`Observation` 类型未变、下游三票零改动、prompt 与牌谱各改一次）、以及那条判据
      「一个投影里出现第二个只能从历史算的字段，就是形态错了」。**这是 M1 最值钱的产出，不许省略**

## 四、纪律

- [ ] **不许为了让某条验收通过去改实现**。发现不符就如实记、park 掉，让主人裁
- [ ] 真跑要用真 key（`/tmp/deepseek_key`，`deepseek-v4-flash`）。**思考预算默认不开**——
      实测单手 medium 思考要 17–180 秒，整场跑不完，且账单没必要。key 绝不进任何文件
- [ ] 两档各一场用**同一种子**，否则 prompt 与决策的差异无法归因
