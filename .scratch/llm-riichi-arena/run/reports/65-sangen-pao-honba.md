# 65 票 —— G 族：大三元包牌荣和时，本场棒记错了人

**STATUS: done**（先从 4 场证据 + 天凤官方 JSON 归纳口径再动手：**包牌荣和时本场棒整根由
包牌家付**（4/4 实证），供托照常归和了者、不受包影响（2 根供托那场实证）；修法一处——
`Score.hora` 的 `honbaPayers` 在「荣和 + 有包」时从放铳家换成责任者。2025 整年重扫
**64 处 / 59 场 → 59 处 / 55 场**：G 4 场归零，剩余 55 场与 63 号的 D 族名单**逐场相同**、
零新类。同族边界用全年 48 处大三元包牌实例核过：**包牌自摸的本场归包牌家 7/7 语料实证**、
大四喜包牌与四杠子包牌无判别性语料（分开列明，未当成验证过的）。固件 106→111 场
（G 全部 4 场 + 1 场包牌自摸），撤修法当场红。顺带活：两个规则开关字段的解码器行为
已对齐成**都可缺省、缺省都是天凤侧（false）**，新用例钉住。）

## 一、从证据归纳的口径（先证据后修法）

**证据源有两份、互证**：mjai 事件流（`/tmp/t63/g-cases/`，63 号提好；复现命令见 63 号报告附录）
与天凤官方 JSON（`mjlog2json.cgi`，本票取的 oracle，`[和了家, 放铳家, 包牌家]` 是天凤自己写的字）：

| 场 / 局 | 和了家 | 放铳家 | 包牌家 | 本场 | 供托 | 天凤 deltas |
|---|---|---|---|---|---|---|
| 2025030716-d863b937 k7 | s3 | s1 | **s0** | 1 | 0 | s0 **-16300**, s1 -16000, s3 +32300 |
| 2025051020-f618f429 k9 | s0 | s1 | **s3** | 1 | 0 | s1 -16000, s3 **-16300**, s0 +32300 |
| 2025061121-5f8f6cdd k2 | s2 | s3 | **s0** | 1 | **2** | s0 **-16300**, s3 -16000, s2 **+34300** |
| 2025110918-81d698a8 k10 | s3 | s0 | **s1** | 1 | 0 | s0 -16000, s1 **-16300**, s3 +32300 |

**归纳**：

1. **本场棒（300 × 本场数）整根由包牌家付**，不是放铳家、也不是两家分——4/4 无一例外
   （引擎原来记在放铳家头上，两家各 16000 的劈半没错、错的只有 300 的归属）。
2. **供托照常归和了者、不受包影响**：5f8f6cdd 的 2 根供托原样进和了家的增项
   （32000 + 300 + 2000 = 34300），两个付家分文不涉——查了证据，不是猜的。
3. 役满劈半本身（放铳家、包牌家各 16000）两侧一致，59 号之前就对。

## 二、修法

`Score.hora`（`src/Janpo.Engine/Score.fs`）一处：`honbaPayers` 的荣和分支从恒取
`[ transfer.Target ]` 改为**有包时取 `[ liable ]`**（没包不变；包牌家=放铳家时两者同座位，
行为连续）。自摸分支原样——它本来就是 `charges` 的付家（包了就只剩责任者一家），
即包牌自摸的本场归包牌家**引擎原来就对**（第三节的语料实证也证实天凤同此）。
和了点、级别、符番、供托一概不动；`KanProperties` 的两条包不变量
（「包只改谁付、不改和了点」「自摸由责任者一家付光」）原样全过。

单元测试（`ScoreTests`，TDD 先红后绿）：

- `包牌荣和：本场由包牌家付，供托照常归和了者`——修前红：
  `Expected: [0, 32300, -16000, -16300]  Actual: [0, 32300, -16300, -16000]`（300 挂错了人）。
- `包牌家自己放铳：全额与本场都由它付`、`包牌自摸：包牌家一家付光，本场也全归它`——
  钉住两条邻边（修前修后都绿，防回归）。

## 三、同族边界的核对（验证过的与未验证的分开列）

自写 `/tmp/t65/boundary-scan.py`（纯数据解析，字节预筛 + JSON 确认，不移植引擎）扫 2025
整年包，得**大三元包牌实例全年 48 处**（含 G 4 场），全部与重扫差异名单零交集
（＝修完引擎逐场与天凤一致）：

| 形 | 本场=0 | 本场>0 | 判定 |
|---|---|---|---|
| 荣和、包牌家≠放铳家（劈半） | 8 | **4（=G 族）** | **验证过**：本场归包牌家（修的就是它） |
| 荣和、包牌家=放铳家（退化） | 15 | 2 | **验证过**：全额+本场一家付，两侧本来一致 |
| **自摸** | 12 | **7** | **验证过**：包牌家付全额 + **全部本场**（如 5171bc17 k5：2 本场，包牌家 -32600；df6f1a79 k2：亲役满 1 本场，包牌家 -48300）——引擎原行为即此，7/7 零差异 |

**没有语料佐证的两条，按规则书口径列明、未验证**：

- **大四喜包牌**：2025 整年大四喜只和出 2 次（役种普查），两次都不足四组风牌副露，**全年零包牌
  实例**。引擎按与大三元同一条 `completedBy` / `Score.hora` 路径处理（第四组风牌喂家负责，
  本场同归包牌家）——按规则书口径与代码同构性推定，**无语料佐证，未验证**。
- **四杠子包牌**：2025 唯一一场四杠子（2025022622-21335aa8 k8）的四杠是 1 暗杠 + 3 加杠、
  自摸，天凤按常规自摸劈账（16000/8000/8000）——**没有「第四杠是大明杠」的判别性实例**。
  引擎任何规则集都不给四杠子立包（59 号既有口径），与天凤官方手册「包＝大三元/大四喜」一致；
  「喂第四杠」那一支**无语料佐证，未验证**。

## 四、重扫 2025 整年：G 归零，剩下逐场等于 D

62 号管道原样重跑（4 分片 `nice -n 19` 各 21–22 分钟，共享语料只读，输出落 `/tmp`）：

| | 63 号（修 G 前） | **本票（修 G 后）** |
|---|---|---|
| 场 / 局（显式跳过） | 178,888 / 1,893,891（0） | **178,888 / 1,893,891（0）** |
| 和了 / 清算座位 / 终局点数对拍 | 1,608,793 / 1,050,092 / 1,715,003 | **逐位相同**（覆盖没缩水） |
| 差异 | 64 处 / 59 场 | **59 处 / 55 场** |
| G. 包牌荣和的本场归属 | 4 场 | **0** |
| D. 三家和了被上游删成裸 ryukyoku | 55 场 | **55 场**（51 裸 ryukyoku + 4 立直宣言被三响吞掉的残影）|
| 新差异类 | — | **零**（剩余 55 场 id 与 63 号名单 `diff` 为空） |

## 五、固件与闸门

- **+5 场（106 → 111 场 / 1,128 局 / 6.4 MB）**，`pick-fixtures.py` 同一条命令：G 族仅有的
  4 场进 `--seed` 无条件保留，另加 1 场**包牌自摸**（df6f1a79，亲役满包自摸 + 1 本场 + 立直棒，
  钉自摸那一支）；`REPEAT` 补 `pao`（抵 5，corpus-features 从天凤 JSON 的「包牌家≠和了家」数出）。
  **挑选可复现已实测**：同输入重跑 `PICK REPRODUCED ✓`，与仓库固件逐场相同。
  顺带修了 `pick-fixtures.py` 一个既有边角：`--seed` 已含全部候选时贪心步 `max()` 空列表崩，
  补「池子挑空了就停」的 guard（行为不变）。
- 新增闸门 `包牌的荣和与自摸在固件里各走到几次`：从牌谱事件流自己数（荣和负项 ≥2 家＝包劈半、
  自摸负项恰 1 家＝包一家付光；供托在 `reach_accepted` 已扣，负项只能是付家），
  荣和 ≥4、自摸 ≥1，少一次就红（判据 3；票 65 之前包牌在 CI 的执行次数是 **0**——
  57 号的 12,188 场里一次都没有，而 2025 带本场的包牌荣和局局带差异进不了候选池）。
- 阈值只抬不降：场 106→111、局 1090→1120、和了 930→960、符 680→700、役行 1680→1740、
  清算 560→580、终局点数 980→1010（役种 39 不变——新场没带进新役种）。
- **反向自证（判据 1），红的输出**：撤修法（`honbaPayers` 改回恒 `[Target]`）→
  - `ScoreTests.包牌荣和…` 当场红（第二节那条 Expected/Actual）；
  - `重放产出的事件流与牌谱零差异` 当场红：`Actual: ["2025030716gm-…-d863b937 第 7 局（2z2局
    1 本场）…", "2025051020gm-…-f618f429 第 9 局…", "2025061121gm-…-5f8f6cdd 第 2 局…",
    "2025110918gm-…-81d698a8 第 10 局…"]` —— **恰是 G 族全部 4 场**；包牌自摸那场不红
    （自摸支未动，正合预期）。

## 六、顺带活：两个规则开关字段的解码器一致性（调度器已裁）

裁定：`riichi_ankan_mentsu_unchanged` 与 `minkan_rinshan_sekinin` **都可缺省、缺省都是
天凤侧（false）、不涨牌谱版本**（26 号版本策略「加可缺省字段不涨版本」）。核对结果：
两个字段在 `Ruleset.decoder` 里原都是 `Required`（59/63 各自加的，63 号报告待审项 3 记过）
——**不一致的是「与裁定不一致」，两字段彼此倒是一致的**；已一并改成
`get.Optional.Field … |> Option.defaultValue false`，解码器文档注释写明「例外只有这两个
后加的规则开关」的理由（旧导出件就是按天凤口径打的，补 false 回放逐字相同）。
新用例 `PaifuTests.两个规则开关字段可缺省，缺省都是天凤侧，其余字段仍是必需`（TDD 先红后绿）
钉三件事：缺两字段读得动且都是 false、写过 true 的读回来是 true（可缺省 ≠ 被忽略）、
**其余字段仍是必需**（抽 `kuitan` 验证——可缺省是这两个字段的例外，不是解码器的新设计）。
牌谱 `version` 仍是 3，未动。

## 七、黄金用例与 soak

- **黄金用例**：`janpo golden check` 全绿；照 28 号流程 `janpo golden write` 重录，
  **diff 为空**（`40 条用例、2069 个字段、3378 行：全部一致 ✓`）——黄金里唯一的包牌用例
  `points-sekinin-split` 是 **0 本场**，不在本票的改动面上，与预期一致。
- **soak 同种子对比**（改前＝基点 `563401d3` 的引擎源另建 CLI，改后＝本树；
  `janpo game <seed> --covering` 各 500 种子）：**500/500 逐字节相同**。涉及局为 0 属实：
  500 场 843 次和了里包牌零出现（役满包牌全年 178,888 场才 48 处，500 场撞不上合乎期望），
  「只有涉及局该变」以零变化的形式兑现；解码器改动不在对局路径上。

## 八、CI 与预算

- `./scripts/ci.sh` 全绿：热跑 **33.6s / 33.9s**（review 修一轮后重跑 34.2s；
  上限 34s × 1.15 = 39.1s，余量充足）；
  `dotnet fantomas .` 干净。
- 机器：重扫 4 进程 `nice -n 19` 约 22 分钟；边界扫描单进程 nice 约 12 分钟（重扫收尾后才起）；
  soak `xargs -P 4`，并行进程始终 ≤ 4。
- 语料只读：`/home/xerxes2/janpo-corpus/` 未写一个字节（重扫与边界扫描输出都落 `/tmp`）；
  零下载；天凤侧 **5 次请求**（4 场 G + 1 场包牌自摸的 oracle，1.5s 间隔）。

## 九、review 记录（Standards + Spec，fixed point `563401d3`）

派不出 sub-agent，两轴顺序自跑（workbook 允许的退路）。

**Standards 轴，blocking 一处（review 中发现、当场修一轮并重跑全量）**：`ScoreTests` 两条
新用例的注释写于边界扫描之前，说「包牌家自己放铳语料无实例」——而扫描实际找到 17 处
（含本场 2 处）；包自摸那条也含糊。注释与证据不符是反向的「把验证过的当没验证」，
同样是准确性缺陷，已改成引实例数与零差异结论。nitpick（只记录不修）：

- `paoHoras` 与 `boundary-scan.py` 的判形逻辑同源不同工（那一份扫全量、这一份守 CI），
  沿 59 号 `kanTimings` / `kanTags`、63 号 `riichiAnkans` 的既有先例，注释互指。
- `ScoreTests.荣和的本场由放铳者一家付，供托归和了者` 的**用例名**在包牌语境下有歧义
  （它测的是没包的常规荣和，断言本身仍对）；紧邻的新用例已写明包牌口径，未改旧名。
- fsharp-style 逐条过：无新增 `let mutable`、无三层嵌套变换链、`honbaPayers` 的 match
  不属强行管道的三种情形；`check-style.sh` 过（CI 里）。

**Spec 轴（逐条对票面）**：证据先行（第一节 4 场 + oracle 互证，供托也查了）；修完 4 场归零
（第四节）；同族边界验证过的与未验证的分开列（第三节，两条未验证明写）；重扫 G 归零、
剩余**只是** D 族 55 场、零新类（第四节，id 级 diff 为空）；固件经 `pick-fixtures.py`、
撤修法当场红、红输出在第五节；解码器一致性按裁定落地并有用例（第六节）；黄金空 diff 有说明、
soak 同种子 500/500 且涉及局为 0 的理由已量化（第七节）；CI 33.6/33.9s < 39.1s（第八节）。
边界三条：语料只读（第八节）、`web/` / prompt / workflow 零改动（`jj diff` 无这些路径）、
术语只报不改（CONTEXT.md 未动，提案在 DECISIONS「## 65」）。无 scope creep：
大四喜/四杠子只核对不立新开关，边界发现照四分类续编的新类为**零**。

## 十、留给人的待审项

1. **CONTEXT.md 两处措辞提案**（DECISIONS「## 65」，等授权）：「Honba 的点数」词条的
   「和了时由放铳者或三家分担」宜补「有包时由包牌家付（票 65 的 4/4 实证）」；
   「Sekinin Barai」词条宜补一句本场归属。
2. 大四喜包牌、四杠子「喂第四杠」两支无语料佐证（第三节）：剩余 17 个年度包开扫后
   若出实例，值得回头验这两支。
3. `ScoreTests` 旧用例名的歧义（第九节 nitpick），随手改名即可，本票未动。

## 附：复现命令

```bash
# 2025 整年重扫（62 号管道原样；输出落 /tmp，共享语料只读）
python3 scripts/paifu/zip-index.py /home/xerxes2/janpo-corpus/2025.zip > /tmp/janpo-scan/index-2025.tsv
for s in 0 1 2 3; do nice -n 19 dotnet fsi --exec scripts/fsi/paifu-scan-zip.fsx -- \
  /home/xerxes2/janpo-corpus/2025.zip /tmp/janpo-scan/index-2025.tsv /tmp/t65/scan-2025-post65 $s 4 & done
python3 scripts/paifu/summarize-scan.py /tmp/t65/scan-2025-post65

# 同族边界扫描（纯数据解析；脚本正文在本票报告同批的 /tmp/t65/boundary-scan.py，
# 判形：三元/风牌副露样式与杠事件计数预筛 → JSON 确认 → 按（自摸/荣和 × 本场 × 包牌家）归类）
nice -n 19 python3 /tmp/t65/boundary-scan.py /home/xerxes2/janpo-corpus/2025.zip \
  /tmp/janpo-scan/index-2025.tsv > /tmp/t65/boundary-hits.tsv

# 固件挑选（可复现；候选池 = 旧固件 106 + G 族 4 场 + 包牌自摸 1 场）
python3 scripts/paifu/corpus-features.py <池目录> > features.json
nice -n 19 dotnet fsi --exec scripts/fsi/paifu-scan.fsx -- <池目录> 0 1 > pick-scan.tsv
python3 scripts/paifu/pick-fixtures.py pick-scan.tsv features.json \
  --seed <旧 106 场 + G 4 场 + df6f1a79 的名单> --kyoku 1000 --corpus <池目录>

# G 族四场与包牌自摸场的提取 / oracle
python3 scripts/paifu/extract-from-zip.py /home/xerxes2/janpo-corpus/2025.zip <目录> \
  2025030716gm-00a9-0000-d863b937 2025051020gm-00a9-0000-f618f429 \
  2025061121gm-00a9-0000-5f8f6cdd 2025110918gm-00a9-0000-81d698a8 \
  2025120619gm-00a9-0000-df6f1a79
python3 scripts/paifu/fetch-corpus.py <目录> --ids <名单> --oracle
```
