# 52 —— 文档追上 42 / 44 / 47 三票

**状态**：done　**工作区**：`janpo-ws-a`　**fixed point**：`aa83bc01`（change `ynkxwpzq`）
**验证**：`./scripts/ci.sh` **EXIT=0**（dotnet 侧 + JS 侧十三道，浏览器内七道全 ✓）
**改到的文件**：`README.md`、`docs/development.md` —— **就这两个**，一行代码没动
（`jj diff --stat` 只有这两条；引擎、`web/src/`、`.github/workflows/`、`docs/images/table.png` 全未触碰）。

一句话：把 README 与 `docs/development.md` 里**已经与代码不符**的七处逐条改对，
每个数字要么本次亲手跑出来（写清命令与种子），要么点名引哪份报告的哪一节；
另有三处不实长在这一票禁改的文件里（页面文案、脚本注释、截图），只报告不改，理由逐条写在 §4。

---

## 1. README：逐条「原文 → 改成什么 → 出处」

### 1.1 截图图注（票 44 的三×三真牌桌）

**原文**

> 围观视角（座位 0）下的牌桌：场况、四家的河（虚线是摸切）与副露、自己的手牌。
> 他家那几行看不到牌面——**模型看到的和你一样多**，别人的暗牌在页面拿到的数据里根本不存在。
> 想复盘就按一下切到上帝视角。

**改成**

> 围观视角（座位 0）下的牌桌：四家围坐——**自家在下、下家在右、对家在上、上家在左**，
> 场况（局数与本场、供托、剩余摸牌、宝牌指示牌）摆在桌子中央，每家的河朝桌心、副露在身侧，
> 河里虚线的那几张是摸切。**他家只看得见牌背**——模型看到的和你一样多，
> 别人的暗牌在页面拿到的数据里根本不存在。想复盘就按一下切到上帝视角；
> 换个座位坐，四家的方位跟着转。

**出处**

| 这句话 | 凭什么 |
|---|---|
| 四家的方位与「方位跟着视角转」 | `src/Janpo.Web/Board.fs` 的 `Board.position` / `Board.anchor`；页面自己在桌心写着「坐在座位 0：自家在下、下家在右、对家在上、上家在左」；`reports/44-real-table-layout.md` §1.1 §1.3、CI 第八道 `verify-board.mjs` 的方位断言（§3.3） |
| 场况在桌子中央、那四项 | 44 报告 §1.1（`.board-head` 整条搬进 `.table-center`，testId 一个没改）；**我自己打开 `docs/images/table.png` 看过**：桌心写着「场况 东1局 0 本场／供托 0 根／剩余摸牌 30 张／宝牌指示牌 1万」 |
| 河朝桌心、副露在身侧 | 44 报告 §1.2 那张「三排从外往内摆」的表 |
| 虚线是摸切 | `TablePage.fs:527` 那段注释与 `verify-board.mjs` 的「摸切必须是 dashed」断言（44 §3.2 ②） |
| 「他家只看得见牌背」 | `verify-board.mjs` 第①项（自家牌面、他家牌背，且牌背边框不许透明） |

**「他家那几行」是这次删掉的那句**：44 之后四家不再是四行，是四个方位上的面板
（44 报告 §9 第 1 条点名它「现在略旧」）。**没有改弱成含糊话，是换成当前布局的准确说法。**

**为什么图注不写任何计数**：票 51 正在改副露画法并会**重出这张图**。
图注现在说的全是布局与信息量（谁在哪一边、河朝哪、他家看不看得见牌面），
**51 重出图之后逐句仍然成立**；真要写「几张牌、第几巡」就会又过期一次。
→ **51 落地后建议再核一次**：若它把副露挪出了「身侧」那一档，图注里「副露在身侧」那半句要跟着改。

**alt 文字亲手复核过**：`![牌桌截图：围观视角坐在座位 0，种子 1177 走了 52 手]` 一个字没改，
因为它是对的 ——

```
$ cd web && node scripts/shoot-table.mjs --scan 1 --seed 1177 --turns 52
种子 1177　走了 52 手　有副露的家 4　[0] 河11 吃/吃 亲/视角　[1] 河11 碰 　[2] 河11 碰 　[3] 河11 碰
```

`--scan` 那一档**只印形态不写文件**，所以 `docs/images/table.png` 一个字节没动（`jj st` 里它不出现）。
印出来的四家副露与河长度，与图上看到的逐项对得上（座位 0 两副吃、其余三家各一副碰、四家河各 11 张）。

### 1.2 三处「三家随机选手」→ 说清有两种（票 42）

| # | 原文 | 改成 |
|---|---|---|
| a | WIP 告示：「一个座位交给模型 + **三家随机选手**」 | 「一个座位交给模型 + **三家自带选手**」 |
| b | 第 3 步：「挑一个座位交给模型（现在只支持一席，**其余三家是随机选手**）。」 | 「挑一个座位交给模型（现在只支持一席）。其余三家由自带的选手打，**两种可选**：**均匀随机**（默认，从这一手能做的动作里等概率挑一个）与**有主见**（听牌就立直、能和就和、有役才鸣——想看牌桌上真的出现立直、一发与供托就选它）。」 |
| c | 「现在」：「一个座位交给模型、**三家随机选手**」 | 「一个座位交给模型、**三家自带选手（均匀随机或有主见）**」 |

**出处**：两个中文标签逐字取自 `src/Janpo.Web/Roster.fs` 的 `Bot.toDisplay`（「均匀随机」/「有主见」，
牌桌上那两个按钮就是它）；「等概率挑一个」取自同文件 `Bot.Uniform` 的文档注释；
三条主见取自 `reports/42-opinionated-random-players.md` §2（能和就和 / 听牌就立直 / 有役才鸣）；
**默认仍是均匀随机**核的是代码不是报告：`TablePage.fs:392` 的 `Bot = Bot.Uniform`（42 报告 §5 记的也是这个）。
「真的出现立直、一发与供托」这句的底气是 42 报告 §1.1 那张表（2000 颗种子：立直场次 15 → 1996、
一发 1 → 1045、供托结转 3 → 1148）——**README 正文里不写这些数**，它们是开发向的证据，
用户要知道的只是「选它才看得到这些」。

### 1.3 重试语义（票 47）

**原文**（「模型不听话会怎样」那段）

> 接住之后带着原因重问，**每手最多问 3 次**；仍然交不出来，就由规则引擎替它打一手（…）。

**改成**：那两句原样保留，后面**补一段**

> **重问没有意义的那几类不重问**——key 不认、请求本身不合法、模型名或端点地址不存在这种，
> 同一份请求再发一遍还是同一个答案，于是第一次失败就直接代打，不白烧你的额度
> （牌桌上那句话会说清这一手是「没有重试」还是「重试 2 次仍无结果」）。

**出处**：`web/src/agent/retry.ts` 的 `NAMED_4XX`（401「这个端点不认这把 key」、400/422「这份请求端点不接受」、
404「这个模型名或这个端点地址不存在」）与 `loop.ts` 的 `if (!retry.worth) break`；
两句收尾话逐字来自 `loop.ts` 的 `gaveUpBecause`，本次 CI 第十三道打出来的真页面就是这句：
「（没有重试：这个端点不认这把 key，再问一遍还是一样）」。
**「每手最多问 3 次」没动，因为它仍然对**：`Agent.fs:306` 的 `retryLimit = 2`，
`loop.ts` 的 `rounds = retry_limit + 1`。

### 1.4 坏 key 演习的数字（一局 / 一整场各写清）

**原文**

> 所以模型再怎么坏，对局都打得完——拿一把作废的 key 实测过一局：
> **60 次请求全部 4xx/5xx，20 手由引擎代打，一局照样打到终局**。

**改成**

> 所以模型再怎么坏，对局都打得完——拿一把作废的 key 实测过**一局**：那一席被问到 20 次、
> 每手只问一次，**20 次请求全部 4xx/5xx、20 手全由引擎代打，一局照样打到终局**；
> 同一场演习打完**一整场**东风战，是 85 次请求、85 手全代打，一样打得完。

**出处（一局那一组：本次亲手复跑，不需要真 key）**

```
$ cd /home/xerxes2/janpo-ws-a/web
$ JANPO_CHROME=/usr/bin/google-chrome-stable node scripts/verify-llm-seat.mjs --bad-key
模式：断电演习（坏 key）　座位 1　模型 deepseek-v4-flash　脚手架 bare
一局打完，用时 9.8 s
  座位 1 兜底代打：provider 报错：401: {"message":"Authentication Fails, Your api key: ****-key is invalid",…}
  （没有重试：这个端点不认这把 key，再问一遍还是一样）　这一桌已兜底 20 手
兜底代打：20 手
provider 请求：20 次，其中 4xx/5xx 20 次
浏览器资源错误（请求失败）：20 条
人工验收通过 ✓
```

种子是脚本默认的 **2088**、座位 1、裸奔档、key 是脚本内置的 `sk-deliberately-broken-key`
（**不读 `/tmp/deepseek_key`，一分钱不花**）。**60 → 20 正是票 47 那条判据的直接后果**：
每手三问变成每手一问，而**兜底手数一手没变**（20 → 20），这也是 47 报告 §3 那张表的形状。

**出处（一整场那一组）**：`reports/47-classify-provider-errors.md` §3 的前后对照表与它下面的实跑输出
（同一把坏 key、种子 2088、座位 1、8× 倍速：provider 请求 **255 → 85**、兜底 **85 手**、4 局、
事件流逐条相同）。README 里**只留改后的 85**，255 那个旧数没进用户向正文。

## 2. `docs/development.md`：逐条

| # | 原文 | 改成 | 出处 |
|---|---|---|---|
| 1 | `./scripts/ci-web.sh  # 只跑 JS 侧：Biome + tsc + Agent 层用例 + Fable + Vite + 浏览器内三道` | `# 只跑 JS 侧（Biome、tsc、Agent 层用例、prompt 语义不变量、Fable、Vite，再加浏览器内那几道）` | `scripts/ci-web.sh` 现在的调用顺序：多出票 41 的 `verify-invariants.mjs`；浏览器内是 **七**道不是三道（tracer / board / golden / export / export --to-end / poison / redaction）。**道数不写死**（它每加一道就漂一次），照票面「要写就写怎么自己查」 |
| 2 | `dotnet build janpo.slnx  # 构建五个工程` | `# 构建解决方案里的全部工程` | `janpo.slnx` 现在列着 **6** 个工程（`src/` 四个 + `tests/` 两个）。同样不写死数字 |
| 3 | `scripts/ci-web.sh  JS 侧的那八道，被 ci.sh 调，也能单跑` | `JS 侧的关卡（道数与清单看脚本头部注释），被 ci.sh 调，也能单跑` | 同 1：脚本头部注释自己写着「十三道」，那里是真源 |
| 4 | CLI 那节只列了子命令名，没提任何开关 | 补一句「子命令的开关以 `janpo --help` 为准（那份帮助文本就长在 `src/Janpo.Cli/Program.fs` 里）」，并点名对局长度 `--hanchan` / `--tonpuusen`、自带选手 `--uniform` / `--covering` / `--opinionated`、`--no-akadora` | `src/Janpo.Cli/Program.fs` 的 `usage` 与三个 parser（`game` / `soak` / `decide` 各自认哪几个开关，见 usage 那几行） |
| 5 | 浏览器侧命令表里没有 `verify:invariants` / `verify:redaction` | 各补一行 | `web/package.json` 的 `scripts`（两条都在，且都是 CI 里跑的那一道） |

**没动的两个会漂的数字**：`pnpm run dev` 那行的「改 .fs 约 6s 后页面更新」（体感值，不是声称），
以及票 33 已经清过一轮的测试条数（现在正文里本来就没有）。

## 3. 核过、结论是「仍然属实」因而一个字没改的声称

逐条对着代码核的（**不实的才改，属实的连措辞都不动**）：

| 声称 | 核的什么 |
|---|---|
| provider 八家 + 自定义端点 | `Agent.fs` 的 `providers` 列表逐个对上，顺序也一样 |
| 「每手最多问 3 次」 | `Agent.fs:306` `retryLimit = 2` + `loop.ts` `rounds = retry_limit + 1` |
| 模型坐席「现在只支持一席」 | `Roster.withLlm` 只接一个 `seat`；面板上是单选 |
| 「工具搜索」一档灰着 | `TablePage.fs:1393` 的注释与 disabled 那一档 |
| 危险度四条规则、威胁只认立直或副露的家 | `src/Janpo.Engine/Danger.fs:184`（「判据只有现物、筋、壁与宝牌周边四条」）与 `Threat` 的定义 |
| 默认规则对齐天凤鳳凰卓 | `Ruleset.fs` 的默认值注释（双响、三家和了流局、连风雀头 4 符、岭上自摸符…都写着「天凤」） |
| WIP 告示里「裸奔档 4 局 91 手、信息辅助档 7 局 153 手、全程没有一手代打」 | `reports/27-m1-acceptance.md` §0（同一种子 2088、两档各一场、兜底 0 手 / 重试 0 次） |
| 牌谱里不含 key「有一道自动检查专门守着」 | CI 里其实是**三道**（`verify-export` 的断言、拌 key 的反向自证、回显 key 的假端点）——README 说「有一道」不算不实（它说的是有检查守着），本次没有改成数数字 |
| 「还差」那一串（四模型同桌、思考气泡、导入与链接分享、首页 demo、真人坐席、AI 基线） | `M2-SCHEDULE.md`「功能票」那节：四块**尚未切片**，一条都还没落地 |

## 4. 顺手发现的其它不实之处

**改了的**（都在这一票的两个文件里，已列在 §1 §2）：README 图注、三处 bot 说法、重试段、
坏 key 数字；development.md 的两处闸门道数、工程数、CLI 开关、两条漏掉的命令。

**发现了但没改的三处**（都在这一票禁改的文件里，逐条交代）：

1. **页面自己的介绍段仍是旧说法** —— `src/Janpo.Web/TablePage.fs:1610`：
   「默认四家随机选手；…**他家那几行看不到牌面**…」。两处旧：一是「那几行」（44 之后是四个方位上的面板），
   二是「四家随机选手」没提两种。README 的图注原本与它**逐字同一句**（票 33 定的稿），
   这次 README 改了、页面没改，两处**从此不再逐字相同**。
   **没改的理由**：改它要动 F#（这一票明写不改行为、不碰引擎与页面）。
   这正是 44 报告 §9 第 1 条留下的那笔账，**建议下一张碰 `TablePage.fs` 的票顺手带走**，
   措辞可直接取 README 现在这版。
2. **`scripts/ci-web.sh` 的 `JANPO_NO_BROWSER` 注释数错了道数** —— 它写「可以 `JANPO_NO_BROWSER=1`
   跳过**后六道**，前五道照跑」，而同一份脚本头部写着「十三道」，实际是**前六道**（Biome / tsc /
   Agent 层用例 / prompt 语义不变量 / Fable / Vite）+ **后七道**（浏览器内那七道）。
   **没改的理由**：它是脚本文件不是文档，而票 51 正在改 web 那一摊、随时可能加减闸门；
   这条注释的真源是它上面那份清单，谁加闸门谁顺手改一行最省。**一行的活，谁碰 `ci-web.sh` 谁带走。**
3. **`docs/images/table.png` 里那段页面帮助文字比代码旧** —— 图是票 44 拍的（早于 47），
   图里写着「重试两次仍不行就兜底代打」；而代码里那句**票 43 已经补上了**
   （`TablePage.fs:1432`：「…认证失败这类再问一遍还是一样的错不重试，直接兜底」）。
   **没重出图**（票面禁止，票 51 会重出），且 **README 正文没有一句依赖图里那段文字**，
   所以这条不会让读者被骗——51 重出之后自动对上。

**判过但认为不属于「不实」因而没动的一处**：README 第 2 步没提配置面板上的「人格」与
「prompt 模板」两个输入框（它们在截图里就看得见）。那是**不全**不是**不实**，
而票面把这一票的界线画在「只改说法与事实不符」，所以留给功能票或下一次 README 追平。

## 5. 验证记录

| 验的什么 | 命令 | 结果 |
|---|---|---|
| 全套闸门（文档改动也要过） | `./scripts/ci.sh` | **EXIT=0**，dotnet 侧 + JS 侧十三道全绿 |
| 坏 key 演习（一局，取新数） | `cd web && node scripts/verify-llm-seat.mjs --bad-key` | 20 次请求全 4xx/5xx、20 手全代打、一局打完，见 §1.4 |
| alt 里那个手数 | `cd web && node scripts/shoot-table.mjs --scan 1 --seed 1177 --turns 52` | 「走了 52 手」，**不写图** |
| 当前那张图长什么样 | 直接看 `docs/images/table.png` | 三×三真牌桌，桌心场况，四家方位与图注逐句对得上 |
| 用户向语感 | `grep -nEi "nix\|dotnet\|pnpm\|fable\|票 [0-9]\|M1\|M2\|M3" README.md` | 无输出 |
| 改动边界 | `jj st` | 只有 `README.md`、`docs/development.md` |

## 6. code-review —— Spec 一轴（fixed point `aa83bc01`）

按票面只跑 Spec 轴（对象是「文档说的与代码做的是否一致」），Standards 轴按票面免跑。
派不出 sub-agent，自己顺序跑的；diff 用 `jj diff --from aa83bc01`（**全程 jj，没跑过 git**）。

**(a) 票面要的、缺了或只做一半的：0 条。** 五条逐条落地（§1.1 §1.2 §1.3 §1.4 §2），
纪律四条逐条落地（数字有出处、用户向措辞、零行为改动、别的不实一并列出）。

**(b) 没要求却做了的（scope creep 自查）：两处，都在票面允许的「顺手核别的不实之处」之内。**

1. `docs/development.md` 补了 `pnpm run verify:invariants` / `verify:redaction` 两行 ——
   命令表列了别的 `verify:*` 却漏了这两条，读者会以为 CI 只跑那三道；改的是「说法与事实不符」。
2. `dotnet build janpo.slnx  # 构建五个工程` → 「全部工程」—— 票面点名的是「CLI 新开关与 CI 闸门道数」，
   这条是同一段里挨着的另一个过期数字，一并收掉比留着便宜。

**(c) 看着做了、但做得不对的：0 条。** 逐句回核过的清单在 §3；新写进文档的每个数字
在 §1 §2 都点了出处（本次实跑的写了命令与种子，引用的写了报告的哪一节）。

**留给人的两条**（都在 §4）：`TablePage.fs:1610` 那句页面文案与 `ci-web.sh` 的道数注释，
各自要等一张碰得着那个文件的票；另有一条**提醒**：票 51 重出 `docs/images/table.png` 之后，
图注里「副露在身侧」那半句值得再核一次（其余各句与副露画法无关）。
