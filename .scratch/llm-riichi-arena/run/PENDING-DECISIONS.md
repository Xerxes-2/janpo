# 待你裁的（M3 十六票 + 上线之后的全部积压，2026-08-19 汇总）

调度器整理。来源是票 87 / 89 / 90 / 92 / 93 / 101 / 102 报告里的「留给人的待审项」逐条，
外加我集成时自己撞见的几处。**每条都写了我的判断**——你只需要在我判错的地方说一声。

## 零、已经不用裁了（先划掉，免得账面重复）

| 原挂在 | 那件事 | 现状 |
|---|---|---|
| 92-1 / 93-3 | 上线那条 Pages workflow 没做 | **票 101 做完了**，站点 200 |
| 93-4 | 「它为什么那么打」永远没有 → 建议开票 | **票 103 已派**（候选分布带过界） |
| 101-3 | Pages 对 6 MB 的 wasm 压不压 | **我自己 curl 核了：压。`content-encoding: gzip`、传输 4,793,432 字节** ⇒ 票 91 那句「传输 4.8 MB」在生产上成立 |
| 102-1 / 102-3 | 那几句文案、风险措辞的软硬 | **你说了「感觉不错」**，按通过记 |
| 87-nitpick-1 | `danger` 开关在真人对局里该不该默认关 | **票 89 解了**：Bare 档的真人在座时，那一块与那一枚开关都不在 DOM 里 |

## 一、术语表收词（四票各留了提案，本质是同一个决定）

四票落地之后，`CONTEXT.md` 里有 **10 条已经有执行者的不变量还没被收进词条**。
agent 一律没写——「改术语表要单票授权」是硬约束。逐条：

- **`Human Seat`**（票 87，提案在 `DECISIONS.md` 87-1）：三条
- **`ScaffoldTier`**（票 89，89-3）：①Bare 档真人在座时危险度那一块与开关都不在页面上
  （执行者 `TableState.assists`）②ToolSearch 在真人侧按 Assisted 处理（执行者 `HumanScaffold.shows`）
- **`复盘`**（票 90，90-3，**今天连词条都没有**）：①只在终局之后出现 ②每个数都是引擎已会算的量、
  没有总分 ③对着一席，上帝视角没有主语
- **`强 AI 基线的对照标注`**（票 93，93-3）：①喂给它的必须是那一手当时喂给该席的那份投影
  ②它只给一个动作，不给理由、不给分数

**我的判断：该收，而且值得单开一票**（不是我顺手写）。理由：这 10 条每条都有指得出的执行者
（函数 + 闸门），收词的动作本身要**逐条去核那个执行者还在不在**——那是一趟真活，不是抄写。
⇒ **要你的第十一次术语授权。**

## 二、产品口味（我判不了，或我判了但你该知道）

| # | 那件事 | 我的判断 |
|---|---|---|
| 89-2 | 思考时限那一格没有下限，填 `1` 就是一秒一手 | 我倾向**不加下限**：主持人自己的事（同 `TimeoutMs`）。但「误填 1 让整局自动打完」这个坑很浅——若你要，加个 ≥3 秒 |
| 89-3 | 到点代打**恒走 Bare 兜底**：Assisted 档的人超时会拿到一手摸切，比模型席的兜底更笨 | agent 的理由（那一手不是他打的，平台不该替他用一遍辅助）我认。**但这条你该知道**，因为它决定超时那一手好不好看 |
| 90-2 | 「更好的候选」判据是**帕累托占优**：有效牌更多但更危险的那张**不列** | agent 说「加权就是『这一手打得几分』的开头」——**这句我同意**。改成「有效牌优先、危险度当附注」只动 `Review.dominates` 一处 |
| 90-3 | 一整场一百多条排成一列，要不要「只看有更好候选的那几手」 | 该做，但要往 `TableModel` 加一格 ⇒ 单独一票，与 93-2 合并划算 |
| 93-2 | 时间轴滑块上要不要标出分歧那几手 | 同上，与 90-3 是同一块地（`TablePanel`），**合成一票**：复盘的导航 |
| 92-2 | **摘掉三麻权重**（传输量 −42%） | 它会触发 Apache-2.0 §4(b)（改了上游文件就要标注修改）。**我倾向不摘**：省 2 MB 换一条要长期维护的修改声明，不值 |

## 三、技术债（我打算直接立票，不用你裁，列出来备你否）

1. **`ci.sh` 里没有许可闸门**（101-1）：今天谁删掉 `web/public/third-party/` 都不会红，
   只有 Pages 那条路上的 `check-pages-dist.sh` 会。**这是 Apache-2.0 §4 的义务挂在一条不常跑的路上。**
   ⇒ 我立票，往 `ci-web.sh` 加一道。
2. **闸门趟数写在好几处，一直在漂**（87-nitpick-3 + 我自己撞的）：
   `verify-setup.mjs` 文件头还写着「十一趟」（现在十八趟）；**我这一批集成时被那句总述撞红过三次**
   （92 加一趟、90 加一趟、89 加一趟，每次两侧都改同一行）。⇒ 我立票：**趟数只许有一处真源**。
3. **产物路径写在五处**（101-2）：TS / YAML / bash 三种文件。⇒ 并进上面那一票（都是「同一事实写多处」）。
4. **模型席那条「过期问话」**（92-3 / 101-5）：同族问题，两票都没动。⇒ 我立票，但排在对照实验之后。
5. **逐字可重现**（101-4）：同一份源码在不同目录造出的 wasm 差几百字节
   （我上线核对时实测 960 字节）。⇒ 记在案，**不立票**：它今天没有害处，
   而票 101 已经把「别拿本机那份比 sha256」写进了报告与 Summary。

## 四、你说要自己搞的

- **三档脚手架的对照实验**（裸奔 / 信息辅助 / 工具搜索，外加强 AI 当锚）——你说等有空。
  我把它记在这儿，并提醒一件：**票 79 的语料与今天不可比了**（票 95 改过 prompt 记法与前缀，
  `render_version` 已经变过两次）。要对照就得重新跑一遍基线，别拿 79 的数当对照组。
