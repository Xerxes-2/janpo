# M6 排班 — 把「打完了」变成「有结果」

**来源**：2026-08-30 那份外部设计评审（原文存档在 `reports/design-review-2026-08-30.md`）。
主人的指示是**先开票、不写代码**，∴ 这一份只有票与波次，没有派工。

**票号从 133 起**：仓库里的票文件停在 119，而 120–132 是主人当场裁的、没落成文件
（见 `git log`）。∴ 133 是第一个安全的号（票号只由调度器分配，判据见 `dispatch.md`）。

## 票

| 票 | 状态 | 阻塞于 | 交付 |
|---|---|---|---|
| 133 | ready-for-agent | — | **终局记分卡**：四行一表 + 复制成纯文本 |
| 134 | ready-for-agent | 133 | **记分卡出图**：一张能贴出去的 PNG |
| 135 | ready-for-agent | 133 | **本机累计战绩**：主人准许新前缀 `janpo.record.` |
| 136 | ready-for-agent | — | **对局中的右轨**：刚落子那一席的推理全文 + 三个元数字 |
| 137 | ready-for-agent | — | **首页时间轴的记号与落点** |
| 138 | ready-for-agent | — | **一行式开桌** + key 去向就地说明 |
| 139 | ready-for-agent | — | **两档牌宽**：所看那一席大一档 |
| 140 | needs-triage | 133 | 双回放并排——主人裁：**等 133 落地再切**，不进 M6 波次 |
| 141 | ready-for-agent | — | 一屏英文说明 |
| 142 | ready-for-agent | — | 手机竖屏**只留一句「横过来看」**（「读」的版式挂账不开号） |

## 波次（按文件算地盘，不按模块名算）

先 `grep` 过各票要动的文件，交叉如下：

| 波 | 并行 | 为什么不撞 |
|---|---|---|
| W1 | **133 ∥ 138 ∥ 139** | 133 动 `Janpo.Engine/Paifu.fs` + `TableBoard.fs` 的终局那一段；138 动 `TablePanel.fs`；139 几乎只动 `styles.css`。三处不交叉 |
| W2 | **136 ∥ 137 ∥ 141** | 136 动 `TablePage.layout` 的右轨那一格 + `TableBoard.statusRail`；137 动 `Review.fs` + `TablePanel.ops` 的时间轴；141 动 `Route.fs` 与一页新文件。**136 与 137 都会碰 `ReviewPanel.fs`** ⇒ 票面里写死「改动尽量集中，方便 rebase」，或把 137 挪到 W3 |
| W3 | **134** | 阻塞于 133 |
| —  | 135 / 140 / 142 | 等主人裁 |

**上限四路**（本机 32 核 / 96 G），W1 三路、W2 三路都在预算内。

## 那三条已经裁了（2026-08-30，主人）

1. **票 135 的新 localStorage 前缀 `janpo.record.`：准许。** 授权只到这一个前缀，
   既有那四个（`janpo.llm.` / `janpo.rules.` / `janpo.profiles.` / `janpo.seats.`）仍旧一个字不动。
2. **票 140：可以等落地再动。** 133 落地、记分卡形状定死之后再切，不进 M6 波次。
3. **票 142：同意只做最小的那一半**——竖屏留一句「横过来看」；
   「读」的那一版式**挂账、不开号**，等真有手机来客的证据再说。

∴ **M6 这一波没有待裁项了**，十票里九票可派，140 挂着等 133。

## 顺带记一笔（评审逮到的，不在上面十票里）

README 的截图比代码落后四票（`docs/images/` 一次都没回流过），
且 README 写的「390 px 一张牌 11 px」在票 118 之后是 9.0 px——**而今天固定舞台已拆，这两个数都得重量**。
**这不该并进上面任何一票**（判据：边界该由调度器松，不该让 agent 自己扩）。
它是独立的一件事，等这一波落完再开一张「门面对账」的票。

## W1 派工记录（2026-08-30）

集成头 `46d18444`（change `uuwyypzq`），三个工作区都已 `jj new` 对齐到它。
**票文件已在基点上**（`jj file list -r uuwyypzq | grep issues/13[389]` 三条齐）。

| 票 | 工作区 | 状态 |
|---|---|---|
| 133 终局记分卡 | `../janpo-ws-a` | 派工中 |
| 138 一行式开桌 | `../janpo-ws-b` | 派工中 |
| 139 两档牌宽 | `../janpo-ws-c` | 派工中 |

**地盘划分（写进了三份派工单，防撞车）**：

- `src/Janpo.Engine/Paifu.fs` + `TableBoard.fs` 终局那一段 → 只有 133
- `src/Janpo.Web/TablePanel.fs` → 只有 138
- `web/src/styles.css` 的 `:root` 与牌那几段 → 只有 139；133/138 若要加样式，一律新起一段放文件末尾（写明「改动尽量集中，方便 rebase」）

**三份派工单里各写了这一票特有的坑**：133 是「选手·档那一列回放屏没有来源」与 voided ask 进不进 tok；138 是「配桌今天是顶栏不是抽屉」与「key 不许出现第三个输入框」；139 是 M5 那张假绿表的四种形状 + 「一个像素数都别信票面，自己先量」。

## 从这里接

- W1 三票回来后**逐票集成**（顺序按 `dispatch.md` 第四节，别改）：rebase → 看冲突 → `resolve-append-conflicts.py` 处理 `DECISIONS.md` 的双向追加 → `ci.sh` 全绿 → commit。
- **139 是视觉票，集成时必须把图打开看**（票 22 的事故：只读报告放行，漏掉牌背隐形两周）。
- **语义撞车比文本冲突危险**：133 与 139 都会改浏览器闸门里量牌的那几条断言，各自绿、合流可能红。`ci.sh` 是唯一裁判。
- W1 全绿之后派 W2 = 136 ∥ 137 ∥ 141（136/137 都碰 `ReviewPanel.fs`，撞了就把 137 挪到 W3）。
