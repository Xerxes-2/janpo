# 调度器裁决与备注

与 `DECISIONS.md` 分开的理由很实际：agent 与调度器都往 `DECISIONS.md` 尾部追加，
每次都在同一处冲突。本文件**只有调度器写**，agent 不碰。

## 裁决 D-1：标识符用术语表拼法，wire 用 mjai 原拼

04 票把事件 case 命名为 `Event.Ryuukyoku`，而 wire 是 `"ryukyoku"`（mjai 的实际拼法，
少一个 u），并把这个出入标为待审——因为我在派工时写的是「case 名 = mjai 事件名转 PascalCase」。

**我派工的措辞不对，04 的做法对。** ADR-0001 已经定了：标识符遵循术语表的罗马字拼法。
`Ryuukyoku` 是 `CONTEXT.md` 里的词，wire 是外部协议的既有事实，两者本就该分离——
这正是 ADR-0001「人类可读形式隔离在渲染层」的同一条原则，只是这次隔离的是**协议层**。

规则定死，05-14 票不必再议：**F# 标识符用 `CONTEXT.md` 的拼法；JSON wire 用 mjai 原拼；
两者不一致时在编解码处一行映射，不改标识符去迁就 wire。**

## 裁决 D-2：把 06 与 07 并行，偏离睡前的波次表

睡前定的是 `{04,07}` 并行、`06` 串行。实际情况：04 先落地，07 仍在跑，而 06 只阻塞于 04。

让 06 干等 07 是白等一整波。06 改的是 step 机制（和了成立、振听、头跳），07 是纯役种函数，
**两者的重叠面与刚刚成功集成的 `{04,07}` 同构**（只在 fsproj 与 Program.fs 的列表上撞）。
且 06 票明确不算点数与役——它不消费 07 的任何东西。

所以 06 派进 ws-a（04 腾出来的），与 ws-b 的 07 并行。串行纪律仍然守在真正互相踩的那批上：
`09/10/11/12` 全部改同一块响应阶段与动作集，那里不并行。

## 备注 N-1：04 交出的动作集扩展契约（05-12 票都要照它）

加一个动作固定改三处：① `Action` 的 case；② 产出动作集的函数——摸牌后的填
`awaitingDahaiActions`（06 自摸和 / 09 立直 / 11 暗杠），他家打牌后的填 `responsesTo`
（06 Ron / 10 Pon·Chi / 11 大明杠，现恒返 `[]`）；③ `step` 的执行 match。漏哪处编译器都报。

**04 留下的关键警告**：06 与 10 谁先填 `responsesTo`，就必须同时加一条「过」（mjai `none`），
否则响应阶段会停在那里没人推得走——这是个能让 soak 死锁的坑，写在这里免得两票互相以为对方会加。

## 备注 N-2：05 与 06 的票范围重叠（我切票时的失误）

07 落地后，理论上的 frontier 是 `05`（只阻塞于 04）与 `10`（只阻塞于 04）。两者我都**没有**派：

- `10` 要往 `responsesTo` 里填 Pon/Chi，而 06 此刻正在往同一处填 Ron 与「过」——这正是裁决 D-2
  说好不并行的那批。
- `05` 的问题更微妙，是**我切票时的失误**：05 票写了「无人和了时按规则决定进局或连庄」，
  而 06 票也写了「和了产出 Hora 事件并正确进局或连庄」。**进局/连庄 这块逻辑被两张票同时声明了。**
  并行会让两个 agent 各写一份，且 05 只能看到 Ryuukyoku 一种终局，写出的转移逻辑接不住 Hora。

所以 07 之后没有可派的票，等 06。这不是 DAG 画错（05 确实只依赖 04 的 `GameState`），
而是**票的边界没切干净**——DAG 只能表达「依赖」，表达不了「两票会写同一段逻辑」。

早上若要复盘切票质量，这是最值得看的一条：`to-tickets` 产出的阻塞边是必要条件，不是充分条件。

## 裁决 D-3：`NotAgari` 名字碰撞——整晚第一次集成失败

06 与 07 在各自工作区都全绿，合起来编译失败 2 处。原因：

- 06 定义 `IllegalAction.NotAgari of Seat * Tile`（宣言了和了但手牌不成型）
- 07 定义 `YakuError.NotAgari`（牌姿不成和了型）

两个 DU 都没有 `[<RequireQualifiedAccess>]`，命名空间里同名。src 里的 4 处**靠「就近定义优先」
侥幸编译通过**（各自文件里都先定义了自己那个），测试文件里的 2 处则解析到了另一个类型。

**处置**：把 `NotAgari` 的全部 8 处使用点显式限定（`IllegalAction.NotAgari` / `YakuError.NotAgari`），
不改名、不加 `RequireQualifiedAccess`、零语义变化。只修报错的 2 行是不够的——src 里那 4 处
依赖的解析顺序会被「将来在两者之间插入一个新文件」改变，而这正是刚刚发生的事。

**留给早上**：两个层各有一个叫 `NotAgari` 的 case，表达的是同一个事实（这手牌不成和了型）
在不同层的投影。限定之后不再有编译歧义，但读代码的人仍要停一下。是否统一命名（如
`YakuError.ShapeIncomplete`）值得一裁——这是 `CONTEXT.md` 该管的事，我不擅动。

**这条是并行开发的固有成本，不是谁的失误**：两个 agent 都看不到对方的类型名，
编译器也只在合并那一刻才有全部信息。

## 裁决 D-4：08 与 05 并行，但给 05 加一条测试纪律

剩下 8 票越来越互相纠缠，纯串行大概要 4-5 小时，赶不完；乱并行会像 D-3 一样出事。折中：

`{08 符与点数, 05 Kyoku 循环}` 并行，然后 `09 → 10 → 11 → 12 → 13 → 14` 严格串行。

08 与 05 的真实风险是**语义的**：06 把和了点数记 0，08 要把它填成真值；若 05 同时把
「跨局点数」写成字面量断言，08 一落地 05 的用例就红。所以给 05 的硬约束是：

> **点数的具体数值不许进黄金用例的断言**，用不变量表达（四家点数与 Kyotaku 之和恒定、
> 连庄与 Honba 的变化、Kyotaku 的结转与归属）。理由写在派工里：08 正在把和了点数从 0 改成真值。

这不是「希望它们不打架」，是让 05 的测试对 08 的改动**天然免疫**。

## 裁决 D-5：09 与 10 换序——避开 Kyotaku 这个热点

原计划 08 之后串行跑 09（立直）。但 05 仍在跑，而 **09 与 05 都动 Kyotaku**：
09 要把立直棒计入 Kyotaku，05 要让 Kyotaku 跨 Kyoku 结转并在终局归属。
08 刚落地时又留了一句：「供托进和了者的 Deltas，**05 只需和了后把 Kyotaku 归零，别再补一次立直棒**」。

三张票都摸 Kyotaku，这是典型的热点。09 与 05 并行是自找的语义冲突。

**改为**：先派 10（Pon/Chi）——它只动响应阶段与副露，**完全不碰 Kyotaku**，与 05 的重叠面比 09 小得多；
且 10 落地后 11（三种杠）立刻解锁（11 需要 10 + 08，08 已好）。

新的串行尾：`10（现在，与 05 并行）→ 09 → 11 → 12 → 13 → 14`。
09/10 之间的串行纪律不变——它们填的是同一处动作集。

## 备注 N-3：Kyotaku 的三方交汇，集成 05 时必须验

- 06：和了时的 Kyotaku 处理
- 08：供托进和了者的 `Deltas`（一次和了的增减之和 = 供托点数）
- 05：跨 Kyoku 结转与终局归属
- 09（未做）：立直棒计入

**05 在 ws-b 里看不到 08 的改动**（它的基线上和了点数还是 0），所以它无法自己发现「立直棒被补了两次」
这类错误。集成 05 时**必须专门验一次**「四家点数与 Kyotaku 之和恒定」这条属性在有真实和了点数后仍成立。
这正是 D-4 要求 05 用不变量而非字面量写测试的原因——那条不变量就是这次集成的探针。

## 裁决 D-6：`RiichiBou` 与 `RiichiStick` 是同一字段的两个名字

05 与 08 并行，各自往 `Ruleset` 加了「一根立直棒的点数」：05 叫 `RiichiBou`，08 叫 `RiichiStick`，
两处文档注释说的是同一件事（供托记根数，换算成点数要乘它），默认值都是 1000。

与 S-C（`TileKindSet` vs `Ruleset.TileKinds`）不同——那是**两种表示**，这是**纯重名**，必须合。

**按 ADR-0001 统一到 `RiichiBou`**：标识符用罗马字日麻术语，`RiichiStick` 是英文。
08 自己也把它的两个英汉混拼字段名标成了提案 08-A 待裁，方向一致。删掉 `RiichiStick`，
改掉 08 的 4 个测试文件共 10 处（我第一遍只改了 src，测试工程漏了，CI 立刻报 10 个错——
**改名要连测试一起 grep**）。

## 备注 N-4：这次集成里编译器替我们抓了一个真 bug

05 给 `Event` 加了 `EndKyoku`/`EndGame`，08 在 `GameState` 里新增了一处对 `Event` 的 match
（判天和/地和的「这家还没打过牌」）。两个工作区各自全绿，合起来 F# 立刻报模式匹配不完整。

修法是补两个 case 而**不是加通配符 `| _ ->`**。理由：正是这份显式列表让编译器抓到了它。
若当初写成通配符，`EndKyoku` 会被静默当成「不影响首巡判断」——本例中恰好也对，
但下一次加的 case 就不一定了，而那时没人会知道这里需要复查。

这条印证了我夜里对三麻 bonus 的判断（提案 S-B）：**F# DU 加 case 时编译器把所有 match 点找出来
是优点，不该为「将来可能加 case」提前抽象**。这次它就是这么帮我们的。

## 裁决 D-7：用这段空转做 13 票的数据前置

10 仍在跑，而 09（立直）与它填同一处动作集，按 D-2 不并行；11/12/13/14 都还阻塞着。
所以这一波无票可派——但 13 票（真实牌谱对拍）是我睡前标为最可能 park 的那张，它的风险**全在数据侧**。

派一个**只碰数据、不碰代码**的前置任务：取一小批天凤鳳凰卓的 mjai 格式牌谱，
统计其中出现的事件类型与字段，与我们 `Event` DU 已有的 case 做差集，产出一份缺口清单。
零碰撞面（不动 fsproj、不动引擎、不加依赖），却能让 13 票开工时就知道要补哪些字段——
甚至能提前告诉 09/11 票它们该产出什么形状的事件。

## 备注 N-5：D-7 的前置任务回报远超预期，并推翻了它自己的一条结论

前置任务（只碰数据）产出的缺口清单让我改了 4 张票（09/11/12/13）。最有价值的三条：

**一、上游 mjai 牌谱是瘦身版。** `hora` 只有 actor/target/deltas/ura_markers，`ryukyoku` 只有 deltas——
**役、符、番、点数、流局形态全都没有**。也就是说单靠这个数据源，13 票的核心验收（对拍役种与符点）
根本做不了。前置任务自己发现了这点，并额外接了天凤官方 JSON（`mjlog2json.cgi`）作 oracle，
按 `(bakaze,kyoku,honba,kyotaku)`+deltas 对齐，177 局零误差。**这是它最大的贡献**：
如果 13 票开工才发现，那一票几乎必然 park。

**二、字牌记法是 `E/S/W/N/P/F/C`。** 这正是 domain-modeling 那晚被 ADR-0001 明确列为「避免」的写法，
而真实数据就是这么写的。它的建议（映射放 13 的适配器、不放宽 `Tile.parse`）**正是 ADR-0001 的原意**：
内部单一规范形，边界处适配。这不是 ADR 被现实推翻，是 ADR 被现实印证。

**三、我否掉了它的 A4。** 它把「没有脚本化牌山入口」列为 13 票最大工作量，但引擎里
`Wall.ofOrdered` + `GameState.startFrom` 早就有了（06 的测试就是用它摊牌山的，
`GameStateGenerators.scriptedWall` 是现成样例）。已在 13 票里改成「用已有的，不要新造」。
前置任务只读了 `Event.fs`（我给它的禁令让它没去翻别的源码），看不到这个——**这是我给的约束造成的盲区，
不是它的失误**。

另外它报的「13 的 blocked-by 漏了 10 票」不成立：13 阻塞于 11，而 11 阻塞于 10，**传递满足**。
已在票里写明以免下一个 agent 再疑。

## 备注 N-6：样本实证了提案 S-A 的两条

样本全部是天凤鳳凰卓 `鳳南喰赤`，实测：
- **双响成立**（3 次 `hora`→`hora` 相邻）
- **无切上满贯**（`30符4飜7700点` × 30、`60符3飜7700点` × 1）

这是对提案 S-A 的经验验证——不再是「文档说天凤如此」，而是「牌谱里就是这样」。
早上裁 Atamahane 默认值时可以拿这条当证据：**默认值若不翻成双响，13 票的对拍必须显式关掉头跳**，
而那意味着「我们的默认配置跑不出真实牌谱」。

## 备注 N-7：`Event` 加 case 的漏 match 第二轮，以及为什么这不算问题

集成 10 时又出现 3 处模式不完整（`GameState.junme`、`GameTests` 的 startKyokus 计数、`KyokuTests.isDahai`），
原因同 N-4：并行的两票各自加 case、各自新增 match，谁都看不见对方。

**但这四次（N-4 的 1 处 + 本次 3 处）没有一次是 bug 泄漏，全部是编译器在集成那一刻当面指出。**
每次的修复都是「补 case → 绿」，单次成本一两分钟。对比 D-3 的 `NotAgari` 重名——那次也是编译器抓的。

值得记下的对照：今晚**所有**跨票的类型级冲突都被编译器抓住了，
而**唯一**需要我动脑判断的冲突（D-6 的 `RiichiBou`/`RiichiStick` 重名、S-C 的 `TileKindSet` 二重身）
都是**编译器抓不到的语义重复**——两个名字各自合法，只是说的是同一件事。

这条给早上的启示：无人值守并行的真实风险不在「编译不过」，而在「编译得过但概念重复」。
前者自动暴露，后者只有集成者逐个看类型名才能发现。

## 裁决 D-8：预先切开 09 与 11 的重叠（N-2 的同类问题，这次在开工前处理）

09 票写了「立直后只能摸切；**暗杠的例外条件**按规则清单处理」，
11 票写了「Ankan/Minkan/Kakan 的合法动作集正确，**含立直后暗杠的条件**」。
又是同一段逻辑被两票声明——与 N-2（05/06 的连庄）一模一样，但这次我在派工前就发现了。

**切法**：暗杠这个**动作**本身完全属于 11；09 只负责产出一个**判据**（「此刻这家立直后是否允许暗杠」），
做成可被 11 调用的函数，**不许自己往动作集里加暗杠**。09 的验收里那条改成「判据存在且有单元测试」。

这样 11 落地时只需调用它，两票零重叠。N-2 那次我是事后收敛，这次是事前切开——后者便宜得多。

## 备注 N-8：随机 Player 在 4 场对局里和了 0 次、立直 0 次

09 落地后我量了一下四个种子的随机对局事件频次：

```
seed 1:     309 dahai  280 tsumo  20 chi  9 pon  4 ryukyoku  0 hora  0 reach
seed 42:    300 dahai  280 tsumo  12 chi  8 pon  4 ryukyoku  0 hora  0 reach
seed 777:   313 dahai  280 tsumo  26 chi  7 pon  4 ryukyoku  0 hora  0 reach
seed 31337: 310 dahai  280 tsumo  22 chi  8 pon  4 ryukyoku  0 hora  0 reach
```

**每一局都是荒牌流局。** 鸣牌有（随机取样时 Pon/Chi 在动作集里占比不低），但**和了与立直一次都没有**——
均匀取样下，一个随机 Player 几乎不可能走到和牌，也很少正好在能立直的那一手挑中立直。

后果很具体：**14 票的 soak 若沿用现在的 `randomPlayer`，和了、点数授受、立直、一发、里宝牌
这些路径在 soak 里全是零覆盖**，而 14 票的验收却写着「随机 Player 覆盖全部动作类型」。
那句话会变成一个口号——CI 绿，但绿的部分不含引擎里最容易错的那一半。

**已改 14 票**：要求 Player 加偏好（有 Hora 就和、Tenpai 时以一定概率立直、以一定概率鸣牌与杠），
并要求 soak **输出各事件类型的频次作为覆盖率证据，且断言每种动作类型至少出现过一次**。

这条也说明「跑 N 局无异常」这种 soak 验收有多容易自欺：**没断言覆盖率的 soak，只证明了没崩，
没证明跑到过。**

## 晨间裁决 R-1..R-6（用户已裁）与 R-3 的中途发现

用户对夜里积压的 6 条提案全部给了裁决：

| # | 裁决 | 文档 | 代码 |
|---|---|---|---|
| R-1 | Atamahane 默认值对齐天凤/雀魂（双响成立）；`Ruleset` 进术语表；立 ADR | ✅ ADR-0004 + CONTEXT.md | 待 11 后翻默认值 |
| R-2 | 立直棒时机对齐天凤 → 09 的实现本就对 | ✅ 写进 ADR-0004 | 无需改 |
| R-3 | 三条听牌边界「需要深入研究」 | 🔬 研究 agent 在跑 | 视结论 |
| R-4 | `Ruleset` 该携带牌种全集（原 S-C） | ✅ ADR-0004 决定 4 | 待 11 后合并 |
| R-5 | `Seat = int` 透明别名不可接受 | — | 待 11 后换类型（宽改动） |
| R-6 | 两层 `NotAgari` 改名 | — | `YakuError.NotAgari` → `NoAgariShape` + 该类型加 `RequireQualifiedAccess` |

### R-3 的中途发现：担心的方向是错的，问题比想象的窄

我夜里给研究 agent 的核心问题是「向听判定是否把牌的剩余枚数混了进来」。查签名即可否掉：

```
Shanten.calculate (kindSet) (hand)                 ← 无「可见牌」入参
Ukeire.calculate  (kindSet) (visible) (hand)       ← 只有 Ukeire 看可见牌
PlayerState.isTenpai state.KindSet                 ← 04 的听牌料，同样不看
```

**听牌判定看不见别人的牌**，`kindSet` 只表示「此规则集存在哪些牌种」（四麻 34 / 三麻缺 2-8m）。
所以「别人把和了牌打光」的纯空听在我们引擎里照样是听牌、照样收听牌料。

用户补充了一条经验证据：**雀魂段位场里纯空听的形式听牌是算的**（收听牌料）。与我们实现的行为一致。

于是 03 那三条改动只可能是**手牌自身的四枚上限**（握满 4 张就摸不到第 5 张），
这是向听传统定义本来就含的，且 oracle 在 100 万手上同意。**「需人确认」的范围因此从
「听牌的定义变了」缩小到「四枚上限的边界情形算对了没有」**——研究 agent 仍要给出每条的精确
判据与出处，并用固件验证天凤的形式听牌授受（这条对 12 票流局形态与 13 票对拍仍有用）。

**教训**：我夜里给研究任务写的「核心问题」建立在没读签名的猜测上。读一行签名就能省掉半个研究方向。

## 备注 N-9：资源可见性的事故记录（看门狗已按用户要求撤除）

**事故**：一个研究 agent 开了 16 个 Python worker 扫语料，load average 冲到 32，
把并行跑 dotnet 的另一个 agent 挤慢了 6 倍（CI 46s → 5m2s）。**唯一发现它的途径是用户听见风扇呼啸**
——这套跑批当时对资源零可见性。

**当时的处置**：写了个看门狗定期采样负载、把辅助工具进程自动 nice 19，日志写进 `RESOURCES.log`。

**现在已撤除**（用户要求，2026-08-16）：一个常驻写日志的后台进程本身就是不可预计的东西，
为了观测而引入一个需要被观测的东西不划算。**保留的是纪律，不是工具**——`RUNBOOK.md` 里那条
硬约束还在：不许 `Pool()` 不带参数、并行上限 4 进程、长跑 `nice -n 19` 起步、
全量跑之前先用 100 个文件估耗时。

真正的根治也不是看门狗，而是同一天定下的另一条：**验证引擎行为用 `dotnet fsi` 直调真引擎，
不许移植到别的语言**。那次 16 核占用的根因是移植，不是缺监控。

## R-3 结案：三条听牌边界改动全部正确，且我用真引擎复验过

研究报告 `docs/research/shanten-tenpai-boundaries.md`（提交 `moutlmyp`）。结论：

1. **面子受 4 组上限** — 正确，纯形态问题。撤掉会假报和了（反例 `2m2m2m 7m8m9m 4p 6p 1s1s1s 6s6s6s` 变 −1）
2. **孤张全是「四张全在手里」+ 无雀头 → +1** — 正确，**天凤取的正是这一边**，两例真实牌谱直接印证
3. **字牌死张下界** — 正确，且**根本不碰听牌边界**（40 万手 0 次翻转）。03 把它标为「需人确认」是误判

**04/06/09/12 一处都不用改。**

**纯空听在天凤算听牌**（用户凭雀魂经验的判断是对的，天凤同样）：官方手册「形式聴牌あり。
5枚目を待つ聴牌あり」，12,188 局实测 342 个纯空听座位 0 个被判不听，93,838 次立直里 13 次
待牌 0 枚仍被受理。已补进 `CONTEXT.md` 的 Tenpai 条目。

**我兑现了用真引擎复验的承诺**：`dotnet fsi` 直调编译好的 API 跑 5 个关键判例（fix1 的假和了反例、
fix2 的两手真实牌谱、纯空听、暗杠后的听牌），**真引擎与移植版结论一致**。研究的语料提取与钱流对齐
（Python，数据处理，规则允许）留用；判定这一步已由真代码独立确认。

顺带纠正我自己夜里的一个错误怀疑：我曾担心这三条改动「带着剩余枚数的味道」，而向听的传统定义不看剩余。
方向错了——它们讲的是**手里的 4 张**，不是**场上可见的 4 张**。`Shanten` 确实不看剩余枚数，这正确。
由此得出一条要写进术语表的推论：**`Ukeire` 有效牌为空 ≠ 不听牌**，别拿它当听牌判据。

## 提案 R-A（研究 agent 提，需人裁）：「5枚目を待つ聴牌」目前结构上不可配置

天凤只看暗牌（暗杠算副露手牌）；EMA 竞技规则 §3.3.8 看整手牌含副露，同一手判不听。
`HandShape` 只有 `nakiCount`、不带副露牌面，**结构上给不出另一侧的答案**。实测影响 4/72,204 座位。
雀魂取哪边无一手资料。

已作为 ADR-0004 的决定 6 记录（按天凤，且明确记下「不可配置」这个事实）。若将来要支持 EMA，
`HandShape` 需携带副露牌面——宽改动，届时立新 ADR。**这条不阻塞 M0。**

## 备注 N-10：一条方法论警告（研究 agent 提的，我认为最有价值）

> 03 的 4000 手离线固件，其**期望值与 oracle 同源**——那是互相印证，不是独立验证。

同一个陷阱在 13 票会更大：如果对拍的期望值来自我们自己的实现，对拍只证明了「实现与自己一致」。
真正的外部事实是**天凤自己的 `deltas`**（钱流），它与我们的实现无因果关系。已给 13 票加了
「荒牌流局逐座位对拍天凤清算」这个独立锚点，并要求用真引擎重做一次（研究那次用的是移植版）。

## 备注 N-11：`<Compile>` 拆成按领域分组的 ItemGroup

`.fsproj` 的 `<Compile>` 是个全局有序列表，而它是并行开发的**强制序列化点**——今晚几乎每个
并行波都在这一处冲突（02∥03、04∥07、05∥08、10∥05）。F# 的自顶向下文件顺序在单人开发时
是优点（依赖方向一目了然），N 个 agent 并发加文件时就是瓶颈。

已把两个工程的列表拆成按领域分组的多个 `ItemGroup`（引擎 29 文件 → 8 组，测试 49 文件 → 7 组），
每组带一句「什么该放这里」。改不同领域的 agent 从此编辑不同区块。

**安全性靠一条断言**：拼接后的文件顺序**逐字节不变**（脚本里 assert 过，CI 也绿）。
没有重排任何文件——落地顺序恰好已经按领域聚簇，8 段和 7 段都是天然连续的。
**不许为了分组好看而重排**：那会静默改变名字解析（就近定义优先，见裁决 D-3 的教训）。

## 备注 N-12：Thoth 的后端选择（Newtonsoft vs System.Text.Json）——不换

用户问起两者区别。结论记下来免得将来重议：

引擎只 reference `Thoth.Json.Core`（抽象层），编解码器用的是 Thoth 自己的 Elm 风格组合子，
**不是 Newtonsoft 的 API**。Newtonsoft 只出现在 CLI 与测试的 `open` 与 `PackageReference`。
所以换后端 = 改一行 `open` + 一个包引用，编解码器一个字不动。

- STJ：in-box、约快 2×、span/UTF-8 低分配、RFC 8259 严格（拒注释与尾逗号）；
  但 `JsonDocument` 是 `IDisposable`、`JsonElement` dispose 后失效，对解码库是真麻烦，
  Thoth 的 STJ 后端也还在 0.4.x
- Newtonsoft：`JToken` 是普通可变对象树无生命周期约束，后端成熟

**不换的理由**：M1 经 Fable 进浏览器用的是 JS 后端，**两个 .NET 后端都不在产品路径上**；
且 Thoth 自己那层比底层库慢，2× 吃不到。

**若 13 票将来要啃 57MB 全量语料**（现在只用 1.1MB 固件），正解不是换后端，
而是在测试 harness 里写个 `Utf8JsonReader` 流式读取**绕过 Thoth**——牌谱解析只需认几个字段，
不需要类型安全的组合子。这条不够 ADR 门槛（换后端是一行的事），记在这里就够。

## 备注 N-13：宽改动（R-5）的验证方式值得成为惯例

`Seat` 从 `int` 换成真类型，307 处、35 个文件，**要求行为一字不变**。重构 agent 的验证方式：
把基线 `okuuwpzk` 整树抽到 `/tmp` 单独构建，用 `dotnet fsi` 对**两个引擎**跑同样四个种子的完整对局，
逐项比事件频次（dahai 1232 / tsumo 1120 / chi 76 / pon 36 / ryukyoku 16 完全相同）。

这比「CI 绿」强得多：CI 只证明现有断言没破，而现有断言是照现有实现写的。**跑同种子比输出**
证明的是「换类型前后是同一台引擎」——这正是 wide refactor 唯一该有的验收标准。
它也只可能在 `dotnet fsi` 存在的前提下便宜（今早那条纪律的第二次回报）。

顺带纠正我自己：N-8 记的 chi 80 / pon 32 是 09 落地时的数，10、11 之后本就不同，与 R-5 无关。
**我把不同时点的观测值写在同一处却没标时点**，差点被当成回归证据。

## 备注 N-14：类型换掉之后「顶出」的三处，不是 bug 但值得看

R-5 报「真 bug：没有」，但换类型顶出三处**过去表达得出、其实不该表达**的东西：

1. 负数座位不再可构造（`Seat.ofIndex` 返回 option）
2. `GameTests` 拿 `SeatCount - 1` 当座位用——现在得经 `Seat.first` / 枚举
3. 属性测试自己手写取模——现在全仓库唯一的取模在 `Seat` 的私有 `shift` 里

三处都不是运行期 bug（输入本来就合法），但它们是「透明别名让错误可表达」的具体样本。
`Seat.index` 从 307 处收缩到 26 处，且只剩渲染层与 wire——这就是这次重构买到的东西。

## 备注 N-15：风格约定成文，起因是用户指出「LLM 写 F# 像 Python」

用户的观察是对的，但我第一轮测错了方向：我去查「管道密度」与「中间临时变量串接」，
结果 6915 行里只有 1 处中间变量串接，管道密度 4.9/100 行——数据不支持「不会用管道」。

用户随后点明真正的特征：**括号嵌套**，即 `f (g (h x))` 这种从里往外读的写法。
换了检测器才看见：`fun x -> f (g x)` 3 处、深度 ≥3 嵌套 66 处（多数是管道内 lambda 体，
确认要改的 2 处）。**我第一轮的检测器完全漏了它**——因为我在查语句层面的形状，
而问题在表达式内部。

更值得记的是归因：**引擎里的 Python 味比预期少，真正的坏例子出在没有约束的一次性脚本里**
（规则 1 的反面教材是 `scripts/fsi/shanten-probe.fsx`，我写的，用户改对的）。
引擎受 `Result<_, 具名 DU>`、DU 穷尽、一概念一文件这些约束挤着，顺带就函数式了；
`.fsx` 一无约束，我就退回命令式手写惯性。

**规律与今早那次 Python 弯路同源：约束写下来就守得住，没写下来就走训练分布里最常见的那条路。**
今天为此写下了三份约束（fsi 优先、资源纪律、F# 风格），都是同一个教训的不同切面。

风格文档特意写了「规则 4：三种不许强行管道的情况」——防止 `code-review` 把它当 cargo cult 执行。
管道密度不是指标，我在文档里明说了 4.9/100 行这个数字本身不说明任何事。

**待办**：文档里列出的 5 处确认可改（3 处 encoder 的 `>>`、`KyokuStart.fs:75`、`RiichiState.fs:121`）
等 12 票落地后一起清，纯机械改动、编译器全程护着。现在 ws-a 有 agent 在跑，不动 `.fs`。

## 备注 N-16：`jj commit` 会把用户的未提交手改一起卷走

今天发生两次：

1. 用户手改 `AgariShape.fs`（规则 3 的实例），被我 `jj commit -m "docs: 写下 F# 风格约定"` 连带提交进 docs commit
2. 我随后修文档时，改动又落进了刚分出来的 style commit（因为 `@` 就是它）

根因：**jj 没有暂存区，`jj commit` 快照整个工作副本**。这在单人手工操作时是优点（不会漏提交），
在「人与调度器共用一个工作副本」时是陷阱——我提交的东西不一定全是我写的。

**纪律**：调度器每次 `jj commit` / `jj describe` 前先 `jj st`，出现不是自己写的路径就
`jj squash --from X --into Y <路径>` 按路径分出来，并在描述里标明作者。
反向也一样：改文档前确认 `@` 是不是某个已有语义的 commit。

这条也解释了为什么并行 agent 各用独立 workspace 是对的——它们各有自己的工作副本，
互不快照对方。只有默认工作区是人与调度器共用的，唯一的冲突面就在这里。

## 备注 N-17：把风格文档用在真代码上，两次否掉了文档自己

改 `KyokuStart.fs:75` 与 `RiichiState.fs:121` 时，文档第一版的建议**两次都不对**：

**一、`RiichiState.fs:121` 建议的「给中间值命名」会破坏 `&&` 的短路。**
`tenpaiDahai` 要对 14 张候选打牌各跑一次 `Shanten`（约 84µs）。原本非门清的手牌在第一个子句
就被短路掉，抽成 `let` 之后每次都算——**那是性能回归，不是风格改进**。
已改成管道（与用户在 `AgariShape` 那处同形），并把「布尔链里不许抽 `let`」写成文档的限制 B。

**二、`KyokuStart.fs:75` 的深度是构造子给的，不是变换链给的。**
`Error(NoDoraIndicator(List.length (Wall.deadWall dealt)))` 里只有内两层是变换；
`Error(Foo(x))` 是 ADT 构造的常规形状，而且同一个 `match` 的邻居都是那个形状。
写成 5 段管道会为了消灭括号破坏局部一致性。已只管道化变换部分，并写成文档的限制 A。

**教训**：风格规则**必须在真代码上跑一遍才算立住**。我第一版写规则时只看了检测器报的位置，
没看那些位置周围是什么——一处的邻居决定了什么叫「一致」，一处的 `&&` 决定了什么叫「等价」。
这与今早 `Seq` vs `Array.sumBy` 那次同源：**相信机制而不做验证**。区别是这次代价仍然是零，
因为改完跑了 CI（502 绿）。

## 备注 N-18：CI 变慢的瓶颈是 collection 粒度，不是测试写得差

用户问 CI 为什么要 1m43s。量出来的因果链：

1. **墙钟 103s ≈ `GameStateProperties` 一个类的累计 106.6s**。xunit 并行粒度是 collection、
   默认一类一 collection、**类内串行**，于是 16 条重属性串成关键路径，32 核实际并行度只有 2.5×
2. 每 case 约 100ms ≈ 一整局 145 步 ⇒ 每步约 690µs，主要来自 `RiichiState.canDeclare` 里的
   `tenpaiDahai`（枚举 14 张打法各算一次 Shanten）

**修法是 4 行**：四个胖模块的 `[<Properties>]` 各加 `Parallelism = 8`（FsCheck 3 的参数，
在单条属性内部并行跑 100 个 case）。103s → **34s**，`MaxTest` 未动、断言未动、零覆盖损失。
已写成风格文档规则 7。

## 备注 N-19：`Shanten` 的成本高度依赖手牌，我引用过的「6µs」是错误条件下测的

| 手牌 | 单次 `Shanten.calculate` |
|---|---|
| 随机手牌（03 票与我之前测的） | 5.8 – 13.7 µs |
| 结构化的 14 张（`1m1m1m 2m3m4m5m6m7m8m9m 9p9p 5z`） | **150 µs** |
| 枚举 14 张打法（`tenpaiDahai` 的实际形状） | **2100 µs** |

25 倍差距。原因：搜索空间随手牌结构化而膨胀，而**随机手牌在真实对局里不具代表性**——
牌局进行中手牌恰恰会变得结构化。

**这是今天第三次「我引用的数字是在错误条件下测的」**（前两次：`Seq` vs `Array.sumBy` 的纳秒 vs 微秒、
N-13 里把不同时点的事件频次写在一处）。规律很清楚：**benchmark 的输入分布必须与真实用法一致，
否则数字是假的。** 14 票的 soak 规模估算要用结构化手牌的数字，不是随机手牌的。

据此开了 15 票（`tenpaiDahai` 前置闸门），**阻塞于 14**——它是热路径上的优化且有正确性风险，
M0 该先验收现有引擎，再优化。

## 备注 N-20：编译器**不**保证抓到漏 match ——我前面 N-4/N-7 的说法要修正

我在 N-4 与 N-7 里说过「今晚所有跨票的类型级冲突都被编译器抓住了」。**12 票给出了反例。**

它给 `RyuukyokuReason` 加了 6 个 case，需要补的 match 共 **47 处**（引擎 9 + 测试 38）。
它用批量脚本插，**插错了 4 处，而编译器只顶出 3 处**。漏掉的那一处形如：

```fsharp
match reason with
| Fanpai -> false
| KyuushuKyuuhai | SuufonRenda -> true    // ← 新 case 被插进了这一组
```

**穷尽性检查只保证「每个 case 都被覆盖」，不保证「被覆盖到正确的分支」。** 插进错误的
or-pattern 组照样编译通过，行为却反了。它是靠**逐条读 diff** 抓到的。

修正后的说法：**编译器能抓「漏了 case」，不能抓「case 归错组」。** 前者是覆盖性问题（机械可判），
后者是意图问题（只有人或测试能判）。

这条直接约束我自己——今晚我做过四次同类批量补 case（`EndKyoku`/`EndGame`、`Pon`/`Chi`）。
我当时的做法恰好是安全的（先 `sed -n` 打印上下文、用唯一锚点、一处一处改），但那更像运气而非纪律。
**从此立为纪律：批量改 match 之后必须逐条读 diff，编译绿不算完。**

顺带 12 报的第二个坑：跨文件的同名 `type Ryuukyoku` 与 module 会撞出 `FS0250`，
需要手写 `[<CompilationRepresentation(CompilationRepresentationFlags.ModuleSuffix)>]`——
同文件内 F# 自动加，跨文件不加。

## 备注 N-21：随机对局仍只产出一种流局，14 票的覆盖率要求再次被证明必要

12 票落地后，五个种子的随机对局里 `reason` **全部是 `fanpai`**，四类途中流局与流し満貫一次没出。
（同 N-8：和了 0 次、立直 0 次。）

所以 12 票的验收全靠摊好的剧本用例，这是对的。而 14 票的「soak 必须断言每种动作类型至少出现一次」
现在要加一句：**流局形态也要断言覆盖**——否则 `RyuukyokuReason` 的 6 个新 case 在 soak 里等于不存在。

## 备注 N-22：把散文规则变成可执行闸门，第一次跑就暴露我文档里三处错

用户在 `Wall.fs:109` 看到 `FSAC0004 Parentheses can be removed`。查清后写了规则 8 与
`scripts/check-style.sh`（只放零假阳性的机械检查）。**闸门第一次跑，五条里三条炸**——炸的不是代码，
是我两小时前写的文档：

1. **规则 2 我写「现存 3 处」却从没改过**（`Tile.fs` / `Kaze.fs` / `Event.fs` 的 encoder）。
   我在总结里说「两处清完」，那是规则 3 的两处；规则 2 的三处一直躺着。**文档把待办写成了现状。**
2. **规则 5 的基线写 7，闸门数出 9**——我只数了引擎，闸门把 `src/Janpo.Cli` 也算了。
   CLI 那 2 处是 stdin 读行循环，属驱动层，已把闸门收窄到引擎。
3. **规则 7 的阈值（≥5 条属性）实测无意义**：给其余 8 个模块加 `Parallelism`，CI 35s → 37s，
   无收益反略慢。**判据本该是「实测慢不慢」，而静态检查数不出运行时间**——已把规则 7 从闸门里删掉，
   只留文档指导，并把这个实测结论写进去。

三条的共同点：**我写规则时用的是「听起来对的判据」，而不是量出来的判据。** 阈值凭直觉设、
基线凭记忆写、待办与现状混在一处。闸门的价值不只是拦将来的回归，是**当场证伪文档本身**。

顺带一条关于 FSAC0004 的判断：它是纯语法判断，**能判「可删」，不能判「该删」**。
`Wall.fs:109` 那处删掉行为完全一致（实测），但 `... @ if rest > 0 then [ rest ] else []` 里
`@ if` 的并置要读者停一下。括号在那里是分隔符不是噪声——写进规则 8 的第三档。
