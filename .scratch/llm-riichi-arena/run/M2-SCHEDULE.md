# M2 排班表

spec 的 M2 一行：**四 LLM 同桌 + 思考气泡 + 回放导出/导入/URL 分享 + 首页 Demo Paifu 自动播放。**

## 前置票（M1 暴露出来的，不是 spec 的新功能）

按「先让改动变容易，再做那个容易的改动」，这五张排在功能票之前。**五张之间零依赖，可并行。**

| 票 | 状态 | change | 一句话 | 为什么是前置 |
|---|---|---|---|---|
| 41 | **done** | xrxorwkp | 11 条语义不变量 + 12 个反向自证；离线扫 120 种子 21,306 份 prompt 零违反 | 三档对照实验若建在没有语义闸门的渲染层上，分不清「这档更强」与「这档有句错话」 |
| 42 | **done** | tqplnvql | 有主见的选手：立直场次 15→1996、一发 1→1045、供托结转 3→1148；**默认仍是均匀随机**（闸门覆盖需要它） | 没有立直就没有对抗，四家 LLM 会打成平淡的自摸竞赛 |
| 43 | **done** | syvyuwku | | `render_version` 认不出渲染器改动 | 归因工具漏一半，命中率与账单数据就有一半不可解释 |
| 44 | 待派 | | 真牌桌布局 + 「渲染给人」那侧的闸门 | 四个思考气泡挂在竖排列表上没锚点，布局一改气泡得重挂 |
| 45 | 待派 | | 远端 CI 加缓存（6m25s→?）+ 消噪 | 慢 CI 会让人少推、攒着推、本地跳着测 |

## 收尾与裁决落地票（M1 的尾巴，与前置票并行）

| 票 | 状态 | change | 一句话 |
|---|---|---|---|
| 46 | **done** | zutovnyq | | M1 碎账打包（provider id 防撞名、渲染版本显示、页脚年份、死链接、textarea 样式…）；另列了六条**维持原判不做**的 |
| 47 | 待派 | | provider 错误分类：401 这类「再问一遍还是一样」的不重试（实测 255 次里 170 次白烧） |
| 48 | **done** | kmmmykwu | 术语表收四处新词（**获准改 `CONTEXT.md`**，范围锁死六处） |

## 下一波（重启后从这里接）

| 票 | 状态 | 一句话 | 备注 |
|---|---|---|---|
| 43 | **done** | syvyuwku | `render_version` 认不出渲染器改动 | 独立，可与 45/46/47 并行 |
| 45 | 待派 | 远端 CI 加缓存 + 消噪 | 独立，只碰 workflows |
| 46 | **done** | zutovnyq | M1 碎账 + `Persona` 一局内不变要可执行 | 碰面板与若干小处 |
| 47 | 待派 | provider 错误分类（401 不重试） | 碰 Agent 层 `loop.ts` |
| 49 | **done** | zlolwxuk | 把有主见选手的语料掺进语义闸门 | 碰 41 的闸门脚本；与 47 都动 web，错开或接受 rebase |
| 44 | **排最后** | 真牌桌布局 + 渲染给人那侧的闸门 | 会大改 `TablePage.fs` 与 `styles.css`，让 46 先落地 |

**实际派工（2026-08-17 重启后）**：**45 ∥ 46 ∥ 49**。
改了原计划：查明 `renderVersion` 横跨 `Paifu.fs` / `Agent.fs` / `TablePage.fs` / `template.ts` / `loop.ts`，
而票 46 要改面板（人格锁、provider id、显示渲染版本）也在 `TablePage.fs` 与 `Agent.fs`——**43 与 46 会撞**，
所以 43 挪到下一波。49 碰 `web/scripts` 与 CLI，与两者零重叠。
**下一波**：43 ∥ 47，**最后** 44。

## 本波之后剩下的

| 票 | 状态 | 一句话 | 备注 |
|---|---|---|---|
| 43 | **done** | syvyuwku | `render_version` 认不出渲染器改动 **+ 收 47-A**（帮助文字仍写「重试两次」） | 等票 44 落地再派（同碰 `TablePage.fs`） |
| 47 | **done** | wxusksxk | 判据取 HTTP 语义（4xx=请求本身不对→不值得；5xx=端点这一刻不行→值得；408/425/429 例外）；演习 255→85 次请求；**零 F# 改动** |
| 44 | **done** | nkyxytqu | 三×三格子、方位跟视角转（判据一处 `Board.position`、参照系写在桌心）；八项断言进 CI；**13 次反向自证全实跑** |
| 50 | **done** | 罕见形态各归其主 | 两种从无闸门变有闸门；三种如实记为「谁也到不了」 |

## M2 前置票收官

**十张全部落地**（41 语义闸门 / 42 有主见选手 / 43 渲染版本 / 44 真牌桌 / 45 CI 消噪 / 46 碎账与人格锁 /
47 错误分类 / 48 术语表 / 49 喂语料 / 50 罕见形态），另有主人临时提出的 **51**（副露位置编码）在跑。

M1 暴露的三件事逐条闭合：
- **prompt 没有语义闸门** → 41 立 11 条不变量、49 把语料喂饱（「立直后全摸切」0 → 1,470 次执行）
- **随机 bot 碰不到立直/供托** → 42 有主见选手（立直场次 15 → 1996 / 2000 颗种子）
- **`render_version` 认不出渲染器改动** → 43 补上渲染器摘要，三道闸门守住「漏不掉」

顺带清掉的：真牌桌（44）、CI 噪音与那笔被实测推翻的缓存账（45）、M1 碎账与人格一局内不变（46）、
白烧的重试（47，255→85 请求）、术语表四词（48）、罕见流局形态各归其主（50）。

## 主人插进来的三票（CI 提速与语料）

| 票 | 状态 | 一句话 | 备注 |
|---|---|---|---|
| 55 | **done** | ylotrqrt | 每决策 34 长数组 596.8 → 5.9（101×）；浏览器 1.37–1.49×；公开签名一字未改 |
| 56 | **done** | vzuvtqup | `ci.sh` 2m00.9s → 39.2s；`dotnet test` CPU 507.7s → 126.4s；用例数一个没降 |
| 57 | **done** | zzqwttpw | 把真牌谱对拍扩到更多完整场次 | 主人要求先提速再扩 |

三票的事实依据（本机分段实测、远端 4 核的推论、以及那处「九条轨迹算了八条不用」的发现）
记在 `DECISIONS.md` 的「CI 提速的事实调查」一节。

## 长期课题：引擎性能（主人 2026-08-17 定为定期课题）

登记册 `docs/research/engine-performance-track.md`——**每轮落地后加一行**，
让下一轮不必翻三千行 DECISIONS 才知道上一轮量到哪、哪些前提悬空。
一轮 = 一张研究票（只量、不动 `src/`）+ 视结论一张实现票。

| 轮 | 票 | 状态 | 一句话 |
|---|---|---|---|
| 1–3 | —（主人给的分支） | 已并入 | 函数级不是瓶颈；瓶颈在调用形状与浏览器原语 |
| 4 | 55 | **done** | B 级暂存缓冲：每决策 34 长数组 596.8 → 5.9 |
| 5 | **58** | **done** | 研究 `Observation.ofState`（每决策最大的一段，O(n²)/局）；
入口假设：**29a 已造好增量维护，牌桌在用，决策路径没用上** |

## 扩语料与研究挖出的三票

| 票 | 状态 | 一句话 |
|---|---|---|
| 59 | **done**（vplzwsoz，续做） | 两个真 bug：连杠时明杠宝牌翻太晚（28 局）+ 大明杠岭上开花的责任支付天凤不采用（24/24）；**修完要把那些场加进固件** |
| 60 | **done**（smtktsys） | 一条永远红不了的闸门：增量维护与一次性 fold 的一致性（第六例「执行体存在但空转」） |
| — | 不做 | 票 58 建议的 `forSeatWith`：CI 收益 0.33%、扩语料收益 0，只剩产品路径 2.2×，按登记册规矩第 4 条**判为不做** |

| 61 | **done**（vyplrnks，续做） | 同族另外两条恒真属性（票 60 只报未改）+ `SeatStream` 进术语表 + 全仓库扫同族 |

| 62 | **done**（vmyklrox，续做） | 推到百万场：先量 6ms/局的分布，再流式扩到整年包（上游有 18 个年度包）；oracle 改成「先筛后取」 |

| 63 | **done**（pnomtokm） | 2025 整年语料的两个新 bug 族：立直后暗杠判太严（E20）+ 同巡振听没被自家鸣牌解除（F4）；验收顺带证 59 在整年语料上归零 |

| 65 | **done**（lyvltyyy） | G 族：大三元包牌荣和的本场归属（4 场，59 修掉 B 后现形）；顺带核两个 Ruleset 开关的解码器一致 |

| 66 | **done**（ttskxyqk） | 引擎性能第 6 轮实现：waits 向听前置闸 + canRiichi 单次向听（预验证 2.06×） |

| 67 | **done**（nulxlnvx） | 第二批收词：重试判据核对 + MinkanRinshanSekinin + Doujun/RiichiAnkanMentsuUnchanged + Honba 措辞（第六次授权） |
| 68 | **done**（onwtrqtp） | 扫剩余 17 个年度包（2009–2024，从新往旧）；四分类扩五分类（+年代口径）；oracle 超 500 场停下交清单 |

## 天凤数据正确性战役：收官（主人裁决 2026-08-17）

2,512,424 场 / 26,822,804 局；引擎 bug 七个全修；终态零引擎错误。57-5（自有数据管道）搁置。
详细档案见 DECISIONS 同名段。**对拍这条线不再主动扩**；引擎侧新 bug 的来源换轴
（三麻/别的口径、或引擎之外的层）。

| 69 | **done**（rlquvkkk） | README 加对拍声称（措辞：引擎错误零场，**不是**零差异） |

## 功能票（**已切片，2026-08-18 与主人过完六个分叉**）

十张票（70–79）。切片判据与主人当场裁掉的六个分叉记在 `DECISIONS.md`「M2 功能切片」一节。

| 票 | 状态 | 阻塞于 | 一句话 |
|---|---|---|---|
| 70 | **done**（yqnymypp） | — | 拆 `TablePage.fs` → `TableState` / `AgentLine` / `TableBoard` / `TablePanel` + 58 行转出外壳；**零行为改动** |
| 71 | **done**（wkppxssw） | 70 | 首页第一眼是 Demo 回放（自动播）；`Source = Live \| Replay` 接缝；Live 搬到 `?table=1`，四道闸门跟着迁 |
| 72 | **done**（ruyvwuum） | 71 | 27-E 三开关（长度 / 赤 / 食断）+ 重定超时默认值（思考实测 17–180 s） |
| 73 | **done**（mpzxmvoq） | 72 | `ModelProfile`（模型档案，**获准改 `CONTEXT.md`**，范围锁死两处）+ 四席各自绑定选手 |
| 74 | **done**（yoltwynv） | 73 | 响应阶段同时问多席；等待与票号按座位（串行的话一局一小时以上） |
| 75 | **done**（qlskzkvk） | 71 | 回放时间轴：拖动 + 逐事件步进（游标是权威，局面是它的 fold） |
| 76 | **done**（qtpwzkvo） | 75 | 思考气泡：每席一个、三态、点开看全文；Live 与回放两边都要 |
| 77 | **done**（pvnoymvy） | — | `Paifu.stripAudit` + `Share.fs` + `share/payload.ts`（压缩换成带校验和的 `deflate`）；东风战 4,842 / 半庄 7,720 字符；闸门 7 趟 → 9 趟 |
| 78 | **done**（kuupkqss） | 75, 77 | 导入 JSON + 打开分享链接（牌谱回流那半边） |
| 79 | **done**（plznsozz；parked 的那条红线由调度器改锚点解掉） | 74, 76, 80 | 用真 key 跑四席真局，换掉首页占位资产；M2 的端到端验收与第一份真实语料账 |
| 84 | **done**（tpqnomlx，开关 −18%；B 级判不做） | — | 浏览器分配悬崖：`Int32Array(34)` 每调用一次现新建 11 处；先量 `--typedArrays false` 一个开关，再判要不要把 `ShantenScratch` 推广到剩下六处 |
| 85 | 待派（引擎/回放侧，与 Web 那几张零重叠） | — | 导入半场牌谱时末帧会多出一张推断牌山造的假牌（票 79 §5 撞出来的机制，产品侧也踩得到） |
| 81 | 待派 | — | 视角成为信息闸门（气泡与状态线按视角掩蔽）+ 气泡只放一句话；**含术语表第八次授权** |
| 82 | 待派 | 81 | 左右两家的牌竖着摆（真麻将布局）+ 名牌带档案/档位；票 44 与 76 的坐标断言要重校 |
| 83 | 待派 | — | **页面重排**：操作控件贴到牌桌（第⑦条）+ 配桌表单让出中心、四席一眼看全（第⑥条） |
| 80 | **done**（oolxzqnk，待主人看图） | 74, 78 | 纸面牌谱风静态打磨：CC0 牌面资产（赤五/牌背）+ 暖纸/朱红/靛青配色；主人加的（W5 期间立票） |

**波次**（`70` 与 `77` 零重叠，第一波就能并行）：

| 波 | 票 | 并行理由 |
|---|---|---|
| W1 | **两张都并了** 70（testId 75 个与字符串字面量全集逐字相同）∥ 77（压缩档的因果我重量了一遍，结论照旧、理由改掉：见 77-1 的复核引文） | 一个只搬 `src/Janpo.Web/Table*.fs`，一个只碰 `Paifu` 的一次变换 + 新的 TS 模块 + 一道闸门 |
| W2 | 71 **已并**（复核：CI 十趟全绿；两张截图我自己看了，另裁一条 71-8 见 `DECISIONS.md`） | 独占：它改地址与四道闸门，谁都别同时动 `web/scripts/` |
| W3 | 72 **已并**（CI 十一趟全绿；72-1 与 72-3 我裁了，见 `DECISIONS.md`）∥ 75 **已并**（集成时补了一处签名撞车；另裁「上帝视角不摆里宝牌」落票 76） | 配桌面板 vs 回放与时间轴（拆文件之后是两组文件；MVU 文件的改动各自集中，接受一次 rebase） |
| W4 | 73 **已并**（`CONTEXT.md` 只改了授权那两处，我逐字核过；真跑一局：1873 ms/手中位数、缓存命中 45%、0 兜底）∥ 76 **已并**（三处撞车照预测修掉；ci-web 的 set -e 被我解冲突弄丢又找回，红演习过） | 模型档案与四席绑定 vs 气泡（面板 vs 牌桌视图） |
| W5 | 74 **已并**（引擎零改动核过；并发 flash 档只省 ~7%，机制真伪靠假端点 708/1400ms 判）∥ 78 **已并**（三处冲突全是「两边都要」型；闸门二十道/十四趟） | 并发问话（MVU 与 Agent 状态） vs 导入与分享（地址与文件入口） |
| W6 | 80 **已并**（赤牌断言改硬了；dist +0.78MB / 首屏 +29ms 判可收；白板与牌背两问留主人） | 独占：美化横切所有视图文件，且要动几何/颜色断言，谁都别与它并行 |
| W8 | **可派** 81 ∥ 83（79 已并，阻塞解除） | 气泡与状态线（`TableState`/`AgentLine`/`ThinkingBubble`） vs 配桌面板（`TablePanel`）——`styles.css` 两处远隔 |
| W9 | 82 | 独占：它重排牌桌几何，票 44 八项 + 票 76 矩形不相交都要重校 |
| W10 | → M3 | 真人坐席：动作输入 UI（可见性规则已由票 81 打下地基） |
| W7 | **已派** 79（ws-a） | 收官：真 key 跑真局换资产 + 定妆照（拍在美化之后）；不改代码 |

**真 key**：`/tmp/deepseek_key`，模型 `deepseek-v4-flash`（与 M1 同一把同一档）。
现成口子是 `JANPO_KEY_FILE` + `--llm`（见 `verify-export.mjs`）。**CI 零真实请求**（硬约束 4）；
73 / 74 各允许一局的真跑冠气，79 允许 ~6 场东风战。

## 待主人裁的存货

**已清空。** 2026-08-17 分诊：29 条开着的挂账里，4 条被前置票吸收、5 条是纯记录、16 条调度器直接定、
4 条主人已裁（见 `DECISIONS.md`「待裁项分诊」一节）。**现在没有任何一条在等主人。**
新挂账仍按老规矩：agent 报上来 → 调度器分诊 → 只有真需要主人的才进索引。

## 跑批判据（对 M2 同样生效）

全部收进 **`docs/agents/judgments.md`**（18 条，四节，每条附真实案例与出处）。
派工单指那份清单即可，**不要再在这里或派工单里手抄**——抄一次就多一份会各自过期的副本
（这正是判据 15「哪些地方描述过它」自己说的事）。

清单不覆盖的两样，派工时仍要单独给：**硬约束**（`AGENTS.md` 人人适用的那六条 +
`docs/agents/workbook.md` 的工作区、不许问人、park、交付物、资源预算；当次的机器上限在 `RUNBOOK.md`）
与**那一票自己的验收标准**。

